---
phase: 23-configuration-test-coverage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/gui/test_presets.py
  - tests/api/test_settings.py
  - Makefile
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Running pytest tests/gui/test_presets.py exercises Scalper and Swing presets and asserts freq, horizon, tau, and enter_threshold values"
    - "Running pytest tests/api/test_settings.py includes a settings round-trip that saves sub-hourly freq/horizon via PUT and verifies via GET"
    - "make test-release includes the new preset and settings tests and passes"
  artifacts:
    - path: "tests/gui/test_presets.py"
      provides: "Scalper and Swing preset parameter value assertions"
      contains: "test_scalper_preset_parameters"
    - path: "tests/api/test_settings.py"
      provides: "Preset-based and sub-hourly settings round-trip tests"
      contains: "test_put_preset_scalper_round_trip"
    - path: "Makefile"
      provides: "test-release target includes preset and settings test files"
      contains: "test_presets.py"
  key_links:
    - from: "tests/gui/test_presets.py"
      to: "src/bitbat/gui/presets.py"
      via: "imports SCALPER, SWING constants and asserts their field values"
      pattern: "SCALPER\\.freq.*5m"
    - from: "tests/api/test_settings.py"
      to: "src/bitbat/api/routes/system.py"
      via: "PUT /system/settings with preset=scalper then GET verifies round-trip"
      pattern: "preset.*scalper"
---

<objective>
Add automated test coverage for Scalper/Swing preset parameters and sub-hourly settings round-trip to close TEST-01 and TEST-02.

Purpose: Prevent regressions in preset definitions and API settings persistence for the full sub-hourly frequency range. This is the final phase in v1.4 Configuration Alignment.
Output: Extended test files, updated Makefile test-release target.
</objective>

<execution_context>
@/home/eimi/.claude/get-shit-done/workflows/execute-plan.md
@/home/eimi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/gui/test_presets.py
@tests/api/test_settings.py
@src/bitbat/gui/presets.py
@src/bitbat/api/routes/system.py
@Makefile

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly -- no codebase exploration needed. -->

From src/bitbat/gui/presets.py:
```python
@dataclass
class Preset:
    name: str
    description: str
    freq: str
    horizon: str
    tau: float
    enter_threshold: float
    color: str
    icon: str

SCALPER = Preset(name="Scalper", freq="5m", horizon="30m", tau=0.003, enter_threshold=0.55, ...)
SWING = Preset(name="Swing", freq="15m", horizon="1h", tau=0.007, enter_threshold=0.60, ...)
PRESETS: dict[str, Preset]  # keys: scalper, conservative, balanced, aggressive, swing
def get_preset(name: str) -> Preset  # falls back to BALANCED
```

From tests/api/client.py:
```python
class SyncASGIClient:
    def get(self, url: str, **kwargs) -> httpx.Response
    def request(self, method: str, url: str, **kwargs) -> httpx.Response
```

From tests/api/test_settings.py (existing fixture):
```python
@pytest.fixture()
def client(tmp_path, monkeypatch) -> SyncASGIClient:
    # Patches _USER_CONFIG_PATH to tmp_path, creates app via create_app()
```

API endpoint contracts:
- PUT /system/settings accepts {"preset": "scalper"} and resolves Scalper preset values
- PUT /system/settings accepts {"freq": "5m", "horizon": "30m"} for direct sub-hourly values
- GET /system/settings returns {"freq": "...", "horizon": "...", "tau": ..., "enter_threshold": ..., ...}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scalper and Swing preset parameter assertion tests</name>
  <files>tests/gui/test_presets.py</files>
  <action>
Add a new test class `TestScalperSwingParameters` to `tests/gui/test_presets.py` with these tests:

1. `test_scalper_preset_parameters` -- Assert SCALPER.freq == "5m", SCALPER.horizon == "30m", SCALPER.tau == 0.003, SCALPER.enter_threshold == 0.55.

2. `test_swing_preset_parameters` -- Assert SWING.freq == "15m", SWING.horizon == "1h", SWING.tau == 0.007, SWING.enter_threshold == 0.60.

3. `test_scalper_display_uses_sub_hourly_labels` -- Assert SCALPER.to_display()["Update Frequency"] contains "5 min" and SCALPER.to_display()["Forecast Period"] contains "30 min".

4. `test_swing_display_uses_sub_hourly_labels` -- Assert SWING.to_display()["Update Frequency"] contains "15 min" and SWING.to_display()["Forecast Period"] contains "1 hour".

5. `test_scalper_and_swing_in_registry` -- Assert get_preset("scalper") is SCALPER and get_preset("swing") is SWING (explicit identity check beyond the existing test_get_preset_known which already covers this -- skip if redundant, keep only the parameter value tests).

Place the new class after the existing `TestPresetRegistry` class. Import SCALPER and SWING (already imported at the top of the file).

Do NOT modify existing tests. Only add the new class.
  </action>
  <verify>
    <automated>poetry run pytest tests/gui/test_presets.py -v -k "scalper or swing" --tb=short</automated>
  </verify>
  <done>All new Scalper and Swing tests pass, asserting exact parameter values (freq, horizon, tau, enter_threshold) and human-readable display labels</done>
</task>

<task type="auto">
  <name>Task 2: Add preset settings round-trip tests and wire into test-release</name>
  <files>tests/api/test_settings.py, Makefile</files>
  <action>
**Part A: Add round-trip tests to tests/api/test_settings.py**

Add a new test class `TestSettingsPresetRoundTrip` after the existing `TestSettingsSubHourlyPersistence` class. Use the existing `client` fixture (already patched for tmp_path).

Tests:

1. `test_put_preset_scalper_round_trip` -- PUT {"preset": "scalper"} to /system/settings, assert 200. Then GET /system/settings, assert response has freq=="5m", horizon=="30m", tau==0.003, enter_threshold==0.55, preset=="scalper".

2. `test_put_preset_swing_round_trip` -- PUT {"preset": "swing"} to /system/settings, assert 200. Then GET /system/settings, assert response has freq=="15m", horizon=="1h", tau==0.007, enter_threshold==0.60, preset=="swing".

3. `test_sub_hourly_freq_horizon_round_trip` -- PUT {"freq": "5m", "horizon": "30m"} to /system/settings, assert 200. Then GET /system/settings, assert freq=="5m" and horizon=="30m". This covers the explicit sub-hourly value persistence case (not preset-based).

Do NOT modify existing tests. Only add the new class.

**Part B: Wire into Makefile test-release**

Add a new line to the `test-release` target in Makefile that runs:
```
poetry run pytest tests/gui/test_presets.py tests/api/test_settings.py -q
```

Append this as the 4th `poetry run pytest` line in the test-release target, after the existing 3 lines. Use a tab character for indentation (Makefile requirement).
  </action>
  <verify>
    <automated>poetry run pytest tests/api/test_settings.py -v -k "round_trip" --tb=short && make test-release</automated>
  </verify>
  <done>All round-trip tests pass (preset-based and direct sub-hourly), and make test-release includes the new test files and passes cleanly</done>
</task>

</tasks>

<verification>
1. `poetry run pytest tests/gui/test_presets.py tests/api/test_settings.py -v --tb=short` -- all tests pass (existing + new)
2. `make test-release` -- passes with the new test files included
3. `poetry run pytest` -- full suite passes with zero regressions
</verification>

<success_criteria>
1. Scalper (5m/30m) and Swing (15m/1h) preset parameter values are tested with exact assertions
2. Settings round-trip saves preset=scalper via PUT, reloads via GET, values match exactly
3. Settings round-trip saves sub-hourly freq=5m/horizon=30m via PUT, reloads via GET, values match
4. make test-release includes the preset and settings test files and passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-configuration-test-coverage/23-01-SUMMARY.md`
</output>
