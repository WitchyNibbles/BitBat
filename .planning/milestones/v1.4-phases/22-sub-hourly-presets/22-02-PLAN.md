---
phase: 22-sub-hourly-presets
plan: "02"
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - dashboard/src/components/PresetSelector.tsx
  - dashboard/src/pages/Settings.tsx
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03]

must_haves:
  truths:
    - "React dashboard shows Scalper preset card that sets freq=5m and horizon=30m"
    - "React dashboard shows Swing preset card that sets freq=15m and horizon=1h"
    - "Selecting a preset in the React dashboard updates the freq and horizon dropdowns to match"
    - "Dropdown options display human-readable labels (e.g. '5 min' not '5m')"
  artifacts:
    - path: "dashboard/src/components/PresetSelector.tsx"
      provides: "5 preset cards including Scalper and Swing"
      contains: "scalper"
    - path: "dashboard/src/pages/Settings.tsx"
      provides: "Preset selection wired to freq/horizon state"
      contains: "handlePresetSelect"
  key_links:
    - from: "dashboard/src/components/PresetSelector.tsx"
      to: "dashboard/src/pages/Settings.tsx"
      via: "onSelect callback with preset id"
      pattern: "onSelect.*scalper|swing"
    - from: "dashboard/src/pages/Settings.tsx"
      to: "dashboard/src/api/client.ts"
      via: "api.updateSettings with preset name"
      pattern: "updateSettings.*preset"
---

<objective>
Add Scalper and Swing preset cards to the React dashboard and wire preset selection to automatically set freq/horizon, with human-readable dropdown labels.

Purpose: Operators need one-click preset selection in the React dashboard that configures sub-hourly freq/horizon pairs. The dropdown labels should show human-readable text instead of raw code strings.
Output: React PresetSelector with 5 preset cards; Settings page that auto-updates freq/horizon on preset select; dropdowns showing human-readable labels.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sub-hourly-presets/22-01-SUMMARY.md

@dashboard/src/components/PresetSelector.tsx
@dashboard/src/components/PresetSelector.module.css
@dashboard/src/pages/Settings.tsx
@dashboard/src/pages/Settings.module.css
@dashboard/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scalper and Swing cards to PresetSelector with human-readable labels</name>
  <files>dashboard/src/components/PresetSelector.tsx, dashboard/src/components/PresetSelector.module.css</files>
  <action>
Update `dashboard/src/components/PresetSelector.tsx`:

1. **Add two new preset definitions** to the `PRESETS` array:

   Scalper (insert before conservative -- fastest first):
   - id: "scalper"
   - name: "Scalper"
   - description: "5m freq, rapid sub-hourly signals"
   - icon: Use `Zap` from lucide-react (lightning bolt)
   - color: "#F59E0B" (amber)
   - freq: "5m"
   - horizon: "30m"

   Swing (insert after aggressive -- or at end):
   - id: "swing"
   - name: "Swing"
   - description: "15m freq, sub-hourly swing signals"
   - icon: Use `TrendingUp` from lucide-react
   - color: "#8B5CF6" (purple)
   - freq: "15m"
   - horizon: "1h"

   Final order: scalper, conservative, balanced, aggressive, swing (fastest to most active to most methodical).

2. **Add a `label` field** to `PresetDef` interface for human-readable freq/horizon display. Add a helper function `formatFreqHorizon(freq: string, horizon: string): string` that maps:
   - "5m" -> "5 min"
   - "15m" -> "15 min"
   - "30m" -> "30 min"
   - "1h" -> "1 hour"
   - "4h" -> "4 hours"
   - "1d" -> "1 day"
   - "24h" -> "24 hours"

   Export this function so Settings.tsx can use it for dropdown labels.

3. **Update the `.params` span** in the card to use human-readable labels: `{formatFreqHorizon(preset.freq)} / {formatFreqHorizon(preset.horizon)}` instead of raw `{preset.freq} / {preset.horizon}`.

4. **Update `PresetSelector.module.css`:** Change grid-template-columns from `repeat(3, 1fr)` to `repeat(5, 1fr)`. Add a responsive breakpoint for smaller screens: at max-width 1024px use `repeat(3, 1fr)`, at max-width 640px use `repeat(2, 1fr)`.

5. **Update the `PresetSelectorProps` interface** to also pass out the preset's freq and horizon when selected. Add an optional `onPresetData` callback: `onPresetData?: (freq: string, horizon: string) => void`. When a card is clicked, call both `onSelect(preset.id)` and `onPresetData?.(preset.freq, preset.horizon)`.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor/dashboard && grep -c "scalper\|swing" src/components/PresetSelector.tsx | xargs -I{} test {} -ge 2 && echo "PresetSelector has scalper and swing" && grep -q "formatFreqHorizon" src/components/PresetSelector.tsx && echo "formatFreqHorizon helper exists" && grep -q "repeat(5" src/components/PresetSelector.module.css && echo "5-column grid confirmed"</automated>
  </verify>
  <done>PresetSelector shows 5 cards (scalper, conservative, balanced, aggressive, swing) with human-readable freq/horizon labels; grid is 5 columns; onPresetData callback exposes freq/horizon to parent</done>
</task>

<task type="auto">
  <name>Task 2: Wire preset selection to auto-set freq/horizon in Settings page</name>
  <files>dashboard/src/pages/Settings.tsx</files>
  <action>
Update `dashboard/src/pages/Settings.tsx`:

1. **Import `formatFreqHorizon`** from `../components/PresetSelector.tsx` (exported in Task 1).

2. **Add a `handlePresetSelect` function** that:
   - Takes preset id and looks up the preset's freq/horizon from a local PRESET_MAP (a simple Record mapping id to {freq, horizon}):
     ```
     const PRESET_MAP: Record<string, { freq: string; horizon: string }> = {
       scalper: { freq: '5m', horizon: '30m' },
       conservative: { freq: '1h', horizon: '24h' },
       balanced: { freq: '1h', horizon: '4h' },
       aggressive: { freq: '1h', horizon: '1h' },
       swing: { freq: '15m', horizon: '1h' },
     };
     ```
   - Calls `setPreset(id)`, `setFreq(PRESET_MAP[id].freq)`, `setHorizon(PRESET_MAP[id].horizon)`.
   - This ensures selecting a preset automatically fills the correct freq/horizon values.

   Alternatively, use the `onPresetData` callback from Task 1 to receive freq/horizon directly:
   ```tsx
   <PresetSelector
     selected={preset}
     onSelect={setPreset}
     onPresetData={(f, h) => { setFreq(f); setHorizon(h); }}
   />
   ```

   Choose the approach that is cleaner -- the `onPresetData` callback is preferred as it avoids duplicating preset data.

3. **Human-readable dropdown labels:** Update the freq and horizon `<select>` dropdowns to show human-readable text. Change from:
   ```tsx
   <option key={f} value={f}>{f}</option>
   ```
   to:
   ```tsx
   <option key={f} value={f}>{formatFreqHorizon(f)}</option>
   ```
   This satisfies PRES-03: dropdown options show "5 min", "15 min", etc. instead of raw "5m", "15m".

4. **Save behavior adjustment:** Currently `handleSave` only sends freq/horizon when `showAdvanced` is true. When a preset is selected, the freq/horizon should ALWAYS be sent (they're part of the preset). Update the save logic:
   - If showAdvanced is true, send the manually-selected freq/horizon (current behavior).
   - If showAdvanced is false, send `null` for freq/horizon so the API resolves from the preset name (current behavior -- which is correct since the API's PUT handler resolves preset values).

   Actually, the current behavior is already correct: when showAdvanced is false, preset is sent and the API resolves freq/horizon from the preset. When showAdvanced is true, explicit freq/horizon override the preset. No change needed here.

5. **Ensure dropdown `<option>` values remain the raw strings** (e.g. "5m") while display text is human-readable. The `value` attribute must stay as the API-compatible string.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor/dashboard && grep -q "onPresetData\|handlePresetSelect\|PRESET_MAP" src/pages/Settings.tsx && echo "Preset-to-freq/horizon wiring exists" && grep -q "formatFreqHorizon" src/pages/Settings.tsx && echo "Human-readable labels in dropdowns" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Selecting Scalper preset sets freq=5m and horizon=30m in the React dashboard; selecting Swing sets freq=15m and horizon=1h; dropdown options show human-readable labels like "5 min", "1 hour"; TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
1. `cd dashboard && npx tsc --noEmit` -- TypeScript compiles cleanly
2. `cd dashboard && npx eslint src/components/PresetSelector.tsx src/pages/Settings.tsx` -- lint passes (if eslint configured)
3. Grep for "scalper" and "swing" in PresetSelector.tsx -- both present
4. Grep for "formatFreqHorizon" in both PresetSelector.tsx and Settings.tsx -- shared helper used
5. Grep for "repeat(5" in PresetSelector.module.css -- 5-column grid
6. Verify the `<option>` elements use `value={f}` (raw) and display text via formatFreqHorizon (human-readable)
</verification>

<success_criteria>
- React PresetSelector shows 5 cards: Scalper, Conservative, Balanced, Aggressive, Swing
- Selecting Scalper card sets freq=5m and horizon=30m in Settings state
- Selecting Swing card sets freq=15m and horizon=1h in Settings state
- Dropdown `<option>` display text shows human-readable labels (5 min, 15 min, etc.)
- Dropdown `<option>` values remain raw API-compatible strings (5m, 15m, etc.)
- TypeScript compiles without errors
- No regressions in existing Settings save/reset/load behavior
</success_criteria>

<output>
After completion, create `.planning/phases/22-sub-hourly-presets/22-02-SUMMARY.md`
</output>
