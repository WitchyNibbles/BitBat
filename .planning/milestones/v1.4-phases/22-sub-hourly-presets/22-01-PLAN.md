---
phase: 22-sub-hourly-presets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/gui/presets.py
  - streamlit/pages/1_⚙️_Settings.py
autonomous: true
requirements: [PRES-01, PRES-02, PRES-03]

must_haves:
  truths:
    - "Scalper preset exists with freq=5m and horizon=30m"
    - "Swing preset exists with freq=15m and horizon=1h"
    - "Format helpers display '5 min', '15 min', '30 min' for sub-hourly frequencies"
    - "Streamlit Settings page shows Scalper and Swing as selectable presets"
    - "Streamlit advanced dropdowns include sub-hourly freq/horizon options"
  artifacts:
    - path: "src/bitbat/gui/presets.py"
      provides: "Scalper and Swing preset definitions with sub-hourly format helpers"
      contains: "SCALPER"
    - path: "streamlit/pages/1_⚙️_Settings.py"
      provides: "5-preset selector with sub-hourly advanced options"
      contains: "scalper"
  key_links:
    - from: "src/bitbat/gui/presets.py"
      to: "src/bitbat/api/routes/system.py"
      via: "get_preset() called in update_settings PUT handler"
      pattern: "get_preset.*scalper|swing"
    - from: "streamlit/pages/1_⚙️_Settings.py"
      to: "src/bitbat/gui/presets.py"
      via: "list_presets() and get_preset() imports"
      pattern: "from bitbat.gui.presets import"
---

<objective>
Add Scalper (5m/30m) and Swing (15m/1h) presets to the backend preset registry and Streamlit GUI, with human-readable format helpers for all sub-hourly frequencies.

Purpose: Operators need named trading presets that configure sub-hourly freq/horizon pairs in a single click. The backend preset definitions must exist before the React dashboard can reference them (API resolves preset names via get_preset()).
Output: Updated presets.py with 5 presets + sub-hourly format helpers; Streamlit Settings showing all 5 presets with sub-hourly advanced dropdown options.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-api-config-alignment/20-01-SUMMARY.md
@.planning/phases/21-settings-ui-expansion/21-01-SUMMARY.md

@src/bitbat/gui/presets.py
@streamlit/pages/1_⚙️_Settings.py
@src/bitbat/api/routes/system.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Scalper and Swing presets with sub-hourly format helpers</name>
  <files>src/bitbat/gui/presets.py</files>
  <action>
Add two new Preset instances to `src/bitbat/gui/presets.py`:

1. **SCALPER** preset:
   - name: "Scalper"
   - description: "Rapid sub-hourly predictions for active scalp trading. Highest frequency, shortest horizon."
   - freq: "5m"
   - horizon: "30m"
   - tau: 0.003
   - enter_threshold: 0.55
   - color: "#F59E0B" (amber/yellow)
   - icon: (use a lightning bolt unicode character or similar -- match the style of existing icons)

2. **SWING** preset:
   - name: "Swing"
   - description: "Sub-hourly signals for swing positions. Balances speed with confirmation time."
   - freq: "15m"
   - horizon: "1h"
   - tau: 0.007
   - enter_threshold: 0.60
   - color: "#8B5CF6" (purple)
   - icon: (use a wave or trend unicode character -- match the style of existing icons)

3. Add both to the `PRESETS` dict with keys `"scalper"` and `"swing"`.

4. Update `_format_freq()` mapping to include sub-hourly entries:
   - "5m": "Every 5 min"
   - "15m": "Every 15 min"
   - "30m": "Every 30 min"

5. Update `_format_horizon()` mapping to include sub-hourly entries:
   - "15m": "15 min ahead"
   - "30m": "30 min ahead"

These additions satisfy PRES-03's requirement that format helpers display human-readable labels for sub-hourly frequencies.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run python -c "
from bitbat.gui.presets import get_preset, list_presets, PRESETS
# Check presets exist
assert 'scalper' in PRESETS, 'scalper preset missing'
assert 'swing' in PRESETS, 'swing preset missing'
# Check Scalper values
s = get_preset('scalper')
assert s.freq == '5m', f'scalper freq={s.freq}'
assert s.horizon == '30m', f'scalper horizon={s.horizon}'
# Check Swing values
w = get_preset('swing')
assert w.freq == '15m', f'swing freq={w.freq}'
assert w.horizon == '1h', f'swing horizon={w.horizon}'
# Check format helpers
assert '5 min' in s._format_freq().lower() or '5m' not in s._format_freq(), f'bad freq format: {s._format_freq()}'
assert '30 min' in s._format_horizon().lower() or '30m' not in s._format_horizon(), f'bad horizon format: {s._format_horizon()}'
assert '15 min' in w._format_freq().lower() or '15m' not in w._format_freq(), f'bad freq format: {w._format_freq()}'
# Check list_presets returns 5
assert len(list_presets()) == 5, f'expected 5 presets, got {len(list_presets())}'
print('All preset checks passed')
"</automated>
  </verify>
  <done>PRESETS dict contains scalper (5m/30m) and swing (15m/1h) entries; _format_freq and _format_horizon return human-readable strings for 5m, 15m, 30m</done>
</task>

<task type="auto">
  <name>Task 2: Update Streamlit Settings page for 5 presets and sub-hourly advanced options</name>
  <files>streamlit/pages/1_⚙️_Settings.py</files>
  <action>
Update `streamlit/pages/1_⚙️_Settings.py` to display all 5 presets and support sub-hourly frequencies:

1. **Preset selector section:** Currently creates 3 columns and iterates over `["conservative", "balanced", "aggressive"]`. Change to 5 columns iterating over all preset keys from `list_presets()`. The layout should be: `st.columns(5)` and iterate over `["scalper", "conservative", "balanced", "aggressive", "swing"]` (ordered from fastest to slowest trading style). Each column renders the same card pattern (icon, name, description, select button) already used for the existing 3.

2. **Advanced settings dropdowns:** Currently hardcoded:
   - freq options: `["1h", "4h", "1d"]`
   - horizon options: `["1h", "4h", "24h"]`

   Replace with the full set of supported frequencies sorted by duration:
   - freq options: `["5m", "15m", "30m", "1h", "4h", "1d"]`
   - horizon options: `["15m", "30m", "1h", "4h", "24h"]`

   The `index` parameter for `st.selectbox` must handle values that may not be in the old hardcoded lists. Use a safe index lookup: `options.index(cfg["freq"]) if cfg["freq"] in options else 0`.

3. **Help text section:** Update the "When to use each strategy" to include Scalper and Swing descriptions alongside the existing three.

Do NOT change the save/reset logic or any other behavior -- only the preset display and advanced dropdown options.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run python -c "
import ast, sys
# Parse the Settings page and check for scalper/swing references
with open('streamlit/pages/1_⚙️_Settings.py') as f:
    content = f.read()
assert 'scalper' in content.lower(), 'scalper not found in Settings page'
assert 'swing' in content.lower(), 'swing not found in Settings page'
assert '5m' in content, '5m freq option not found'
assert '15m' in content, '15m freq option not found'
assert '30m' in content, '30m freq/horizon option not found'
print('Streamlit Settings page checks passed')
"</automated>
  </verify>
  <done>Streamlit Settings shows 5 preset cards (Scalper, Conservative, Balanced, Aggressive, Swing); advanced dropdowns include 5m, 15m, 30m frequencies and 15m, 30m horizons</done>
</task>

</tasks>

<verification>
1. `poetry run python -c "from bitbat.gui.presets import list_presets; assert len(list_presets()) == 5"` -- 5 presets registered
2. `poetry run python -c "from bitbat.gui.presets import get_preset; s=get_preset('scalper'); assert s.freq=='5m' and s.horizon=='30m'"` -- Scalper correct
3. `poetry run python -c "from bitbat.gui.presets import get_preset; w=get_preset('swing'); assert w.freq=='15m' and w.horizon=='1h'"` -- Swing correct
4. `poetry run python -c "from bitbat.gui.presets import get_preset; print(get_preset('scalper').to_display())"` -- format helpers show human-readable labels
5. `poetry run ruff check src/bitbat/gui/presets.py streamlit/pages/1_⚙️_Settings.py` -- lint passes
6. `poetry run pytest tests/ -x --timeout=120` -- no regressions
</verification>

<success_criteria>
- 5 presets (scalper, conservative, balanced, aggressive, swing) available via list_presets()
- Scalper preset: freq=5m, horizon=30m
- Swing preset: freq=15m, horizon=1h
- Format helpers produce human-readable labels for 5m, 15m, 30m
- Streamlit Settings displays all 5 presets as selectable cards
- Streamlit advanced dropdowns include sub-hourly options
- All existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/22-sub-hourly-presets/22-01-SUMMARY.md`
</output>
