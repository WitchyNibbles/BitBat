# Milestone v1.6: Accuracy Recovery & Technical Debt Remediation

**Status:** ✅ SHIPPED 2026-03-12
**Phases:** 29-37
**Total Plans:** 19

## Overview

Diagnosed the live accuracy collapse, fixed the broken training/inference/validation path, added an accuracy guardrail, and retired the four deferred technical-debt items while closing the two audit gaps that originally blocked milestone archive readiness.

## Phases

### Phase 29: Diagnosis

**Goal**: Operators can identify which pipeline stage caused the live accuracy collapse and have a documented, reproducible trace before any fix is applied  
**Depends on**: Phase 28  
**Plans**: 2 plans

Plans:

- [x] 29-01: Create diagnostic test scaffold (pipeline stage trace + ROOT_CAUSE.md structural tests)
- [x] 29-02: Write ROOT_CAUSE.md and confirm full diagnostic suite passes

**Details:**
Phase 29 established the diagnosis-first workflow, added pipeline stage trace tests, and committed the root-cause document before any fix code landed.

### Phase 30: Fix & Reset

**Goal**: The root cause of live accuracy ~1% is fixed in code, a clean reset procedure is available via CLI, and a retrained model achieves directional accuracy above random baseline  
**Depends on**: Phase 29  
**Plans**: 3 plans

Plans:

- [x] 30-01: Fix three root-cause bugs (`train.py`, `infer.py`, `validator.py`) + unit tests
- [x] 30-02: Invert Phase 29 diagnostic tests + add `bitbat system reset` CLI command
- [x] 30-03: Accuracy gate verification + operator reset + retrain checkpoint

**Details:**
Phase 30 corrected the classification objective, three-class inference decoding, and validator `tau` wiring, then added the reset command and inverted diagnosis assertions to become post-fix gates.

### Phase 31: Accuracy Guardrail

**Goal**: The monitor agent alerts operators when realized directional accuracy falls below a configurable threshold  
**Depends on**: Phase 30  
**Plans**: 1 plan

Plans:

- [x] 31-01: Add accuracy guardrail config + check function + agent wiring + behavioral tests

**Details:**
Phase 31 added the runtime guardrail for realized accuracy collapse with configurable thresholds and deterministic alert coverage.

### Phase 32: CLI Decomposition

**Goal**: Split `cli.py` into focused command-group modules with no behavioral change  
**Depends on**: Phase 28  
**Plans**: 3 plans

Plans:

- [x] 32-01: Create `cli/` package skeleton: `_helpers.py` + `__init__.py` + `commands/__init__.py`
- [x] 32-02: Move 8 simple command groups into `commands/`
- [x] 32-03: Move model + monitor groups, refactor `model_cv` for C901, update test monkeypatch targets

**Details:**
Phase 32 completed the CLI package split and removed the monolithic `cli.py` implementation, with later formal re-verification completed in Phase 37.

### Phase 33: Path Centralization

**Goal**: Replace all hardcoded `Path("models")` / `Path("metrics")` sites with config-driven resolution  
**Depends on**: Phase 28  
**Plans**: 2 plans

Plans:

- [x] 33-01: Add `resolve_models_dir` / `resolve_metrics_dir` helpers to config loader + TDD scaffold
- [x] 33-02: Sweep all hardcoded path literals and turn structural grep tests green

**Details:**
Phase 33 unified artifact path resolution so operators can relocate model and metrics outputs through config alone.

### Phase 34: DB Unification

**Goal**: Consolidate ORM and raw sqlite access into one consistent DB layer  
**Depends on**: Phase 28  
**Plans**: 3 plans

Plans:

- [x] 34-01: Establish `AutonomousDB` read facade, transient-lock retry/circuit-breaker, and atomic write helpers
- [x] 34-02: Migrate `/system` API routes to `AutonomousDB` and add fail-fast API coverage
- [x] 34-03: Migrate Streamlit DB helpers, unify retraining transactions, and add a structural no-`sqlite3` gate

**Details:**
Phase 34 made `AutonomousDB` the single runtime DB access path while preserving the existing `autonomous.db` schema.

### Phase 35: XGBoost Fix

**Goal**: Replace the regression objective with a classification objective and retrain the model path  
**Depends on**: Phase 30  
**Plans**: 2 plans

Plans:

- [x] 35-01: Make walk-forward and optimizer classification-aware for label targets while preserving regression fallback
- [x] 35-02: Wire CLI CV/optimization to labels and surface PR-AUC-aware summaries

**Details:**
Phase 35 aligned the evaluation, optimization, and CLI selection stack with the already-correct classifier training path.

### Phase 36: Live Recovery Evidence Closure

**Goal**: Verify the operator reset + retrain path end-to-end on fresh runtime data and formally close `FIXR-03`  
**Depends on**: Phase 35  
**Plans**: 2 plans

Plans:

- [x] 36-01: Add a recovery-evidence harness, config-aware diagnosis resolution, and docs/contracts for the reset + retrain flow
- [x] 36-02: Run the sandbox reset + retrain evidence flow, update Phase 30 verification, and record passed `FIXR-03` evidence

**Details:**
Phase 36 closed the deferred live-evidence gap with fresh sandbox recovery proof: `239/300` correct realized predictions (`79.67%`), `zero_return_count=1`, and a passing diagnosis suite.

### Phase 37: CLI Decomposition Re-Verification

**Goal**: Reconcile the stale Phase 32 verification artifact and close `DEBT-01` with a passed verification record  
**Depends on**: Phase 36  
**Plans**: 1 plan

Plans:

- [x] 37-01: Re-run CLI regression evidence, rewrite the stale Phase 32 verification artifact, and close `DEBT-01` traceability

**Details:**
Phase 37 reran the CLI regression surface (`45 passed`), reconfirmed the C901 gate, and replaced the stale Phase 32 gap report with an archive-clean passed artifact.

---

## Milestone Summary

**Key Decisions:**

- Keep diagnosis-before-fix discipline so the root cause is committed before repair work.
- Prefer fail-fast, operator-visible safeguards over silent degradation in monitor and DB paths.
- Use config-driven roots for models, metrics, and recovery sandboxes to keep runtime state relocatable.
- Close audit blockers with fresh saved evidence instead of relying on inference from later green code.

**Issues Resolved:**

- Live accuracy collapse traced to objective mismatch, sign-only inference, and validator tau wiring.
- Missing post-reset proof for `FIXR-03` replaced with saved sandbox recovery evidence.
- Stale Phase 32 verification artifact replaced with current passed CLI evidence.
- Deferred tech debt on CLI structure, artifact paths, DB access, and XGBoost objective removed.

**Issues Deferred:**

- Next milestone goals are intentionally undefined until a fresh milestone is opened.

**Technical Debt Incurred:**

- None recorded at milestone close.

---

_For current project status, see [ROADMAP.md](/home/eimi/projects/ai-btc-predictor/.planning/ROADMAP.md)._
