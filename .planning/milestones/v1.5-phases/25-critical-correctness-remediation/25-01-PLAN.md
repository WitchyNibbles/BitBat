---
phase: 25-critical-correctness-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/retrainer.py
  - src/bitbat/cli.py
  - tests/autonomous/test_retrainer_cli_contract.py
  - tests/model/test_cv_metric_roundtrip.py
autonomous: true
requirements: [CORR-01, CORR-02]

must_haves:
  truths:
    - "The autonomous retrainer subprocess command no longer passes --tau to features build"
    - "CV metric writing and reading use the same key name (mean_directional_accuracy) without a confusing alias"
    - "A test catches any future CLI argument mismatch between retrainer and the actual CLI interface"
    - "A round-trip test proves cv_summary.json write-then-read returns the same directional accuracy score"
  artifacts:
    - path: "src/bitbat/autonomous/retrainer.py"
      provides: "Fixed subprocess command without --tau, simplified _read_cv_score cascade"
      contains: "features.*build"
    - path: "tests/autonomous/test_retrainer_cli_contract.py"
      provides: "CLI contract test verifying subprocess args match CLI interface"
      min_lines: 20
    - path: "tests/model/test_cv_metric_roundtrip.py"
      provides: "Round-trip consistency test for cv_summary.json write/read"
      min_lines: 25
  key_links:
    - from: "src/bitbat/autonomous/retrainer.py"
      to: "src/bitbat/cli.py"
      via: "subprocess command arguments must match Click CLI options"
      pattern: "_run_command.*features.*build"
    - from: "src/bitbat/cli.py"
      to: "src/bitbat/autonomous/retrainer.py"
      via: "cv_summary.json key names must match between writer (cli.py:781) and reader (retrainer.py:71)"
      pattern: "average_balanced_accuracy|mean_directional_accuracy"
---

<objective>
Fix the two highest-severity correctness bugs found in the Phase 24 audit: the retrainer subprocess CLI contract (CORR-01, CRITICAL) and the CV metric key naming fragility (CORR-02, HIGH).

Purpose: The retrainer currently fails at autonomous retraining time because it passes `--tau` to `features build` which does not accept it. The CV metric key naming uses `average_balanced_accuracy` as an alias for `mean_directional_accuracy`, creating a confusing fragile cascade. Both are one-fix-one-test discipline fixes.

Output: Fixed retrainer.py, updated cli.py key naming, 2 new test files
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-audit-baseline/AUDIT-REPORT.md (Sections 6 and 7 for CORR-01/02 details)
@src/bitbat/autonomous/retrainer.py
@src/bitbat/cli.py (lines 770-793 for cv_summary.json writer)
@src/bitbat/model/walk_forward.py (WalkForwardResult.summary for key names)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix retrainer CLI contract and add contract test (CORR-01)</name>
  <files>
    src/bitbat/autonomous/retrainer.py
    tests/autonomous/test_retrainer_cli_contract.py
  </files>
  <action>
In `src/bitbat/autonomous/retrainer.py`, remove the `--tau` and `str(self.tau)` arguments from the `_run_command` call to `features build` (lines 200-201). The corrected command should be:

```python
self._run_command([
    "poetry",
    "run",
    "bitbat",
    "features",
    "build",
])
```

The `features build` CLI command reads `tau` from the config file, not from a CLI argument. The `--tau` option does not exist on `features build`.

Create `tests/autonomous/test_retrainer_cli_contract.py` with `@pytest.mark.behavioral` marker and module-level `pytestmark = pytest.mark.behavioral`:

1. **test_retrainer_features_build_has_no_tau_arg**: Monkeypatch `AutoRetrainer._run_command` to capture all calls. Instantiate `AutoRetrainer` with a mock DB. Call `retrain()` (which will call `_run_command` multiple times). Find the `features build` invocation and assert that `"--tau"` is NOT in the argument list.

2. **test_retrainer_subprocess_args_match_cli**: Use Click's `CliRunner` to invoke `bitbat features build --help` and extract the valid option names. Then instantiate `AutoRetrainer`, capture the `features build` subprocess command args, and verify every `--flag` in the subprocess command is a valid CLI option. This is the contract test that catches future argument mismatches automatically.

For both tests, mock the DB with a minimal `AutonomousDB` (use `sqlite:///:memory:`) and monkeypatch `_run_command` to avoid actual subprocess calls. Monkeypatch `_read_cv_score` to return 0.5 and `_training_sample_count` to return 1000.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run pytest tests/autonomous/test_retrainer_cli_contract.py -x -v</automated>
    <manual>Verify --tau no longer appears in retrainer.py features build command</manual>
  </verify>
  <done>
    - The `features build` subprocess command in retrainer.py contains no `--tau` argument
    - test_retrainer_features_build_has_no_tau_arg passes
    - test_retrainer_subprocess_args_match_cli passes and would fail if an invalid flag were re-added
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CV metric key naming and add round-trip test (CORR-02)</name>
  <files>
    src/bitbat/cli.py
    src/bitbat/autonomous/retrainer.py
    tests/model/test_cv_metric_roundtrip.py
  </files>
  <action>
**Fix the key naming in cli.py (the writer):**

In `src/bitbat/cli.py` around line 781, change the key from `"average_balanced_accuracy"` to `"mean_directional_accuracy"`. The value is already `primary_directional.get("mean_directional_accuracy", 0.0)` so the key should match the value's semantic name. This eliminates the confusing alias where the key says "balanced accuracy" but stores directional accuracy.

**Simplify the reader in retrainer.py:**

In `src/bitbat/autonomous/retrainer.py`, simplify `_read_cv_score()` (lines 62-89):

1. Change line 71 from `payload.get("average_balanced_accuracy")` to `payload.get("mean_directional_accuracy")` as the primary key lookup.
2. Keep the champion_decision cascade (lines 75-83) as a fallback for older cv_summary.json files that may still have the old format.
3. Keep the RMSE fallback (lines 85-87) as the final fallback.

**Create round-trip consistency test:**

Create `tests/model/test_cv_metric_roundtrip.py` with `pytestmark = pytest.mark.behavioral`:

1. **test_cv_summary_roundtrip_consistency**: Write a cv_summary.json with the same structure that `model cv` produces (use `tmp_path`). Then instantiate `AutoRetrainer` with monkeypatched `_cv_summary_path` pointing to the tmp file. Call `_read_cv_score()` and assert it returns the exact value that was written under the `mean_directional_accuracy` key.

2. **test_cv_summary_key_names_match_between_writer_and_reader**: Import the key constant (or grep the source) to verify the writer (cli.py) and reader (retrainer.py) reference the same key name. This can use AST inspection or simply import both modules and check the source contains the same key string.

Note: Do NOT add backwards compatibility for the old `average_balanced_accuracy` key in the writer. The writer should only write the correct key going forward. The reader's cascade handles old files.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run pytest tests/model/test_cv_metric_roundtrip.py tests/autonomous/test_retrainer_cli_contract.py -x -v</automated>
    <manual>Grep for "average_balanced_accuracy" in cli.py -- should not appear as a key being written</manual>
  </verify>
  <done>
    - cli.py writes `mean_directional_accuracy` instead of `average_balanced_accuracy` to cv_summary.json
    - retrainer.py reads `mean_directional_accuracy` as the primary key
    - Round-trip test proves write-then-read returns the exact same score
    - All existing tests still pass: `poetry run pytest -x`
  </done>
</task>

</tasks>

<verification>
```bash
# Verify CORR-01: --tau removed from retrainer
grep -n "\"--tau\"" src/bitbat/autonomous/retrainer.py | grep -v "test" && echo "FAIL: --tau still in retrainer" || echo "PASS: --tau removed"

# Verify CORR-02: key renamed
grep -n "average_balanced_accuracy" src/bitbat/cli.py && echo "FAIL: old key still in cli.py writer" || echo "PASS: key renamed"

# Run new tests
poetry run pytest tests/autonomous/test_retrainer_cli_contract.py tests/model/test_cv_metric_roundtrip.py -v

# Run full suite to catch regressions
poetry run pytest -x -q
```
</verification>

<success_criteria>
- `--tau` argument completely removed from the `features build` subprocess command in retrainer.py
- `average_balanced_accuracy` key replaced with `mean_directional_accuracy` in cli.py cv_summary writer
- retrainer.py `_read_cv_score()` reads `mean_directional_accuracy` as primary key
- 4 new tests pass (2 for CORR-01, 2 for CORR-02)
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/25-critical-correctness-remediation/25-01-SUMMARY.md`
</output>
