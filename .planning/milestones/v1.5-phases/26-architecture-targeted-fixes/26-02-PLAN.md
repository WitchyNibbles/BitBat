---
phase: 26-architecture-targeted-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/config/loader.py
  - src/bitbat/api/routes/system.py
  - src/bitbat/common/__init__.py
  - src/bitbat/common/presets.py
  - src/bitbat/common/ingestion_status.py
  - src/bitbat/gui/presets.py
  - src/bitbat/gui/widgets.py
  - tests/config/test_reset.py
  - tests/api/test_no_gui_import.py
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04

must_haves:
  truths:
    - "A reset_runtime_config() function exists in config/loader.py that clears all cached state"
    - "Tests that manipulate config use reset_runtime_config() instead of directly poking _ACTIVE_CONFIG"
    - "api/routes/system.py does not import from gui/ — shared utilities live in a lower layer"
    - "Running tests in any order produces the same results (no config state leaks)"
  artifacts:
    - path: "src/bitbat/config/loader.py"
      provides: "reset_runtime_config function"
      contains: "def reset_runtime_config"
    - path: "src/bitbat/common/presets.py"
      provides: "Preset dataclass and registry relocated from gui/presets.py"
      contains: "class Preset"
    - path: "src/bitbat/common/ingestion_status.py"
      provides: "get_ingestion_status relocated from gui/widgets.py"
      contains: "def get_ingestion_status"
    - path: "tests/config/test_reset.py"
      provides: "Tests for reset_runtime_config function"
      contains: "def test_reset_clears_cached_config"
    - path: "tests/api/test_no_gui_import.py"
      provides: "Structural guard preventing api->gui imports"
      contains: "def test_system_routes_no_gui_import"
  key_links:
    - from: "src/bitbat/api/routes/system.py"
      to: "src/bitbat/common/presets.py"
      via: "import (NOT from gui)"
      pattern: "from bitbat\\.common\\.presets import"
    - from: "src/bitbat/api/routes/system.py"
      to: "src/bitbat/common/ingestion_status.py"
      via: "import (NOT from gui)"
      pattern: "from bitbat\\.common\\.ingestion_status import"
    - from: "src/bitbat/gui/presets.py"
      to: "src/bitbat/common/presets.py"
      via: "re-export for backward compat"
      pattern: "from bitbat\\.common\\.presets import"
---

<objective>
Add config reset for test isolation and eliminate the cross-layer API-to-GUI import dependency.

Purpose: Fix config state leaks between tests (currently tests manually hack `_ACTIVE_CONFIG`) and remove the architectural violation where the API layer imports from the GUI layer.

Output: `reset_runtime_config()` in `config/loader.py`, shared utilities in `bitbat/common/`, updated imports in `api/routes/system.py`, structural guard tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/bitbat/config/loader.py
@src/bitbat/api/routes/system.py
@src/bitbat/gui/presets.py
@src/bitbat/gui/widgets.py
@tests/autonomous/test_orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reset_runtime_config and relocate shared utilities to common layer</name>
  <files>
    src/bitbat/config/loader.py
    src/bitbat/common/__init__.py
    src/bitbat/common/presets.py
    src/bitbat/common/ingestion_status.py
  </files>
  <action>
In `src/bitbat/config/loader.py`:
1. Add a `reset_runtime_config() -> None` function that sets all three module globals to None: `_ACTIVE_CONFIG = None`, `_ACTIVE_PATH = None`, `_ACTIVE_SOURCE = None`. Use the `global` keyword for each. This is the clean public API for test teardown instead of tests reaching into private `_ACTIVE_CONFIG` directly.

Create `src/bitbat/common/__init__.py`:
1. Empty or minimal docstring: `"""Shared utilities used by both API and GUI layers."""`

Create `src/bitbat/common/presets.py`:
1. Move the `Preset` dataclass and all preset definitions (SCALPER, CONSERVATIVE, BALANCED, AGGRESSIVE, SWING), `PRESETS` dict, `DEFAULT_PRESET`, and the public helpers `get_preset` and `list_presets` from `src/bitbat/gui/presets.py` into this file.
2. Keep all imports and logic identical — this is a pure relocation, not a rewrite.

Create `src/bitbat/common/ingestion_status.py`:
1. Move the `get_ingestion_status` function from `src/bitbat/gui/widgets.py` into this file. It's a pure-Python data helper with no Streamlit dependency (the existing code even has a comment noting this: "Data helpers (no Streamlit dependency -- pure Python)").
2. Include necessary imports: `datetime`, `Path`, `Any`, `UTC`, `timedelta`.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run python -c "from bitbat.config.loader import reset_runtime_config; from bitbat.common.presets import Preset, list_presets, get_preset, PRESETS; from bitbat.common.ingestion_status import get_ingestion_status; print('All common imports OK')"</automated>
  </verify>
  <done>`reset_runtime_config()` exists in config/loader.py. `Preset`, `list_presets`, `get_preset` exist in `bitbat.common.presets`. `get_ingestion_status` exists in `bitbat.common.ingestion_status`.</done>
</task>

<task type="auto">
  <name>Task 2: Update importers and add backward-compat re-exports in GUI layer</name>
  <files>
    src/bitbat/api/routes/system.py
    src/bitbat/gui/presets.py
    src/bitbat/gui/widgets.py
    tests/autonomous/test_orchestrator.py
  </files>
  <action>
In `src/bitbat/api/routes/system.py`:
1. Replace the lazy import `from bitbat.gui.widgets import get_ingestion_status` (line 219) with `from bitbat.common.ingestion_status import get_ingestion_status`.
2. Replace the lazy import `from bitbat.gui.presets import list_presets` (line 232) with `from bitbat.common.presets import list_presets`.
3. Replace the lazy import `from bitbat.gui.presets import get_preset, list_presets` (line 355) with `from bitbat.common.presets import get_preset, list_presets`.
4. Verify NO remaining `from bitbat.gui` imports exist in the file.

In `src/bitbat/gui/presets.py`:
1. Replace the entire module body with re-exports from the common layer:
   ```python
   """User-friendly configuration presets for the BitBat GUI.

   Canonical implementation lives in bitbat.common.presets.
   This module re-exports for backward compatibility with GUI-layer callers.
   """
   from bitbat.common.presets import (  # noqa: F401
       AGGRESSIVE,
       BALANCED,
       CONSERVATIVE,
       DEFAULT_PRESET,
       PRESETS,
       SCALPER,
       SWING,
       Preset,
       get_preset,
       list_presets,
   )
   ```
2. This ensures any existing Streamlit code importing from `bitbat.gui.presets` continues to work.

In `src/bitbat/gui/widgets.py`:
1. Remove the `get_ingestion_status` function body.
2. Replace with a re-export: `from bitbat.common.ingestion_status import get_ingestion_status  # noqa: F401` at the top of the "Data helpers" section. Any Streamlit code importing from `bitbat.gui.widgets` still works.
3. Keep all other widget functions unchanged.

In `tests/autonomous/test_orchestrator.py`:
1. Replace the direct `_ACTIVE_CONFIG` manipulation (lines 57-58, 91) with:
   - Before test: `loader_mod.set_runtime_config(...)` or directly `loader_mod._ACTIVE_CONFIG = {...}` (existing pattern works since it's a test).
   - After test (cleanup): `loader_mod.reset_runtime_config()` instead of `loader_mod._ACTIVE_CONFIG = _original`.
   - This demonstrates the intended usage pattern. The test can keep the direct assignment for setup if needed (tests are allowed to reach into internals), but teardown should use `reset_runtime_config()`.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run ruff check src/bitbat/api/routes/system.py src/bitbat/gui/presets.py src/bitbat/gui/widgets.py && poetry run python -c "from bitbat.gui.presets import list_presets; from bitbat.gui.widgets import get_ingestion_status; print('Backward compat OK')" && poetry run python -c "import ast; tree = ast.parse(open('src/bitbat/api/routes/system.py').read()); imports = [n for n in ast.walk(tree) if isinstance(n, ast.ImportFrom) and n.module and 'gui' in n.module]; assert len(imports) == 0, f'Found gui imports: {[n.module for n in imports]}'; print('No gui imports in system.py')"</automated>
  </verify>
  <done>`api/routes/system.py` has zero imports from `gui/`. GUI modules re-export from common layer for backward compat. `test_orchestrator.py` uses `reset_runtime_config()` for cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Add structural guard tests and run full suite</name>
  <files>
    tests/config/test_reset.py
    tests/api/test_no_gui_import.py
  </files>
  <action>
Create `tests/config/` directory if it does not exist.

Create `tests/config/__init__.py` (empty).

Create `tests/config/test_reset.py`:
1. `pytestmark = pytest.mark.behavioral` (this tests actual behavior).
2. Test `test_reset_clears_cached_config`: Call `set_runtime_config()` to cache config, then call `reset_runtime_config()`, then verify `loader._ACTIVE_CONFIG is None` and `loader._ACTIVE_PATH is None` and `loader._ACTIVE_SOURCE is None`.
3. Test `test_get_after_reset_reloads`: Call `set_runtime_config()`, then `reset_runtime_config()`, then `get_runtime_config()` — verify it returns a fresh dict (not None), proving the lazy-load path works after reset.
4. Test `test_reset_is_idempotent`: Call `reset_runtime_config()` twice in a row — no errors raised.

Create `tests/api/` directory if it does not exist.

Create `tests/api/__init__.py` (empty).

Create `tests/api/test_no_gui_import.py`:
1. `pytestmark = pytest.mark.structural` (structural-conformance test).
2. Test `test_system_routes_no_gui_import`: Use `ast.parse` to read `src/bitbat/api/routes/system.py`, walk the AST, collect all `ImportFrom` nodes where `module` contains `"gui"`. Assert the list is empty. This is the CI-safe structural guard that prevents the cross-layer import from being reintroduced.
3. Test `test_api_layer_no_gui_imports`: Broader guard — scan all `.py` files under `src/bitbat/api/` using `pathlib.Path.glob`, parse each, assert none contain `from bitbat.gui` imports. This catches any future api->gui coupling.

Run the full test suite: `poetry run pytest`.
Run lint: `poetry run ruff check src tests`.
Run type check: `poetry run mypy src tests`.
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run pytest tests/config/test_reset.py tests/api/test_no_gui_import.py -v && poetry run pytest && poetry run ruff check src tests</automated>
  </verify>
  <done>Config reset tests pass (clear, reload, idempotent). Structural guard tests pass (no gui imports in api layer). Full test suite passes. Lint passes.</done>
</task>

</tasks>

<verification>
1. `poetry run pytest` passes with no regressions
2. `poetry run ruff check src tests` passes
3. `grep -rn "from bitbat.gui" src/bitbat/api/` returns zero matches
4. `python -c "from bitbat.config.loader import reset_runtime_config; reset_runtime_config(); print('OK')"` works
5. `python -c "from bitbat.common.presets import list_presets; print(list(list_presets().keys()))"` prints preset names
</verification>

<success_criteria>
- `reset_runtime_config()` function exists and clears all cached config state
- Test fixtures use `reset_runtime_config()` for cleanup instead of manually hacking `_ACTIVE_CONFIG`
- `api/routes/system.py` has zero imports from `gui/`
- Shared utilities (`Preset`, `get_ingestion_status`) live in `bitbat/common/`
- GUI modules re-export from common layer for backward compat
- Structural guard tests prevent regression
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/26-architecture-targeted-fixes/26-02-SUMMARY.md`
</output>
