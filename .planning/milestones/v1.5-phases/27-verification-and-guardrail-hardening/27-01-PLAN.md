---
phase: 27-verification-and-guardrail-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - poetry.lock
  - .github/workflows/ci.yml
  - src/bitbat/autonomous/agent.py
  - src/bitbat/autonomous/orchestrator.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/backtest/metrics.py
  - src/bitbat/cli.py
  - src/bitbat/contracts.py
  - src/bitbat/ingest/news_cryptocompare.py
  - src/bitbat/ingest/news_gdelt.py
  - src/bitbat/ingest/prices.py
autonomous: true
requirements:
  - ARCH-05
  - ARCH-06

must_haves:
  truths:
    - "Running `poetry run ruff check src/` exits 0 after C901 is enabled (all 11 pre-existing violations suppressed)"
    - "Running `poetry run ruff check src/ --select C901` identifies zero unresolved violations"
    - "Running `poetry run lint-imports` exits 0 on the current clean codebase"
    - "Adding a `from bitbat.gui import anything` inside `bitbat.api` causes `lint-imports` to exit non-zero"
    - "CI lint job runs `lint-imports` and blocks merges that introduce api->gui imports"
    - "A function added to any src module with cyclomatic complexity > 10 (without noqa) causes `ruff check` to fail"
  artifacts:
    - path: "pyproject.toml"
      provides: "C901 in ruff select, [tool.ruff.lint.mccabe] max-complexity=10, [tool.importlinter] forbidden contract"
      contains: "C901"
    - path: ".github/workflows/ci.yml"
      provides: "lint-imports step in lint job"
      contains: "lint-imports"
    - path: "src/bitbat/autonomous/agent.py"
      provides: "noqa suppression on run_once (CC=14)"
      contains: "noqa: C901"
    - path: "src/bitbat/autonomous/orchestrator.py"
      provides: "noqa suppression on one_click_train (CC=17)"
      contains: "noqa: C901"
    - path: "src/bitbat/autonomous/predictor.py"
      provides: "noqa suppression on predict_latest (CC=18)"
      contains: "noqa: C901"
    - path: "src/bitbat/backtest/metrics.py"
      provides: "noqa suppression on summary (CC=12)"
      contains: "noqa: C901"
    - path: "src/bitbat/cli.py"
      provides: "noqa suppression on model_cv (CC=14)"
      contains: "noqa: C901"
    - path: "src/bitbat/contracts.py"
      provides: "noqa suppression on ensure_feature_contract (CC=19)"
      contains: "noqa: C901"
    - path: "src/bitbat/ingest/news_cryptocompare.py"
      provides: "noqa suppressions on _fetch_page (CC=14) and fetch (CC=18)"
      contains: "noqa: C901"
    - path: "src/bitbat/ingest/news_gdelt.py"
      provides: "noqa suppressions on _fetch_chunk (CC=11) and fetch (CC=15)"
      contains: "noqa: C901"
    - path: "src/bitbat/ingest/prices.py"
      provides: "noqa suppression on fetch_yf (CC=11)"
      contains: "noqa: C901"
  key_links:
    - from: "pyproject.toml [tool.ruff.lint] select"
      to: "C901 rule"
      via: "ruff check src/"
      pattern: "C901"
    - from: "pyproject.toml [tool.importlinter]"
      to: "bitbat.api -> bitbat.gui forbidden contract"
      via: "lint-imports"
      pattern: "forbidden_modules.*bitbat.gui"
    - from: ".github/workflows/ci.yml lint job"
      to: "poetry run lint-imports"
      via: "CI step after ruff steps"
      pattern: "lint-imports"
---

<objective>
Add two CI static-analysis gates to prevent recurrence of architecture drift (api→gui cross-layer imports) and complexity creep (functions exceeding cyclomatic complexity 10) found during the v1.5 audit.

Purpose: ARCH-05 and ARCH-06 are the final v1.5 requirements — the "never again" guardrails that make the audit findings durable without requiring manual code review.
Output: import-linter forbidden contract in pyproject.toml enforced by CI; ruff C901 gate active with 11 pre-existing violations suppressed; CI lint job includes `lint-imports` step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-verification-and-guardrail-hardening/27-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable C901 gate and import-linter contract in pyproject.toml (atomic with noqa suppressions)</name>
  <files>
    pyproject.toml
    poetry.lock
    src/bitbat/autonomous/agent.py
    src/bitbat/autonomous/orchestrator.py
    src/bitbat/autonomous/predictor.py
    src/bitbat/backtest/metrics.py
    src/bitbat/cli.py
    src/bitbat/contracts.py
    src/bitbat/ingest/news_cryptocompare.py
    src/bitbat/ingest/news_gdelt.py
    src/bitbat/ingest/prices.py
  </files>
  <action>
    This task is atomic: ALL changes must land together so `ruff check src/` never goes red mid-task. Do not apply pyproject.toml changes before the noqa suppressions, or CI will break.

    **Step 1 — Install import-linter as dev dependency:**
    ```bash
    poetry add --group dev import-linter
    ```
    This updates pyproject.toml `[tool.poetry.group.dev.dependencies]` and regenerates poetry.lock.

    **Step 2 — Update pyproject.toml [tool.ruff.lint] select:**
    The current select list is: `["E", "F", "B", "I", "UP", "S", "C4", "RET", "SIM"]`
    Add `"C901"` at the end: `["E", "F", "B", "I", "UP", "S", "C4", "RET", "SIM", "C901"]`

    **Step 3 — Add [tool.ruff.lint.mccabe] section to pyproject.toml:**
    Add immediately after the `[tool.ruff.lint]` block:
    ```toml
    [tool.ruff.lint.mccabe]
    max-complexity = 10
    ```

    **Step 4 — Add [tool.importlinter] and [[tool.importlinter.contracts]] sections to pyproject.toml:**
    Add at the end of pyproject.toml (before `[build-system]` or after `[tool.pytest.ini_options]`):
    ```toml
    [tool.importlinter]
    root_package = "bitbat"

    [[tool.importlinter.contracts]]
    name = "API layer must not import from GUI layer"
    type = "forbidden"
    source_modules = ["bitbat.api"]
    forbidden_modules = ["bitbat.gui"]
    ```
    Use `forbidden` contract type (NOT `layers`) — the project has too many cross-cutting modules (contracts.py, common, io, timealign) for a strict layers contract. The forbidden contract narrowly targets the specific violation found in the audit.

    **Step 5 — Add # noqa: C901 to the 11 pre-existing violations:**
    These are the exact function definition lines identified by `ruff check --select C901 src/` on 2026-03-07. Add `# noqa: C901` inline on the `def` line only (not on a separate line):

    - `src/bitbat/autonomous/agent.py` line 182: `def run_once(self) -> dict[str, Any]:  # noqa: C901`
    - `src/bitbat/autonomous/orchestrator.py` line 29: `def one_click_train(...)  # noqa: C901` (find the exact def line near line 29)
    - `src/bitbat/autonomous/predictor.py` line 109: `def predict_latest(...)  # noqa: C901`
    - `src/bitbat/backtest/metrics.py` line 20: `def summary(...)  # noqa: C901`
    - `src/bitbat/cli.py` line 544: `def model_cv(...)  # noqa: C901`
    - `src/bitbat/contracts.py` line 79: `def ensure_feature_contract(...)  # noqa: C901`
    - `src/bitbat/ingest/news_cryptocompare.py` line 80: `def _fetch_page(...)  # noqa: C901`
    - `src/bitbat/ingest/news_cryptocompare.py` line 259: `def fetch(...)  # noqa: C901`
    - `src/bitbat/ingest/news_gdelt.py` line 95: `def _fetch_chunk(...)  # noqa: C901`
    - `src/bitbat/ingest/news_gdelt.py` line 244: `def fetch(...)  # noqa: C901`
    - `src/bitbat/ingest/prices.py` line 94: `def fetch_yf(...)  # noqa: C901`

    Read each file first to find the exact def line text before editing. Preserve the existing type annotations and docstrings — only append `  # noqa: C901` to the def line.

    **Step 6 — Verify C901 gate is clean:**
    ```bash
    poetry run ruff check src/
    ```
    Must exit 0. If any C901 violations remain (line numbers may have shifted after prior phase edits), read the file and add the noqa comment to the current def line.

    **Step 7 — Verify import-linter contract passes on clean code:**
    ```bash
    poetry run lint-imports
    ```
    Must exit 0 with "All contracts okay." or similar success output.
  </action>
  <verify>
    <automated>poetry run ruff check src/ && poetry run lint-imports</automated>
    <manual>Confirm ruff outputs no C901 violations and lint-imports prints "All contracts okay."</manual>
    <sampling_rate>run after this task commits, before Task 2 begins</sampling_rate>
  </verify>
  <done>
    `poetry run ruff check src/` exits 0 (C901 enabled, 11 violations suppressed).
    `poetry run lint-imports` exits 0 (forbidden contract passes on clean code).
    pyproject.toml contains `C901` in select, `[tool.ruff.lint.mccabe] max-complexity = 10`, and `[tool.importlinter]` sections.
    All 11 noqa suppressions are on the correct def lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add lint-imports CI step and smoke-test that both gates block violations</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    **Step 1 — Add lint-imports step to the CI lint job:**
    In `.github/workflows/ci.yml`, inside the `lint` job, add a new step after "Ruff format check":
    ```yaml
    - name: Import architecture contracts
      run: poetry run lint-imports
    ```
    The lint job already runs `poetry install --no-interaction` which will install import-linter (added to dev deps in Task 1). No additional install step needed.

    **Step 2 — Smoke-test that the C901 gate blocks new violations:**
    Temporarily add a function with CC > 10 to any src file (e.g., append to the bottom of `src/bitbat/contracts.py`):
    ```python
    def _test_complexity_gate():  # temporary: remove after gate verified
        x = 1
        if x == 1: pass
        if x == 2: pass
        if x == 3: pass
        if x == 4: pass
        if x == 5: pass
        if x == 6: pass
        if x == 7: pass
        if x == 8: pass
        if x == 9: pass
        if x == 10: pass
        if x == 11: pass
    ```
    Run `poetry run ruff check src/ --select C901`. Confirm it exits non-zero with a C901 violation reported for `_test_complexity_gate`. Then remove the temporary function.

    **Step 3 — Smoke-test that the import-linter gate blocks api→gui violations:**
    Temporarily add to `src/bitbat/api/routes/system.py` (at the very top, after existing imports):
    ```python
    import bitbat.gui  # temporary: testing import-linter gate
    ```
    Run `poetry run lint-imports`. Confirm it exits non-zero. Then remove the temporary import.

    **Step 4 — Final clean-state verification:**
    After removing both temporary additions, run:
    ```bash
    poetry run ruff check src/ tests/ && poetry run lint-imports && poetry run pytest tests/ -q --tb=short
    ```
    All three must exit 0. This is the complete "green CI" gate.
  </action>
  <verify>
    <automated>poetry run ruff check src/ tests/ && poetry run lint-imports && poetry run pytest tests/ -q --tb=short</automated>
    <manual>
      Confirm .github/workflows/ci.yml lint job contains "Import architecture contracts" step with `poetry run lint-imports`.
      Confirm smoke tests confirmed C901 and import-linter each blocked the temporary violations.
    </manual>
    <sampling_rate>run as final verification before commit</sampling_rate>
  </verify>
  <done>
    `.github/workflows/ci.yml` lint job includes a `poetry run lint-imports` step after the ruff steps.
    Smoke tests confirmed: ruff C901 exits non-zero on a >10 CC function; lint-imports exits non-zero on an api→gui import.
    Full test suite passes with both gates active.
    Both ARCH-05 and ARCH-06 requirements are fully satisfied.
  </done>
</task>

</tasks>

<verification>
Run these commands in sequence to verify the phase is complete:

```bash
# Gate 1: C901 enabled and all existing violations suppressed
poetry run ruff check src/ tests/

# Gate 2: import-linter forbidden contract passes on clean code
poetry run lint-imports

# Gate 3: Full test suite still green
poetry run pytest tests/ -q --tb=short
```

All three must exit 0.

CI file check:
```bash
grep -n "lint-imports" .github/workflows/ci.yml
```
Must return at least one line.

pyproject.toml check:
```bash
grep -n "C901\|importlinter\|max-complexity" pyproject.toml
```
Must return lines for all three configurations.
</verification>

<success_criteria>
1. `poetry run ruff check src/` exits 0 with C901 active at max-complexity=10
2. `poetry run lint-imports` exits 0 on the clean codebase
3. A function with CC > 10 (no noqa) causes `poetry run ruff check src/` to exit non-zero
4. An `import bitbat.gui` inside `bitbat.api` causes `poetry run lint-imports` to exit non-zero
5. `.github/workflows/ci.yml` lint job includes `poetry run lint-imports` step
6. `poetry run pytest tests/ -q` exits 0 (no regressions from gate additions)
</success_criteria>

<output>
After completion, create `.planning/phases/27-verification-and-guardrail-hardening/27-01-SUMMARY.md` following the summary template.
</output>
