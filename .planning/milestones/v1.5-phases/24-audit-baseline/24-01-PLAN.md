---
phase: 24-audit-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/analytics/test_phase3_complete.py
  - tests/api/test_phase4_complete.py
  - tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
  - tests/autonomous/test_phase8_d1_monitor_schema_complete.py
  - tests/autonomous/test_session3_complete.py
  - tests/autonomous/test_session4_complete.py
  - tests/gui/test_complete_gui.py
  - tests/gui/test_phase10_supported_surface_complete.py
  - tests/gui/test_phase11_runtime_stability_complete.py
  - tests/gui/test_phase12_simplified_ui_regression_complete.py
  - tests/gui/test_phase5_timeline_complete.py
  - tests/gui/test_phase6_timeline_ux_complete.py
  - tests/gui/test_phase7_streamlit_compat_complete.py
  - tests/gui/test_phase8_d2_timeline_complete.py
  - tests/gui/test_phase8_release_verification_complete.py
  - tests/gui/test_phase9_timeline_readability_complete.py
  - tests/model/test_phase5_complete.py
  - .planning/phases/24-audit-baseline/evidence/test-classification.md
autonomous: true
requirements:
  - AUDT-01

must_haves:
  truths:
    - "Every test function in the suite has exactly one pytest marker from {behavioral, integration, structural}"
    - "All pure source-reader milestone-marker tests are deleted from the repo"
    - "Milestone-marker tests that exercise real production code are reclassified as integration or behavioral, not deleted"
    - "A coverage gap matrix shows which v1.5 requirements (CORR-01 through ARCH-06) have existing test coverage and which do not"
  artifacts:
    - path: "pyproject.toml"
      provides: "Pytest marker registration under [tool.pytest.ini_options]"
      contains: "behavioral"
    - path: ".planning/phases/24-audit-baseline/evidence/test-classification.md"
      provides: "Complete classification report with per-file marker assignments, deletion log, and coverage gap matrix"
      min_lines: 50
  key_links:
    - from: "pyproject.toml"
      to: "tests/**/*.py"
      via: "pytest marker registration enables markers used in test files"
      pattern: "markers.*=.*behavioral"
---

<objective>
Classify every test in the 78-file test suite by type, add pytest markers, delete pure milestone-marker tests, and produce a coverage gap matrix mapping v1.5 requirements to existing test coverage.

Purpose: Establish which tests exercise real production logic vs which inflate count with no coverage value, and identify which v1.5 requirements already have test coverage before remediation begins.
Output: Marked test files, cleaned test suite (milestone-markers deleted), test-classification.md evidence file with coverage gap matrix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-audit-baseline/24-CONTEXT.md
@.planning/phases/24-audit-baseline/24-RESEARCH.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register pytest markers and classify all test files</name>
  <files>
    pyproject.toml
    tests/analytics/test_phase3_complete.py
    tests/api/test_phase4_complete.py
    tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
    tests/autonomous/test_phase8_d1_monitor_schema_complete.py
    tests/autonomous/test_session3_complete.py
    tests/autonomous/test_session4_complete.py
    tests/gui/test_complete_gui.py
    tests/gui/test_phase10_supported_surface_complete.py
    tests/gui/test_phase11_runtime_stability_complete.py
    tests/gui/test_phase12_simplified_ui_regression_complete.py
    tests/gui/test_phase5_timeline_complete.py
    tests/gui/test_phase6_timeline_ux_complete.py
    tests/gui/test_phase7_streamlit_compat_complete.py
    tests/gui/test_phase8_d2_timeline_complete.py
    tests/gui/test_phase8_release_verification_complete.py
    tests/gui/test_phase9_timeline_readability_complete.py
    tests/model/test_phase5_complete.py
    .planning/phases/24-audit-baseline/evidence/test-classification.md
  </files>
  <action>
    **Step 1: Register markers in pyproject.toml**

    Add to `[tool.pytest.ini_options]` section:
    ```toml
    markers = [
        "behavioral: behavioral unit test exercising production logic with mocks/isolation",
        "integration: integration test exercising multiple real components together",
        "structural: structural conformance test checking file existence, schemas, CLI contracts",
    ]
    ```
    Keep existing `slow` marker. Do NOT add a `milestone_marker` marker since those tests will be deleted.

    **Step 2: Classify and triage every `*_complete.py` file (17 files)**

    Read each of the 17 `*_complete.py` files. For each file, determine:
    - Does the test body ONLY do `Path(...).read_text()` and assert string presence? -> DELETE (pure source-reader)
    - Does the test import from `bitbat.*`, call production functions, use `tmp_path`, create DB fixtures? -> RECLASSIFY (add `@pytest.mark.integration` or `@pytest.mark.behavioral`)

    Per research (24-RESEARCH.md), confirmed pure source-readers to delete:
    - `tests/autonomous/test_phase19_d1_monitor_alignment_complete.py` (5 tests, all source-readers)

    Files that need individual inspection (may be integration):
    - `tests/autonomous/test_phase8_d1_monitor_schema_complete.py` (seeds SQLAlchemy DB per research)
    - `tests/api/test_phase4_complete.py` (imports bitbat.api)
    - `tests/analytics/test_phase3_complete.py` (imports bitbat.analytics)
    - `tests/model/test_phase5_complete.py` (may import bitbat.model)
    - All `tests/gui/test_phase*_complete.py` files

    **CRITICAL:** Do NOT delete tests that exercise real production code. If unsure, reclassify as `@pytest.mark.integration` and keep.

    **Step 3: Add markers to ALL remaining test files (non-complete files)**

    For every non-complete test file in `tests/`, read the file and add the appropriate marker:
    - `@pytest.mark.behavioral` — tests that exercise a single function/method, use monkeypatch or mocks, assert return values or side effects
    - `@pytest.mark.integration` — tests that exercise multiple real components, use `tmp_path` for real I/O, seed databases, call CLI commands
    - `@pytest.mark.structural` — tests that check file existence, read source code, verify docstrings, check CLI help text, validate schemas without exercising logic

    Add `import pytest` if not already present. Place marker on every test function (not just the module level).

    **Step 4: Produce test-classification.md evidence file**

    Write `.planning/phases/24-audit-baseline/evidence/test-classification.md` containing:

    1. **Summary table**: total tests before/after cleanup, count by marker type
    2. **Deletion log**: each deleted file with reason (pure source-reader) and test count removed
    3. **Reclassification log**: each `*_complete.py` file that was kept, with original name and new marker
    4. **Full classification list**: every test file grouped by marker type (behavioral/integration/structural)
    5. **Coverage gap matrix**: table with columns `Requirement ID | Description | Existing Test Coverage | Gap?`
       - List CORR-01 through CORR-06, LEAK-01, LEAK-02, ARCH-01 through ARCH-06
       - For each, note whether any existing test exercises the relevant production code path
       - Pre-validate known gaps: CORR-01 (retrainer --tau: no test catches this), CORR-05 (test_leakage.py does not exist yet)
  </action>
  <verify>
    <automated>cd /home/eimi/projects/ai-btc-predictor && poetry run pytest --co -q 2>&1 | tail -5</automated>
    <manual>Verify: (1) no PytestUnknownMarkWarning in output, (2) test count is lower than 608 (milestone-markers deleted), (3) all collected tests run without error</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
    - Every test function has exactly one marker from {behavioral, integration, structural}
    - Pure source-reader milestone-marker tests are deleted (test count reduced from 608)
    - `*_complete.py` files that exercise production code are reclassified, not deleted
    - `evidence/test-classification.md` exists with summary table, deletion log, reclassification log, full classification, and coverage gap matrix
    - `poetry run pytest` passes with no unknown marker warnings
  </done>
</task>

</tasks>

<verification>
- `poetry run pytest --co -q` collects tests without warnings
- `poetry run pytest -m behavioral --co -q` collects only behavioral tests
- `poetry run pytest -m integration --co -q` collects only integration tests
- `poetry run pytest -m structural --co -q` collects only structural tests
- No `*_complete.py` file exists that is a pure source-reader
- `.planning/phases/24-audit-baseline/evidence/test-classification.md` contains coverage gap matrix
</verification>

<success_criteria>
Every test file has a marker, milestone-marker tests are cleaned up, and a coverage gap matrix maps all v1.5 requirements to existing test coverage status.
</success_criteria>

<output>
After completion, create `.planning/phases/24-audit-baseline/24-01-SUMMARY.md`
</output>
