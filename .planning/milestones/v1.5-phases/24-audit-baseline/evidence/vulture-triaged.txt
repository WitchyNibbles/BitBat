# Vulture Dead Code Triage Report
# Date: 2026-03-04
# Tool: vulture 2.14 at 80% confidence
# Target: src/bitbat (with tests/ as usage context)
# Whitelist: vulture-whitelist.py (5 entries)
#
# ============================================================
# SUMMARY
# ============================================================
#
# Raw findings:  17 items (vulture.txt)
# Whitelisted:   17 items (all confirmed false positives)
# Triaged:        0 items (no genuine dead code detected)
#
# ============================================================
# FALSE POSITIVE BREAKDOWN
# ============================================================
#
# Category                         Count  Source
# -------------------------------- -----  ---------------------------
# Pytest fixture parameters          15   tests/api/test_metrics.py,
#                                          tests/api/test_predictions.py
# Lambda stub parameters              2   tests/autonomous/test_orchestrator.py
#
# All 17 findings were pytest dependency-injection artifacts or
# monkeypatch stub parameters. Zero findings in src/bitbat.
#
# ============================================================
# REQUIREMENT MAPPING
# ============================================================
#
# No genuine dead code candidates found at 80% confidence.
# There are no findings to map to v1.5 requirements or mark DEFER.
#
# Note: vulture detects structurally unreachable code via AST analysis.
# It does NOT detect semantic bugs like wrong CLI arguments (CORR-01)
# or key mismatches (CORR-02). Those require manual code review or
# integration testing, not static dead-code analysis.
#
# ============================================================
# CONCLUSION
# ============================================================
#
# The src/bitbat codebase has no dead code at the 80% confidence
# threshold. All defined functions, classes, variables, and imports
# in the production source are reachable through internal references
# or test usage.
#
# This is a CLEAN result for AUDT-02. The whitelist captures the
# known false-positive patterns (pytest fixtures, lambda stubs)
# for repeatable future scans.
#
# ============================================================
# TRIAGED FINDINGS (whitelist applied)
# ============================================================
#
# (none -- all raw findings were false positives)
