---
phase: 33-path-centralization
plan: 02
type: auto
wave: 2
depends_on:
  - 33-01
files_modified:
  - src/bitbat/model/evaluate.py
  - src/bitbat/model/train.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/autonomous/continuous_trainer.py
  - src/bitbat/autonomous/agent.py
  - src/bitbat/autonomous/retrainer.py
  - src/bitbat/cli/commands/monitor.py
  - src/bitbat/cli/commands/system.py
  - src/bitbat/cli/commands/model.py
  - src/bitbat/backtest/metrics.py
  - src/bitbat/api/routes/health.py
  - src/bitbat/api/routes/analytics.py
autonomous: true
requirements:
  - DEBT-02

must_haves:
  truths:
    - "No Path('models') or Path('metrics') literal exists anywhere in src/"
    - "Changing models_dir in default.yaml redirects all artifact reads and writes without code changes"
    - "Existing model training tests pass (monkeypatch.chdir still works because defaults are relative)"
    - "Existing backtest metrics tests pass"
    - "Full test suite passes"
  artifacts:
    - path: "src/bitbat/model/evaluate.py"
      provides: "write_window_diagnostics and write_regression_metrics using resolve_metrics_dir()"
      min_lines: 1
    - path: "src/bitbat/model/train.py"
      provides: "_default_model_path using resolve_models_dir()"
      min_lines: 1
    - path: "src/bitbat/autonomous/predictor.py"
      provides: "LivePredictor.__init__ using resolve_models_dir(config)"
      min_lines: 1
    - path: "src/bitbat/autonomous/continuous_trainer.py"
      provides: "ContinuousTrainer inline paths using resolve_models_dir / resolve_metrics_dir"
      min_lines: 1
    - path: "src/bitbat/autonomous/agent.py"
      provides: "_validate_model_preflight using resolve_models_dir()"
      min_lines: 1
    - path: "src/bitbat/autonomous/retrainer.py"
      provides: "_cv_summary_path using resolve_metrics_dir()"
      min_lines: 1
    - path: "src/bitbat/cli/commands/monitor.py"
      provides: "monitor commands using resolve_metrics_dir()"
      min_lines: 1
    - path: "src/bitbat/cli/commands/system.py"
      provides: "system reset using resolve_models_dir()"
      min_lines: 1
    - path: "src/bitbat/cli/commands/model.py"
      provides: "model commands using resolve_metrics_dir()"
      min_lines: 1
    - path: "src/bitbat/backtest/metrics.py"
      provides: "BacktestMetrics.summary() using resolve_metrics_dir()"
      min_lines: 1
    - path: "src/bitbat/api/routes/health.py"
      provides: "_check_model() using resolve_models_dir()"
      min_lines: 1
    - path: "src/bitbat/api/routes/analytics.py"
      provides: "feature_importance() and system_status() using resolve_models_dir()"
      min_lines: 1
  key_links:
    - from: "src/bitbat/model/evaluate.py"
      to: "bitbat.config.loader"
      via: "from bitbat.config.loader import resolve_metrics_dir"
      pattern: "resolve_metrics_dir"
    - from: "tests/config/test_path_resolution.py"
      to: "src/bitbat/"
      via: "subprocess grep — now returns empty stdout because all 16 literals removed"
      pattern: "test_no_hardcoded_models_path.*PASSED"
---

<objective>
Replace all 16 hardcoded Path("models") and Path("metrics") occurrences in src/ with calls to resolve_models_dir() and resolve_metrics_dir() from bitbat.config.loader.

Purpose: Complete the centralization refactor so operators can redirect artifact paths via config YAML. The two structural grep tests from Plan 01 will turn GREEN after this sweep.

Output: 12 files modified; zero remaining Path("models") or Path("metrics") literals in src/; all 6 tests in test_path_resolution.py GREEN; full suite green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-path-centralization/33-01-SUMMARY.md
@.planning/phases/33-path-centralization/33-RESEARCH.md

<interfaces>
<!-- Helpers added in Plan 01. Executor uses these directly — no codebase exploration needed. -->

From src/bitbat/config/loader.py (added in Plan 01):
```python
def resolve_models_dir(config: dict[str, Any] | None = None) -> Path:
    """Return the canonical models directory (default: Path("models") relative to cwd)."""
    cfg = config if config is not None else get_runtime_config()
    raw = str(cfg.get("models_dir", "models"))
    return Path(raw).expanduser()

def resolve_metrics_dir(config: dict[str, Any] | None = None) -> Path:
    """Return the canonical metrics directory (default: Path("metrics") relative to cwd)."""
    cfg = config if config is not None else get_runtime_config()
    raw = str(cfg.get("metrics_dir", "metrics"))
    return Path(raw).expanduser()
```

Complete inventory of all 16 call sites to replace (from RESEARCH.md):
- src/bitbat/model/evaluate.py:71      Path("metrics") / "window_diagnostics.json"  — default arg → use None sentinel
- src/bitbat/model/evaluate.py:131     Path("metrics")                               — default arg → use None sentinel
- src/bitbat/model/train.py:30         Path("models") / f"{freq}_{horizon}"          — _default_model_path()
- src/bitbat/autonomous/predictor.py:59   Path("models")                             — __init__, has config
- src/bitbat/autonomous/continuous_trainer.py:235  Path("metrics") / f"..."         — inline
- src/bitbat/autonomous/continuous_trainer.py:239  Path("models") / f"..." / "xgb.json"  — inline
- src/bitbat/cli/commands/monitor.py:74    Path("metrics")                           — inline CLI fn
- src/bitbat/cli/commands/system.py:34     Path("models")                            — inline CLI fn
- src/bitbat/cli/commands/model.py:294     Path("metrics")                           — inline CLI fn
- src/bitbat/cli/commands/model.py:626     Path("metrics")                           — inline CLI fn
- src/bitbat/autonomous/agent.py:99        Path("models") / f"..." / "xgb.json"     — _validate_model_preflight
- src/bitbat/autonomous/retrainer.py:60    Path("metrics") / "cv_summary.json"       — _cv_summary_path()
- src/bitbat/api/routes/health.py:95      Path("models") / f"..." / "xgb.json"      — _check_model()
- src/bitbat/api/routes/analytics.py:73   Path("models") / f"..." / "xgb.json"      — feature_importance()
- src/bitbat/api/routes/analytics.py:98   Path("models") / f"..." / "xgb.json"      — system_status()
- src/bitbat/backtest/metrics.py:105      Path("metrics")                            — summary()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Path("models") / Path("metrics") in model/, autonomous/, backtest/</name>
  <files>
    src/bitbat/model/evaluate.py,
    src/bitbat/model/train.py,
    src/bitbat/autonomous/predictor.py,
    src/bitbat/autonomous/continuous_trainer.py,
    src/bitbat/autonomous/agent.py,
    src/bitbat/autonomous/retrainer.py,
    src/bitbat/backtest/metrics.py
  </files>
  <action>
    Process each file. For every file, read it first, then apply the replacement, then run ruff check + ruff format.

    **src/bitbat/model/evaluate.py** — 2 occurrences, both are default arg values:
    - Function write_window_diagnostics: change `output_dir: str | Path = Path("metrics")` to `output_dir: str | Path | None = None`. At the start of the function body add: `if output_dir is None: from bitbat.config.loader import resolve_metrics_dir; output_dir = resolve_metrics_dir()`
    - Function write_regression_metrics: same treatment — change default to None, resolve at start of body.
    - Add `from bitbat.config.loader import resolve_metrics_dir` at the top-level imports if the inline import would be called frequently (it is safe inside function body for lazy loading).

    **src/bitbat/model/train.py** — 1 occurrence in _default_model_path():
    - Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace `Path("models") / f"{freq}_{horizon}"` with `resolve_models_dir() / f"{freq}_{horizon}"`

    **src/bitbat/autonomous/predictor.py** — 1 occurrence in __init__:
    - This class already receives config. Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace `self.model_dir = Path("models")` (or similar) with `self.model_dir = resolve_models_dir(config)`
    - Store as instance attribute so _model_path() uses self.model_dir. Read the file to confirm exact current pattern before editing.

    **src/bitbat/autonomous/continuous_trainer.py** — 2 occurrences (lines ~235, ~239):
    - Add import: `from bitbat.config.loader import resolve_models_dir, resolve_metrics_dir`
    - Replace inline `Path("metrics") / f"..."` with `resolve_metrics_dir() / f"..."`
    - Replace inline `Path("models") / f"..." / "xgb.json"` with `resolve_models_dir() / f"..." / "xgb.json"`

    **src/bitbat/autonomous/agent.py** — 1 occurrence in _validate_model_preflight:
    - Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace `Path("models") / f"..." / "xgb.json"` with `resolve_models_dir() / f"..." / "xgb.json"`

    **src/bitbat/autonomous/retrainer.py** — 1 occurrence in _cv_summary_path():
    - Add import: `from bitbat.config.loader import resolve_metrics_dir`
    - Replace `Path("metrics") / "cv_summary.json"` with `resolve_metrics_dir() / "cv_summary.json"`

    **src/bitbat/backtest/metrics.py** — 1 occurrence in summary():
    - Add import: `from bitbat.config.loader import resolve_metrics_dir`
    - Replace `Path("metrics")` with `resolve_metrics_dir()`

    After all edits: `poetry run ruff check src/bitbat/model/ src/bitbat/autonomous/ src/bitbat/backtest/ && poetry run ruff format src/bitbat/model/ src/bitbat/autonomous/ src/bitbat/backtest/`

    Run partial test: `poetry run pytest tests/model/test_train.py tests/backtest/test_metrics.py -x`
    Both must pass (chdir-based tests still work because defaults are relative).
  </action>
  <verify>
    <automated>poetry run pytest tests/model/test_train.py tests/backtest/test_metrics.py -x && poetry run ruff check src/bitbat/model/ src/bitbat/autonomous/ src/bitbat/backtest/</automated>
  </verify>
  <done>9 occurrences replaced in model/, autonomous/, backtest/; ruff clean; existing tests green</done>
</task>

<task type="auto">
  <name>Task 2: Replace Path("models") / Path("metrics") in cli/commands/ and api/routes/; verify structural tests GREEN</name>
  <files>
    src/bitbat/cli/commands/monitor.py,
    src/bitbat/cli/commands/system.py,
    src/bitbat/cli/commands/model.py,
    src/bitbat/api/routes/health.py,
    src/bitbat/api/routes/analytics.py
  </files>
  <action>
    **src/bitbat/cli/commands/monitor.py** — 1 occurrence (line ~74):
    - Add import: `from bitbat.config.loader import resolve_metrics_dir`
    - Replace inline `Path("metrics")` with `resolve_metrics_dir()`

    **src/bitbat/cli/commands/system.py** — 1 occurrence (line ~34):
    - Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace inline `Path("models")` with `resolve_models_dir()`

    **src/bitbat/cli/commands/model.py** — 2 occurrences (lines ~294, ~626):
    - Add import: `from bitbat.config.loader import resolve_metrics_dir`
    - Replace both `Path("metrics")` with `resolve_metrics_dir()`

    **src/bitbat/api/routes/health.py** — 1 occurrence in _check_model() (line ~95):
    - Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace `Path("models") / f"..." / "xgb.json"` with `resolve_models_dir() / f"..." / "xgb.json"`
    - Safe: get_runtime_config() lazy-loads, same pattern as bitbat.api.defaults._default_freq()

    **src/bitbat/api/routes/analytics.py** — 2 occurrences in feature_importance() (line ~73) and system_status() (line ~98):
    - Add import: `from bitbat.config.loader import resolve_models_dir`
    - Replace both `Path("models") / f"..." / "xgb.json"` with `resolve_models_dir() / f"..." / "xgb.json"`

    After all edits:
    `poetry run ruff check src/bitbat/cli/commands/ src/bitbat/api/ && poetry run ruff format src/bitbat/cli/commands/ src/bitbat/api/`

    Run structural verification — both tests MUST now be GREEN:
    `poetry run pytest tests/config/test_path_resolution.py -v`
    Expected: 6/6 passed.

    Run full suite:
    `poetry run pytest`
    Expected: all green.
  </action>
  <verify>
    <automated>poetry run pytest tests/config/test_path_resolution.py -v && poetry run pytest</automated>
  </verify>
  <done>All 16 hardcoded Path("models") / Path("metrics") literals removed from src/; 6/6 structural + unit tests GREEN; full test suite passes; ruff clean across all modified files</done>
</task>

</tasks>

<verification>
# Structural gate — confirms zero literals remain
poetry run pytest tests/config/test_path_resolution.py -v

# Behavioral regression gate
poetry run pytest tests/model/test_train.py tests/backtest/test_metrics.py tests/test_cli.py -x

# Full suite
poetry run pytest

# Lint gate
poetry run ruff check src/bitbat/model/ src/bitbat/autonomous/ src/bitbat/backtest/ src/bitbat/cli/commands/ src/bitbat/api/routes/

# Manual config redirect smoke check (optional, verifies success criterion 3):
# Edit default.yaml temporarily: models_dir: "/tmp/test_models"
# poetry run python -c "from bitbat.config.loader import resolve_models_dir; print(resolve_models_dir())"
# Expected: /tmp/test_models
# Revert default.yaml
</verification>

<success_criteria>
- Zero Path("models") or Path("metrics") literals in src/ (grep returns empty)
- tests/config/test_path_resolution.py: 6/6 GREEN
- tests/model/test_train.py: GREEN (chdir-based tests still work)
- tests/backtest/test_metrics.py: GREEN
- tests/test_cli.py: GREEN
- Full suite: all green
- All modified files pass ruff check with no suppressions
</success_criteria>

<output>
After completion, create `.planning/phases/33-path-centralization/33-02-SUMMARY.md`
</output>
