---
phase: 17-runtime-pair-alignment-and-startup-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/config/loader.py
  - src/bitbat/cli.py
  - src/bitbat/autonomous/agent.py
  - tests/test_cli.py
  - tests/autonomous/test_agent_integration.py
autonomous: true
requirements:
  - ALGN-01
  - ALGN-02
must_haves:
  truths:
    - Monitor startup reports resolved config source and resolved freq/horizon before the first cycle.
    - Startup fails fast with an actionable error when models/<freq>_<horizon>/xgb.json is missing.
    - Runtime/model mismatch cannot enter a silent monitor loop.
  artifacts:
    - path: src/bitbat/config/loader.py
      provides: Runtime config source metadata (explicit --config, BITBAT_CONFIG, default path)
    - path: src/bitbat/cli.py
      provides: Monitor startup output showing config source/path and resolved runtime pair
    - path: src/bitbat/autonomous/agent.py
      provides: Startup guardrail that blocks monitor initialization on missing model artifact
    - path: tests/test_cli.py
      provides: CLI regression coverage for startup reporting and missing-model blocking
  key_links:
    - from: src/bitbat/cli.py
      to: src/bitbat/config/loader.py
      via: Monitor commands derive and print source/path + freq/horizon from one config-source helper
      pattern: config_source|config_path|freq|horizon|monitor
    - from: src/bitbat/autonomous/agent.py
      to: tests/autonomous/test_agent_integration.py
      via: Agent preflight guard is regression-locked for missing model artifact startup failures
      pattern: preflight|model artifact|xgb.json|FileNotFoundError
---

<objective>
Enforce runtime pair alignment before any monitor cycle runs.

Purpose: Fulfill ALGN-01 and ALGN-02 by making startup explicit about resolved config source/pair and immediately blocking missing model artifacts.
Output: Config-source reporting plus startup model preflight guardrails in monitor commands and agent initialization.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/bitbat/config/loader.py
@src/bitbat/cli.py
@src/bitbat/autonomous/agent.py
@src/bitbat/autonomous/predictor.py
@tests/test_cli.py
@tests/autonomous/test_agent_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime config-source introspection and startup reporting for monitor commands</name>
  <files>src/bitbat/config/loader.py, src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Introduce a loader helper that reports resolved config source/path (explicit CLI path, BITBAT_CONFIG env, or default config) and wire monitor startup paths (`monitor run-once` and `monitor start`) to print resolved source and resolved freq/horizon before creating the monitoring agent.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor and (run_once or start) and (config or source or horizon or freq)"</automated>
  </verify>
  <done>Operators can see exactly which config source/path resolved the runtime pair before any monitor cycle is attempted.</done>
</task>

<task type="auto">
  <name>Task 2: Add startup model-artifact preflight guardrail in monitoring agent initialization</name>
  <files>src/bitbat/autonomous/agent.py, src/bitbat/cli.py, tests/autonomous/test_agent_integration.py, tests/test_cli.py</files>
  <action>Add a monitor startup preflight that validates `models/{freq}_{horizon}/xgb.json` exists before running cycles. Raise an actionable error message with remediation (set intended config via `--config`/`BITBAT_CONFIG` or train/copy the expected artifact), and ensure monitor commands surface this as a startup-blocking failure.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q -k "monitor and (preflight or artifact or xgb or startup)"</automated>
  </verify>
  <done>Missing runtime model artifacts cause deterministic startup failure instead of entering monitoring loops with no predictions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock ALGN-01/02 behavior with focused regression tests</name>
  <files>tests/test_cli.py, tests/autonomous/test_agent_integration.py</files>
  <action>Add/extend tests that assert startup output includes config source + runtime pair and that missing model artifact paths produce blocking, remediation-rich errors for monitor run-once/start.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q -k "ALGN or monitor and (startup or config or artifact)"</automated>
  </verify>
  <done>Automated coverage fails if startup reporting regresses or missing-model guardrails are bypassed.</done>
</task>

</tasks>

<verification>
- Startup output includes resolved config source/path and resolved freq/horizon.
- Missing model artifact blocks monitor startup with actionable remediation.
- No monitor cycle begins when runtime pair lacks model artifact support.
</verification>

<success_criteria>
- ALGN-01 is satisfied by explicit startup reporting of config source and runtime pair.
- ALGN-02 is satisfied by fail-fast startup behavior on missing model artifacts.
- Regression tests enforce alignment and guardrail behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/17-runtime-pair-alignment-and-startup-guardrails/17-01-SUMMARY.md`
</output>
