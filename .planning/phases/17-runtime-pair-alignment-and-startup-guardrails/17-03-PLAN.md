---
phase: 17-runtime-pair-alignment-and-startup-guardrails
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - tests/autonomous/test_schema_compat.py
  - tests/autonomous/test_db.py
  - tests/test_cli.py
autonomous: true
requirements:
  - SCHE-04
must_haves:
  truths:
    - Runtime schema compatibility contract explicitly covers monitor-required performance_snapshots columns.
    - Legacy databases missing monitor-required snapshot columns are detected and upgrade-remediated before monitor status/snapshots execute.
    - Operator-facing monitor commands surface actionable schema remediation instead of raw SQL runtime failures.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Runtime schema contract entries for performance_snapshots compatibility auditing/upgrades
    - path: tests/autonomous/test_schema_compat.py
      provides: Coverage for performance_snapshots missing-column audit/upgrade behavior
    - path: tests/test_cli.py
      provides: CLI regression checks for monitor status/snapshots schema-remediation messaging
  key_links:
    - from: src/bitbat/autonomous/schema_compat.py
      to: tests/autonomous/test_schema_compat.py
      via: Contract-required snapshot columns and upgrade semantics are enforced by compatibility tests
      pattern: performance_snapshots|missing_columns|upgrade|audit
    - from: src/bitbat/autonomous/schema_compat.py
      to: tests/test_cli.py
      via: Monitor status/snapshots schema guardrails are validated through user-facing command behavior
      pattern: monitor status|monitor snapshots|schema incompatible|upgrade
---

<objective>
Extend runtime schema guardrails to protect monitor status and snapshot flows.

Purpose: Fulfill SCHE-04 by adding `performance_snapshots` runtime column requirements to schema compatibility checks and ensuring monitor commands fail with actionable remediation when legacy schema drifts.
Output: Expanded schema contract plus tests that lock compatibility and operator-facing error behavior.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/bitbat/autonomous/schema_compat.py
@src/bitbat/autonomous/models.py
@src/bitbat/cli.py
@tests/autonomous/test_schema_compat.py
@tests/autonomous/test_db.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add performance_snapshots runtime contract coverage to schema compatibility module</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Define runtime-required `performance_snapshots` columns used by monitor status/snapshot reads and snapshot writes, classify additive upgrade-safe fields for legacy DBs, and include the table in audit/upgrade contract evaluation.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py -q -k "performance_snapshots or required_contract"</automated>
  </verify>
  <done>Schema audit/upgrade routines treat missing `performance_snapshots` runtime columns as compatibility issues with deterministic remediation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add schema compatibility regression tests for legacy performance_snapshots drift</name>
  <files>tests/autonomous/test_schema_compat.py, tests/autonomous/test_db.py</files>
  <action>Add legacy-schema fixtures and tests that prove missing monitor-required `performance_snapshots` columns are detected, auto-upgrade behavior remains idempotent, and classified monitor DB errors contain upgrade remediation guidance.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py tests/autonomous/test_db.py -q -k "performance_snapshots or schema_remediation"</automated>
  </verify>
  <done>Automated schema tests fail if monitor-required snapshot columns are omitted from compatibility checks or remediation text regresses.</done>
</task>

<task type="auto">
  <name>Task 3: Lock monitor status/snapshots schema guardrails at CLI level</name>
  <files>tests/test_cli.py</files>
  <action>Add monitor status/snapshots tests that emulate schema incompatibility and assert user-facing errors contain actionable audit/upgrade commands instead of raw runtime SQL failures.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor and (status or snapshots) and schema"</automated>
  </verify>
  <done>CLI monitor status/snapshots are protected by regression tests against schema-drift runtime failures.</done>
</task>

</tasks>

<verification>
- Schema contract includes monitor-required performance_snapshots columns.
- Legacy missing-column paths are detected and remediated through compatibility tooling.
- Monitor status/snapshots surface actionable schema guidance under incompatibility.
</verification>

<success_criteria>
- SCHE-04 is satisfied by explicit compatibility contract coverage for performance_snapshots runtime columns.
- Compatibility audit/upgrade remains deterministic and idempotent for legacy schemas.
- CLI tests prevent regressions to runtime SQL failures for monitor status/snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/17-runtime-pair-alignment-and-startup-guardrails/17-03-SUMMARY.md`
</output>
