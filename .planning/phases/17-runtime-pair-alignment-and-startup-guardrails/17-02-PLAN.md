---
phase: 17-runtime-pair-alignment-and-startup-guardrails
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - scripts/run_monitoring_agent.py
  - tests/test_run_monitoring_agent.py
autonomous: true
requirements:
  - ALGN-03
must_haves:
  truths:
    - Heartbeat payload includes config source/path plus freq/horizon for every monitor state transition.
    - Operators can inspect one heartbeat file and confirm the running process uses the intended runtime pair and config origin.
    - Error heartbeats preserve config metadata so failures remain diagnosable without traceback access.
  artifacts:
    - path: scripts/run_monitoring_agent.py
      provides: Heartbeat writer includes config source/path metadata with runtime pair and status
    - path: tests/test_run_monitoring_agent.py
      provides: Regression coverage for startup/ok/error heartbeat metadata fields
  key_links:
    - from: scripts/run_monitoring_agent.py
      to: tests/test_run_monitoring_agent.py
      via: Heartbeat payload schema is test-locked across status transitions and error paths
      pattern: heartbeat|config_source|config_path|freq|horizon|status
---

<objective>
Propagate runtime alignment metadata into autonomous heartbeat telemetry.

Purpose: Fulfill ALGN-03 by guaranteeing heartbeat payloads include config source metadata in addition to freq/horizon.
Output: Heartbeat writer updates and regression tests proving metadata persistence across lifecycle states.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@scripts/run_monitoring_agent.py
@src/bitbat/config/loader.py
@tests/gui/test_widgets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add config-source metadata to monitoring heartbeat payload schema</name>
  <files>scripts/run_monitoring_agent.py</files>
  <action>Extend heartbeat payload creation so every write includes `config_source` and `config_path` alongside existing status/freq/horizon/interval fields. Use the same config source resolution contract introduced in Plan 01 to avoid divergence between CLI and daemon startup metadata.</action>
  <verify>
    <automated>poetry run pytest tests/test_run_monitoring_agent.py -q -k "heartbeat and metadata"</automated>
  </verify>
  <done>Heartbeat JSON consistently includes config source/path metadata plus runtime pair fields.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve metadata across starting, ok, error, and stopped heartbeat writes</name>
  <files>scripts/run_monitoring_agent.py</files>
  <action>Thread resolved config metadata through all heartbeat write sites (startup, steady-state success, DB/runtime error, and shutdown) so operators can diagnose runtime provenance even after failures.</action>
  <verify>
    <automated>poetry run pytest tests/test_run_monitoring_agent.py -q -k "starting or ok or error or stopped"</automated>
  </verify>
  <done>All lifecycle heartbeat payloads keep config metadata and runtime pair fields intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add ALGN-03 regression tests for heartbeat payload semantics</name>
  <files>tests/test_run_monitoring_agent.py</files>
  <action>Create focused tests around the heartbeat helper(s) to assert config source/path, freq/horizon, and status fields are present and stable for normal and error updates.</action>
  <verify>
    <automated>poetry run pytest tests/test_run_monitoring_agent.py -q</automated>
  </verify>
  <done>Automated tests fail if heartbeat metadata drops config source/path or runtime pair fields.</done>
</task>

</tasks>

<verification>
- Heartbeat payloads include `config_source`, `config_path`, `freq`, and `horizon`.
- Metadata is preserved in error and shutdown states.
- Regression tests enforce heartbeat metadata contract.
</verification>

<success_criteria>
- ALGN-03 heartbeat metadata contract is implemented and test-locked.
- Operator diagnostics from heartbeat files include runtime pair and config origin.
- Heartbeat metadata remains stable across lifecycle transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/17-runtime-pair-alignment-and-startup-guardrails/17-02-SUMMARY.md`
</output>
