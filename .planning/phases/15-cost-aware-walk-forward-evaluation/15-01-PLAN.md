---
phase: 15-cost-aware-walk-forward-evaluation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/dataset/splits.py
  - src/bitbat/model/walk_forward.py
  - src/bitbat/cli.py
  - src/bitbat/config/default.yaml
  - tests/dataset/test_splits.py
  - tests/model/test_walk_forward.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVAL-01
must_haves:
  truths:
    - Walk-forward folds remain strictly time-ordered and support purge/embargo controls for overlapping label horizons.
    - Purge/embargo behavior is deterministic for the same window schedule and horizon inputs.
    - Operators can configure purge/embargo controls from CLI/config without manual fold surgery.
  artifacts:
    - path: src/bitbat/dataset/splits.py
      provides: Purge/embargo-aware walk-forward split builders with deterministic fold generation
    - path: src/bitbat/model/walk_forward.py
      provides: Fold execution wired to new split controls and metadata for leakage checks
    - path: src/bitbat/cli.py
      provides: CLI controls that compile purge/embargo settings into repeatable fold schedules
  key_links:
    - from: src/bitbat/cli.py
      to: src/bitbat/dataset/splits.py
      via: model cv options feed purge/embargo parameters into split generation
      pattern: purge|embargo|label_horizon|walk_forward
    - from: src/bitbat/model/walk_forward.py
      to: tests/model/test_walk_forward.py
      via: Fold ordering and leakage protections are regression-locked
      pattern: fold|purge|embargo|leakage
---

<objective>
Enforce leakage-resistant walk-forward split semantics with explicit purge/embargo controls.

Purpose: Fulfill EVAL-01 by making overlap-aware split controls first-class in CV/evaluation flows.
Output: Purge/embargo-capable split builders plus CLI/config wiring for deterministic execution.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-03-SUMMARY.md
@src/bitbat/dataset/splits.py
@src/bitbat/model/walk_forward.py
@src/bitbat/cli.py
@src/bitbat/config/default.yaml
@tests/dataset/test_splits.py
@tests/model/test_walk_forward.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add purge-aware split generation utilities for overlapping label horizons</name>
  <files>src/bitbat/dataset/splits.py, tests/dataset/test_splits.py</files>
  <action>Extend split utilities so walk-forward schedules can apply both embargo and purge windows tied to label-horizon overlap. Keep generation deterministic from timestamps + duration controls and preserve existing behavior when purge controls are unset.</action>
  <verify>
    <automated>poetry run pytest tests/dataset/test_splits.py -q -k "walk_forward and (embargo or purge or leakage)"</automated>
  </verify>
  <done>Split utilities enforce time ordering plus explicit purge/embargo behavior with regression coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate purge/embargo semantics into walk-forward evaluation execution</name>
  <files>src/bitbat/model/walk_forward.py, tests/model/test_walk_forward.py</files>
  <action>Update walk-forward evaluation internals to consume the new purge/embargo-aware fold schedule and retain fold/window diagnostics. Ensure overlapping horizon bars are excluded from training windows whenever configured.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_walk_forward.py -q -k "walk_forward or fold or embargo or purge"</automated>
  </verify>
  <done>Walk-forward execution runs on overlap-safe folds with deterministic diagnostics metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Expose purge/embargo controls through model CV CLI and defaults</name>
  <files>src/bitbat/cli.py, src/bitbat/config/default.yaml, tests/test_cli.py</files>
  <action>Add configurable purge/embargo options to model CV command and default config, then route them into split-generation calls. Preserve backward compatibility by keeping prior defaults when no new options are provided.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and cv and (purge or embargo or walk_forward)"</automated>
  </verify>
  <done>Operators can run overlap-aware walk-forward evaluation without manual timestamp edits.</done>
</task>

</tasks>

<verification>
- Walk-forward split generation enforces strict time ordering and overlap-safe purge/embargo controls.
- Model evaluation consumes the same purge/embargo semantics used in split creation.
- CLI/config controls produce deterministic fold schedules from operator inputs.
</verification>

<success_criteria>
- EVAL-01 is satisfied with purge/embargo-capable walk-forward evaluation.
- No overlap leakage remains between train/test windows when label horizons overlap.
- Purge/embargo behavior is regression-locked and reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cost-aware-walk-forward-evaluation/15-01-SUMMARY.md`
</output>
