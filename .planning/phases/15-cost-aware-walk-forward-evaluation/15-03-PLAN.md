---
phase: 15-cost-aware-walk-forward-evaluation
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/bitbat/model/evaluate.py
  - src/bitbat/model/walk_forward.py
  - src/bitbat/cli.py
  - src/bitbat/autonomous/retrainer.py
  - tests/model/test_evaluate.py
  - tests/model/test_walk_forward.py
  - tests/autonomous/test_retrainer.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVAL-03
must_haves:
  truths:
    - Candidate evaluation reports combine regression, directional, and risk-aware metrics in one persisted artifact.
    - Champion selection uses explicit deterministic rules and records why the winner was chosen.
    - Champion decision outputs are reproducible from CLI/autonomous evaluation flows.
  artifacts:
    - path: src/bitbat/model/evaluate.py
      provides: Candidate-report builders and explicit champion-selection rule helpers
    - path: src/bitbat/model/walk_forward.py
      provides: Fold-level aggregate metrics needed for candidate-vs-incumbent comparison
    - path: src/bitbat/cli.py
      provides: Operator-facing command/report output with persisted champion decision metadata
  key_links:
    - from: src/bitbat/model/walk_forward.py
      to: src/bitbat/model/evaluate.py
      via: Walk-forward aggregate metrics feed candidate report and champion rule evaluation
      pattern: candidate|champion|metrics|walk_forward
    - from: src/bitbat/cli.py
      to: src/bitbat/model/evaluate.py
      via: CLI evaluation path persists candidate report and champion decision artifacts
      pattern: report|champion|metrics|artifact
---

<objective>
Create unified candidate model reports with explicit champion-selection rules.

Purpose: Fulfill EVAL-03 so model choice decisions are traceable, multi-metric, and automated.
Output: Persisted candidate reports and champion-rule decisions across evaluation/retraining flows.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cost-aware-walk-forward-evaluation/15-02-PLAN.md
@src/bitbat/model/evaluate.py
@src/bitbat/model/walk_forward.py
@src/bitbat/cli.py
@src/bitbat/autonomous/retrainer.py
@tests/model/test_evaluate.py
@tests/model/test_walk_forward.py
@tests/autonomous/test_retrainer.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement unified candidate report builders across regression, directional, and risk metrics</name>
  <files>src/bitbat/model/evaluate.py, tests/model/test_evaluate.py</files>
  <action>Add reusable report builders that package regression accuracy, directional behavior, and risk-aware outputs (drawdown, net return, sharpe/cost-aware metrics) into one deterministic artifact schema for candidate evaluation.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_evaluate.py -q -k "candidate or champion or report or metric"</automated>
  </verify>
  <done>Candidate report schema is deterministic, persisted, and test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit champion-selection rule and wire to walk-forward aggregates</name>
  <files>src/bitbat/model/walk_forward.py, src/bitbat/model/evaluate.py, src/bitbat/autonomous/retrainer.py, tests/model/test_walk_forward.py, tests/autonomous/test_retrainer.py</files>
  <action>Define a deterministic champion rule comparing candidate vs incumbent using net/risk/directional constraints and persist decision rationale. Ensure autonomous retrainer metadata can consume the same rule outputs for consistent downstream promotion logic.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_walk_forward.py tests/autonomous/test_retrainer.py -q -k "champion or candidate or report or retrain"</automated>
  </verify>
  <done>Champion decision is explicit, reproducible, and linked to walk-forward/candidate artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Surface candidate report and champion decision outputs through CLI workflows</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Expose candidate-report/champion outputs in model CLI flows and persist artifacts for auditability. Preserve existing command paths while adding explicit champion decision fields and operator-readable summaries.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and (report or champion or candidate or evaluation)"</automated>
  </verify>
  <done>Operators can produce and inspect deterministic candidate reports and champion decisions from CLI runs.</done>
</task>

</tasks>

<verification>
- Candidate report artifacts include regression, directional, and risk-aware metrics together.
- Champion selection rule is deterministic and records decision rationale.
- CLI/autonomous outputs persist champion decisions for downstream promotion guardrails.
</verification>

<success_criteria>
- EVAL-03 is satisfied with explicit, persisted champion-selection reports.
- Model comparisons are multi-metric and reproducible across repeated runs.
- Champion decision logic is regression-tested and ready for Phase 16 promotion gates.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cost-aware-walk-forward-evaluation/15-03-SUMMARY.md`
</output>
