---
phase: 15-cost-aware-walk-forward-evaluation
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/backtest/engine.py
  - src/bitbat/backtest/metrics.py
  - src/bitbat/model/walk_forward.py
  - src/bitbat/cli.py
  - src/bitbat/config/default.yaml
  - tests/backtest/test_engine.py
  - tests/backtest/test_metrics.py
  - tests/model/test_walk_forward.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVAL-02
must_haves:
  truths:
    - Backtest and walk-forward outputs report net performance after transaction fees and slippage, not gross-only metrics.
    - Cost attribution is explicit (fees vs slippage) and linked to fold/window outputs.
    - CLI-level evaluation surfaces cost-aware metrics with reproducible defaults.
  artifacts:
    - path: src/bitbat/backtest/engine.py
      provides: Cost model including explicit slippage + fee accounting per trade transition
    - path: src/bitbat/backtest/metrics.py
      provides: Net/gross/cost-attribution metrics and persisted cost-aware backtest summaries
    - path: src/bitbat/model/walk_forward.py
      provides: Fold-level cost-aware metrics propagated into walk-forward summaries
  key_links:
    - from: src/bitbat/backtest/engine.py
      to: src/bitbat/backtest/metrics.py
      via: Trade-level cost columns feed persisted net/gross metric summaries
      pattern: slippage|costs|gross_pnl|pnl|net
    - from: src/bitbat/model/walk_forward.py
      to: src/bitbat/backtest/engine.py
      via: Fold evaluation invokes backtest engine with cost controls and consumes net outputs
      pattern: fold|cost_bps|slippage|net_sharpe
---

<objective>
Make evaluation outputs explicitly cost-aware with fee/slippage-adjusted net metrics.

Purpose: Fulfill EVAL-02 by replacing optimistic gross-only reporting with realistic net evaluation.
Output: Backtest/walk-forward cost modeling plus persisted net/gross metric reporting in runtime outputs.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cost-aware-walk-forward-evaluation/15-01-PLAN.md
@src/bitbat/backtest/engine.py
@src/bitbat/backtest/metrics.py
@src/bitbat/model/walk_forward.py
@src/bitbat/cli.py
@src/bitbat/config/default.yaml
@tests/backtest/test_engine.py
@tests/backtest/test_metrics.py
@tests/model/test_walk_forward.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend backtest engine with explicit fee and slippage cost modeling</name>
  <files>src/bitbat/backtest/engine.py, tests/backtest/test_engine.py</files>
  <action>Refine trade-transition cost handling so backtest engine separately tracks transaction fees and slippage while preserving gross and net PnL fields. Keep deterministic behavior under fixed inputs and maintain current defaults for existing commands.</action>
  <verify>
    <automated>poetry run pytest tests/backtest/test_engine.py -q -k "cost or slippage or net or gross"</automated>
  </verify>
  <done>Backtest engine emits deterministic fee/slippage and net-vs-gross fields required for realistic evaluation.</done>
</task>

<task type="auto">
  <name>Task 2: Propagate cost-aware metrics into summary and fold-level outputs</name>
  <files>src/bitbat/backtest/metrics.py, src/bitbat/model/walk_forward.py, tests/backtest/test_metrics.py, tests/model/test_walk_forward.py</files>
  <action>Update metric aggregation to include net, gross, and explicit cost attribution across whole-run and fold-level summaries. Ensure serialized metrics preserve compatibility while exposing additional cost-aware fields for downstream model selection.</action>
  <verify>
    <automated>poetry run pytest tests/backtest/test_metrics.py tests/model/test_walk_forward.py -q -k "net or gross or cost or sharpe"</automated>
  </verify>
  <done>Cost-aware backtest and walk-forward summaries are persisted and regression-locked.</done>
</task>

<task type="auto">
  <name>Task 3: Surface cost-aware controls and reporting through CLI/config defaults</name>
  <files>src/bitbat/cli.py, src/bitbat/config/default.yaml, tests/test_cli.py</files>
  <action>Expose slippage/cost evaluation controls in CLI/config and ensure model/backtest command outputs and artifacts include net-aware summaries. Preserve existing command compatibility when operators do not supply overrides.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "backtest and (cost or slippage or net or gross)"</automated>
  </verify>
  <done>Operators can run cost-aware evaluation with explicit fee/slippage assumptions and reproducible reporting.</done>
</task>

</tasks>

<verification>
- Fee and slippage are modeled explicitly and reflected in net metrics.
- Backtest and walk-forward reports contain cost-adjusted outputs alongside gross references.
- CLI/config controls enable reproducible cost-aware evaluation runs.
</verification>

<success_criteria>
- EVAL-02 is satisfied with net fee/slippage-adjusted evaluation outputs.
- Cost attribution is explicit and machine-readable in persisted metrics.
- Existing evaluation commands remain backward compatible with default settings.
</success_criteria>

<output>
After completion, create `.planning/phases/15-cost-aware-walk-forward-evaluation/15-02-SUMMARY.md`
</output>
