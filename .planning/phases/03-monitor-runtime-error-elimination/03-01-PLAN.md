---
phase: 03-monitor-runtime-error-elimination
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/db.py
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/autonomous/validator.py
  - tests/autonomous/test_validator.py
  - tests/autonomous/test_agent_integration.py
autonomous: true
requirements:
  - MON-01
must_haves:
  truths:
    - Monitor prediction and validation DB operations no longer fail with uncategorized `OperationalError` on missing runtime columns.
    - Runtime DB failures are classified with deterministic monitor-step context.
    - Missing-column failures surface remediation guidance instead of opaque trace-only logs.
  artifacts:
    - path: src/bitbat/autonomous/db.py
      provides: Shared monitor DB fault classification helpers for runtime operations
    - path: src/bitbat/autonomous/predictor.py
      provides: Predictor DB interaction boundaries with explicit DB fault handling
    - path: src/bitbat/autonomous/validator.py
      provides: Validator DB write/read boundaries with explicit DB fault handling
    - path: tests/autonomous/test_validator.py
      provides: Runtime validation regression checks for DB failure paths
    - path: tests/autonomous/test_agent_integration.py
      provides: Monitor integration regressions for legacy-schema runtime execution
  key_links:
    - from: src/bitbat/autonomous/predictor.py
      to: src/bitbat/autonomous/db.py
      via: Prediction duplicate-check and store paths using shared DB fault classification
      pattern: predict_latest|get_unrealized_predictions|store_prediction
    - from: src/bitbat/autonomous/validator.py
      to: src/bitbat/autonomous/db.py
      via: Realization write path routed through shared DB fault handling
      pattern: validate_batch|realize_prediction
    - from: src/bitbat/autonomous/schema_compat.py
      to: src/bitbat/autonomous/predictor.py
      via: Missing-column diagnostics reused in runtime DB failure messages
      pattern: format_missing_columns|SchemaCompatibilityError
---

<objective>
Patch monitor/predictor DB interactions to eliminate uncategorized runtime schema faults.

Purpose: Remove recurring runtime DB `OperationalError` paths caused by schema drift and make failures deterministic.
Output: Shared DB fault classification plus predictor/validator runtime boundaries hardened for MON-01.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monitor-runtime-error-elimination/03-RESEARCH.md
@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/schema_compat.py
@src/bitbat/autonomous/predictor.py
@src/bitbat/autonomous/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared monitor DB fault classification helpers</name>
  <files>src/bitbat/autonomous/db.py, src/bitbat/autonomous/schema_compat.py</files>
  <action>Create a reusable autonomous-layer fault classification path for runtime DB exceptions (especially SQLAlchemy `OperationalError`) that captures monitor step, table/column hints, and remediation text (audit/upgrade commands). Keep it narrow to monitor-critical DB operations and avoid duplicating ad hoc string parsing in predictor/validator.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py tests/autonomous/test_db.py -q -k "compat or schema or log"</automated>
  </verify>
  <done>Runtime DB fault classification utilities exist and return deterministic diagnostics for missing-column vs generic DB failures.</done>
</task>

<task type="auto">
  <name>Task 2: Harden predictor and validator DB boundaries with classified runtime failures</name>
  <files>src/bitbat/autonomous/predictor.py, src/bitbat/autonomous/validator.py</files>
  <action>Refactor monitor-critical DB interaction points (prediction duplicate checks, prediction writes, validation realization updates) to use the shared DB fault classification path. Replace broad fallback behavior for critical DB operations with explicit classified failures that preserve root cause and monitor step metadata.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_validator.py -q</automated>
  </verify>
  <done>Predictor/validator critical DB failures are surfaced as structured monitor-domain failures instead of uncategorized runtime errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime regression tests for monitor DB fault elimination</name>
  <files>tests/autonomous/test_validator.py, tests/autonomous/test_agent_integration.py</files>
  <action>Add regression tests that simulate runtime legacy-schema/missing-column failures in monitor validation/prediction paths and assert deterministic failure classification, including remediation hints, without silent pass-through behavior.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_validator.py tests/autonomous/test_agent_integration.py -q -k "schema or legacy or monitor"</automated>
  </verify>
  <done>Automated tests fail if monitor runtime DB faults regress to raw/uncategorized `OperationalError` behavior.</done>
</task>

</tasks>

<verification>
- Monitor runtime DB operations classify and surface schema-related failures deterministically.
- Predictor/validator critical DB paths no longer rely on uncategorized `OperationalError` handling.
- Regression tests cover runtime (not only startup) missing-column failure scenarios.
</verification>

<success_criteria>
- MON-01 runtime paths have explicit DB fault classification and remediation detail.
- Predictor and validator monitor-critical DB operations fail predictably when schema issues occur.
- Regression coverage guards against reintroducing runtime missing-column operational failures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitor-runtime-error-elimination/03-01-SUMMARY.md`
</output>
