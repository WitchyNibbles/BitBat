---
phase: 03-monitor-runtime-error-elimination
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/bitbat/autonomous/agent.py
  - src/bitbat/autonomous/db.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/autonomous/validator.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MON-01
  - MON-03
must_haves:
  truths:
    - Runtime monitor DB failures emit structured diagnostics that identify failing step and remediation path.
    - Operator-visible failure context is consistent across in-process monitor results, logs, and CLI/script output.
    - Diagnostic signals are regression-tested so failure visibility does not degrade in future changes.
  artifacts:
    - path: src/bitbat/autonomous/agent.py
      provides: Standardized monitor failure payload assembly at cycle boundary
    - path: src/bitbat/autonomous/db.py
      provides: Structured `system_logs` persistence helpers for monitor diagnostics
    - path: src/bitbat/autonomous/predictor.py
      provides: Predictor diagnostic emission for classified runtime DB failures
    - path: src/bitbat/autonomous/validator.py
      provides: Validator diagnostic emission for classified runtime DB failures
    - path: tests/autonomous/test_agent_integration.py
      provides: Integration assertions for structured runtime diagnostics
    - path: tests/test_cli.py
      provides: CLI-level assertions for actionable monitor failure details
  key_links:
    - from: src/bitbat/autonomous/predictor.py
      to: src/bitbat/autonomous/agent.py
      via: Predictor failure metadata forwarded to monitor cycle result/log path
      pattern: step|remediation|error_class
    - from: src/bitbat/autonomous/validator.py
      to: src/bitbat/autonomous/agent.py
      via: Validation failure metadata forwarded to monitor cycle result/log path
      pattern: validate|database|error
    - from: src/bitbat/autonomous/agent.py
      to: src/bitbat/autonomous/db.py
      via: Structured failure events persisted through `SystemLog` helper
      pattern: db.log|service|details
---

<objective>
Add structured diagnostics for monitor DB faults across runtime surfaces.

Purpose: Ensure critical monitor failures are not just raised, but diagnostically useful and consistent for operators.
Output: Structured diagnostics in monitor result/log pathways with regression tests for failure visibility.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monitor-runtime-error-elimination/03-RESEARCH.md
@.planning/phases/03-monitor-runtime-error-elimination/03-01-PLAN.md
@.planning/phases/03-monitor-runtime-error-elimination/03-02-PLAN.md
@src/bitbat/autonomous/agent.py
@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/predictor.py
@src/bitbat/autonomous/validator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define normalized monitor diagnostic payload contract</name>
  <files>src/bitbat/autonomous/agent.py, src/bitbat/autonomous/db.py</files>
  <action>Create a single normalized diagnostic payload shape for critical monitor DB failures (step, failure category, exception class, actionable remediation, freq/horizon) and wire it into monitor result assembly plus optional `SystemLog` persistence for operator visibility.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q -k "monitoring or diagnostics or schema"</automated>
  </verify>
  <done>Monitor cycle outputs include consistent structured diagnostics for critical DB failure paths.</done>
</task>

<task type="auto">
  <name>Task 2: Emit structured diagnostics at predictor and validator failure boundaries</name>
  <files>src/bitbat/autonomous/predictor.py, src/bitbat/autonomous/validator.py</files>
  <action>Instrument predictor/validator critical DB failure paths to emit standardized diagnostic details consumed by monitor orchestration, avoiding inconsistent per-module logging formats and preserving root-cause context for runtime operations.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_validator.py tests/autonomous/test_agent_integration.py -q -k "validate or monitor"</automated>
  </verify>
  <done>Predictor and validator failures produce consistent step-level diagnostics consumed by agent-level surfacing.</done>
</task>

<task type="auto">
  <name>Task 3: Expand regression coverage for actionable diagnostic content</name>
  <files>tests/autonomous/test_agent_integration.py, tests/test_cli.py</files>
  <action>Add tests asserting diagnostic payload content includes failing step and remediation path in both monitor runtime result structures and CLI output; ensure failures are actionable and not generic across monitored surfaces.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q -k "monitor and diagnostics"</automated>
  </verify>
  <done>Regression tests guarantee that monitor failure visibility remains actionable and step-specific across runtime and CLI surfaces.</done>
</task>

</tasks>

<verification>
- Structured monitor diagnostics are consistent across runtime failure surfaces.
- Predictor/validator failure metadata is preserved through agent-level outputs.
- Automated regressions enforce actionable diagnostic payload quality.
</verification>

<success_criteria>
- MON-01 and MON-03 outcomes are observable in monitor diagnostic outputs and tests.
- Runtime DB faults report failing step plus remediation guidance across core monitor surfaces.
- Regression suite protects against reverting to opaque failure messaging.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitor-runtime-error-elimination/03-03-SUMMARY.md`
</output>
