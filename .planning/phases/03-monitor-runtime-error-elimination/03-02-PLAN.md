---
phase: 03-monitor-runtime-error-elimination
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/autonomous/agent.py
  - scripts/run_monitoring_agent.py
  - src/bitbat/cli.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MON-03
must_haves:
  truths:
    - Critical monitor DB failures are surfaced at run boundaries with actionable diagnostics (step + remediation).
    - Monitor commands/scripts no longer silently swallow critical DB errors behind generic success/continue behavior.
    - Operators can identify whether failure occurred during prediction, validation, or persistence from command/script output.
  artifacts:
    - path: src/bitbat/autonomous/agent.py
      provides: Critical-path exception boundaries that preserve DB failure identity
    - path: src/bitbat/cli.py
      provides: Monitor command error surfacing for classified runtime DB failures
    - path: scripts/run_monitoring_agent.py
      provides: Continuous loop boundary that emits actionable DB failure diagnostics
    - path: tests/autonomous/test_agent_integration.py
      provides: Integration tests for monitor critical error propagation
    - path: tests/test_cli.py
      provides: CLI regression tests for monitor critical DB failure messages
  key_links:
    - from: src/bitbat/autonomous/agent.py
      to: src/bitbat/autonomous/predictor.py
      via: Prediction-step failure propagation with classified monitor DB context
      pattern: run_once|predict_latest|except
    - from: src/bitbat/cli.py
      to: src/bitbat/autonomous/agent.py
      via: `monitor run-once` and `monitor start` surfacing monitor critical failures
      pattern: monitor_run_once|monitor_start|ClickException
    - from: scripts/run_monitoring_agent.py
      to: src/bitbat/autonomous/agent.py
      via: Top-level loop heartbeat/logging reflects failure step and remediation
      pattern: run_once|_write_heartbeat|error
---

<objective>
Refactor monitor exception boundaries so critical DB faults are surfaced, not swallowed.

Purpose: Ensure MON-03 by making monitor failure visibility operator-actionable at orchestration and command/script boundaries.
Output: Agent/CLI/script critical error boundaries that expose failing step and remediation guidance.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-monitor-runtime-error-elimination/03-RESEARCH.md
@.planning/phases/03-monitor-runtime-error-elimination/03-01-PLAN.md
@src/bitbat/autonomous/agent.py
@src/bitbat/cli.py
@scripts/run_monitoring_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce explicit critical failure boundaries in monitor run orchestration</name>
  <files>src/bitbat/autonomous/agent.py</files>
  <action>Refactor `run_once` exception handling to distinguish recoverable data-ingest issues from critical DB monitor failures. Critical DB failures must preserve step metadata and be surfaced in returned status and/or raised exception path so callers cannot treat failed critical paths as successful cycles.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q -k "monitoring or schema or preflight"</automated>
  </verify>
  <done>Agent critical DB failures are no longer reduced to generic log-only exceptions and can be programmatically detected by callers.</done>
</task>

<task type="auto">
  <name>Task 2: Surface critical monitor DB failures through CLI and monitoring agent script</name>
  <files>src/bitbat/cli.py, scripts/run_monitoring_agent.py</files>
  <action>Update monitor command and script boundaries to expose classified failure diagnostics (step, root exception class, remediation) to operators. Preserve existing startup schema compatibility messaging, and add runtime critical-failure messaging for failures after startup preflight passes.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor_run_once or schema_error_message or monitor_start"</automated>
  </verify>
  <done>CLI/script output provides actionable, step-specific diagnostics for runtime critical monitor DB failures.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for non-silent critical failure surfacing</name>
  <files>tests/autonomous/test_agent_integration.py, tests/test_cli.py</files>
  <action>Add tests that inject runtime DB failures within monitor cycle execution and assert that error surfaces include operation context and remediation text; ensure tests fail if monitor boundaries revert to swallow-and-continue behavior without actionable detail.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q -k "monitor and (error or schema or run_once)"</automated>
  </verify>
  <done>Automated coverage blocks regressions where critical monitor DB failures are hidden from operators.</done>
</task>

</tasks>

<verification>
- Monitor orchestration explicitly classifies and surfaces critical DB failures.
- CLI/script monitor entrypoints expose actionable runtime diagnostics.
- Regression tests prove silent swallowing of critical DB failures is prevented.
</verification>

<success_criteria>
- MON-03 enforced at agent and entrypoint boundaries for runtime DB failure visibility.
- Operators receive step-specific remediation hints for critical monitor DB failures.
- Automated tests fail if critical failures become opaque or silently swallowed.
</success_criteria>

<output>
After completion, create `.planning/phases/03-monitor-runtime-error-elimination/03-02-SUMMARY.md`
</output>
