---
phase: 11-runtime-stability-and-retirement-guards
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/gui/widgets.py
  - streamlit/app.py
  - tests/gui/test_widgets.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - STAB-01
must_haves:
  truths:
    - Home rendering never crashes when latest prediction rows are missing optional fields.
    - Confidence display uses safe fallback logic for partial prediction payloads.
    - Operators can still interpret latest direction/return without raw exception exposure.
  artifacts:
    - path: src/bitbat/gui/widgets.py
      provides: Schema-tolerant latest prediction payload for home dashboard consumption
    - path: streamlit/app.py
      provides: Home rendering guardrails for missing confidence and partial prediction values
    - path: tests/gui/test_widgets.py
      provides: Regression coverage for partial latest prediction payloads
  key_links:
    - from: src/bitbat/gui/widgets.py
      to: streamlit/app.py
      via: Home rendering consumes schema-tolerant prediction payload without key assumptions
      pattern: get_latest_prediction|confidence|predicted_return
---

<objective>
Harden home prediction rendering against partial database rows.

Purpose: Fulfill STAB-01 by removing the `confidence` keypath crash and replacing it with safe fallback rendering.
Output: Schema-tolerant prediction payload plus guarded home rendering and regression tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-runtime-stability-and-retirement-guards/11-CONTEXT.md
@streamlit/app.py
@src/bitbat/gui/widgets.py
@tests/gui/test_widgets.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make latest prediction payload include safe confidence fallback semantics</name>
  <files>src/bitbat/gui/widgets.py</files>
  <action>Update latest-prediction loading so partial schema/data rows are normalized into a stable payload shape with optional confidence semantics and no hard key assumptions in downstream consumers.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_widgets.py -q -k "latest_prediction or confidence or partial"</automated>
  </verify>
  <done>Latest prediction helper returns a stable dict shape for partial rows without throwing or omitting required core fields.</done>
</task>

<task type="auto">
  <name>Task 2: Guard home rendering against missing confidence and other optional fields</name>
  <files>streamlit/app.py</files>
  <action>Replace direct dictionary key indexing for optional prediction values with defensive accessors and fallback presentation copy so the home page remains readable when confidence is unavailable.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py -q -k "prediction_payload or primary_workflow"</automated>
  </verify>
  <done>Home dashboard renders without traceback when confidence is absent and still communicates direction/forecast context.</done>
</task>

<task type="auto">
  <name>Task 3: Add STAB-01 regression tests for missing-confidence paths</name>
  <files>tests/gui/test_widgets.py, tests/gui/test_complete_gui.py</files>
  <action>Add focused tests that emulate prediction rows missing confidence-related fields and assert helper outputs plus home-facing expectations remain stable.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_widgets.py tests/gui/test_complete_gui.py -q -k "confidence or partial"</automated>
  </verify>
  <done>Automated regressions fail if missing-confidence handling regresses into KeyError behavior.</done>
</task>

</tasks>

<verification>
- `streamlit/app.py` no longer fails on missing `confidence`.
- Latest prediction payload remains consumable under partial row conditions.
- Regression tests lock missing-confidence behavior.
</verification>

<success_criteria>
- STAB-01 crash signature is eliminated by defensive payload + render handling.
- Home view remains readable when optional prediction fields are absent.
- Automated GUI/widget tests cover the missing-confidence path.
</success_criteria>

<output>
After completion, create `.planning/phases/11-runtime-stability-and-retirement-guards/11-01-SUMMARY.md`
</output>
