---
phase: 30-fix-and-reset
plan: 03
type: execute
wave: 3
depends_on:
  - 30-01
  - 30-02
files_modified: []
autonomous: false
requirements:
  - FIXR-03

must_haves:
  truths:
    - "Operator can follow documented reset + retrain procedure step by step"
    - "After reset + retrain with fixed code, realized directional accuracy exceeds 33%"
    - "Accuracy gate test passes or is documented to require live pipeline data"
  artifacts:
    - path: "tests/diagnosis/test_pipeline_stage_trace.py"
      provides: "test_accuracy_exceeds_random_baseline passes or skips gracefully"
      contains: "accuracy > 0.33"
  key_links:
    - from: "tests/diagnosis/test_pipeline_stage_trace.py::test_accuracy_exceeds_random_baseline"
      to: "data/autonomous.db"
      via: "sqlite3 query on realized prediction_outcomes"
      pattern: "correct.*actual_return IS NOT NULL"
---

<objective>
Verify that the complete reset + retrain pipeline produces directional accuracy above 33% (random baseline). This plan is a human checkpoint: the operator runs the documented reset + retrain sequence, then Claude verifies the accuracy gate test passes.

Purpose: Satisfy FIXR-03 — confirm end-to-end accuracy recovery after the three code fixes.
Output: Human confirmation that accuracy exceeds random baseline on a fresh pipeline run.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-fix-and-reset/30-01-SUMMARY.md
@.planning/phases/30-fix-and-reset/30-02-SUMMARY.md
@ROOT_CAUSE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify accuracy gate test and confirm suite is green</name>
  <files></files>
  <action>
Run the accuracy gate test. It will skip if data/autonomous.db is absent, which is expected on a fresh environment. Confirm skip behavior is correct:

```
poetry run pytest tests/diagnosis/test_pipeline_stage_trace.py::test_accuracy_exceeds_random_baseline -v
```

Expected output: `SKIPPED (data/autonomous.db not present — run autonomous monitor first)`.

This is correct behavior — the test gates on live pipeline data. The automated accuracy gate CANNOT pass in CI without a live pipeline run. This is by design: it checks live prediction outcomes in `data/autonomous.db` and uses `pytest.skip` when the DB is absent.

Confirm the full test suite passes with the skip:
```
poetry run pytest -q
```

Confirm reset command works via CLI test:
```
poetry run pytest tests/test_cli.py -k "reset" -v
```
  </action>
  <verify>
    <automated>poetry run pytest tests/diagnosis/ tests/test_cli.py -k "reset or pipeline" -v --tb=short</automated>
  </verify>
  <done>Accuracy gate test correctly skips (if no live data) or passes (if live data exists with fixed model). Reset CLI tests pass. Full suite green with no unexpected failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator reset + retrain verification</name>
  <files></files>
  <action>
Pause for human to choose verification path and optionally run the full reset + retrain sequence.
  </action>
  <verify>
    <automated>MISSING — live pipeline run required; human confirms accuracy result</automated>
  </verify>
  <done>Operator confirms accuracy > 33% after live reset + retrain, or acknowledges deferred.</done>
  <what-built>
Three bug fixes in train.py, infer.py, and validator.py. CLI reset command. Inverted diagnostic tests. All unit tests pass.

The accuracy gate test (test_accuracy_exceeds_random_baseline) requires a full reset + retrain pipeline run with live Bitcoin data. Claude cannot run this autonomously — it requires internet access and real-time data ingestion.
  </what-built>
  <how-to-verify>
Option A — If you want to verify FIXR-03 now (takes ~1-2 hours of pipeline running time):

1. Reset the pipeline:
   ```
   poetry run bitbat system reset --yes
   ```

2. Re-ingest prices and features:
   ```
   poetry run bitbat ingest prices-once
   poetry run bitbat features build
   ```

3. Train the fixed model:
   ```
   poetry run bitbat model train
   ```

4. Run batch inference and start the monitor:
   ```
   poetry run bitbat batch run
   poetry run bitbat monitor run-once
   ```

5. Wait for the configured horizon to pass (e.g., 30m for 5m_30m pair), then run:
   ```
   poetry run bitbat monitor run-once
   poetry run pytest tests/diagnosis/test_pipeline_stage_trace.py::test_accuracy_exceeds_random_baseline -v
   ```

6. Expected: accuracy > 0.33 and test passes.

Option B — If you want to defer live verification:

The unit tests for all three fixes pass. The code is correct. Live accuracy can be verified after Phase 31 monitoring is in place.

Type "approved" to confirm Option A (live verification passed) or "deferred" to confirm Option B (unit tests sufficient for now, live verification deferred to Phase 31).
  </how-to-verify>
  <resume-signal>Type "approved" (live accuracy verified > 33%) or "deferred" (unit tests sufficient)</resume-signal>
</task>

</tasks>

<verification>
Final phase gate:

1. All unit tests pass:
   ```
   poetry run pytest tests/model/ tests/autonomous/ tests/diagnosis/ tests/test_cli.py -q
   ```

2. Lint passes:
   ```
   poetry run ruff check src/ tests/
   poetry run lint-imports
   ```

3. FIXR-01: Confirmed by unit tests in plan 01
4. FIXR-02: Confirmed by reset CLI tests in plan 02
5. FIXR-03: Confirmed by human checkpoint above (live or deferred)
</verification>

<success_criteria>
- FIXR-03 satisfied: Operator confirms accuracy > 33% after reset + retrain, OR acknowledges deferred to Phase 31 monitoring
- Full test suite green (unit + integration skips where live data absent)
- Phase 30 complete — Phase 31 (Accuracy Guardrail) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/30-fix-and-reset/30-03-SUMMARY.md`
</output>
