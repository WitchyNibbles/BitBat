---
phase: 01-schema-contract-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - scripts/init_autonomous_db.py
  - tests/autonomous/test_schema_compat.py
autonomous: true
requirements:
  - SCHE-01
must_haves:
  truths:
    - Existing autonomous DB schemas can be inspected and reported against required runtime columns.
    - Operators can run a compatibility audit and receive clear missing-column results.
    - Schema compatibility checks are test-backed for current and legacy DB states.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Required-column contract and schema introspection helpers
    - path: scripts/init_autonomous_db.py
      provides: CLI-accessible compatibility audit entrypoint
    - path: tests/autonomous/test_schema_compat.py
      provides: Regression coverage for compatibility audit behavior
  key_links:
    - from: scripts/init_autonomous_db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Compatibility audit invocation
      pattern: schema_compat\.\w+
    - from: tests/autonomous/test_schema_compat.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Automated test assertions
      pattern: test_.*schema
---

<objective>
Establish a canonical schema-compatibility contract and auditing surface for autonomous prediction tables.

Purpose: Create the baseline tooling needed to reason about legacy SQLite schema drift safely.
Output: Reusable compatibility module + audit command + regression tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@src/bitbat/autonomous/models.py
@scripts/init_autonomous_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema compatibility contract module</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Implement a focused compatibility module that defines required columns for `prediction_outcomes` (including `predicted_price`) and provides functions to inspect current DB schema via SQLAlchemy/SQLite introspection. Keep it read-only in this plan (no ALTER TABLE yet).</action>
  <verify>poetry run pytest tests/autonomous/test_models.py -q</verify>
  <done>Compatibility module exists with reusable helpers for required-column discovery and missing-column reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Expose compatibility audit through init script</name>
  <files>scripts/init_autonomous_db.py</files>
  <action>Add a non-destructive audit path to the autonomous DB init script that reports schema compatibility status and missing columns using the new compatibility module. Ensure output is operator-readable and suitable for CI/log capture.</action>
  <verify>poetry run python scripts/init_autonomous_db.py --database-url sqlite:///data/autonomous.db || true</verify>
  <done>Operators can run an explicit schema audit and see actionable missing-column output without modifying DB state.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for compatibility audit</name>
  <files>tests/autonomous/test_schema_compat.py</files>
  <action>Create tests covering at least: current schema detection, legacy schema missing `predicted_price`, and audit output behavior. Use temp sqlite DB fixtures; avoid relying on workstation-local DB files.</action>
  <verify>poetry run pytest tests/autonomous/test_schema_compat.py -q</verify>
  <done>Automated tests fail when required-column contract or audit behavior regresses.</done>
</task>

</tasks>

<verification>
- Compatibility audit reports missing runtime-required columns clearly.
- Required-column registry matches runtime expectations from `PredictionOutcome`.
- Tests provide deterministic pass/fail signals for legacy schema states.
</verification>

<success_criteria>
- `schema_compat.py` provides reusable contract + inspection helpers.
- Audit path is callable and non-destructive.
- Regression tests for compatibility audit are present and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md`
</output>
