---
phase: 01-schema-contract-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - scripts/init_autonomous_db.py
  - tests/autonomous/test_schema_compat.py
autonomous: true
requirements:
  - SCHE-01
must_haves:
  truths:
    - Operators can run a schema compatibility audit and get deterministic missing-column output.
    - Runtime-required prediction columns are defined in one canonical compatibility contract.
    - Compatibility audit behavior is regression-tested against both current and legacy DB states.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Required-column contract and DB schema inspection helpers
    - path: scripts/init_autonomous_db.py
      provides: Non-destructive compatibility audit command path
    - path: tests/autonomous/test_schema_compat.py
      provides: Test coverage for compatibility contract and audit output
  key_links:
    - from: scripts/init_autonomous_db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Audit invocation path
      pattern: schema_compat|compatibility
    - from: tests/autonomous/test_schema_compat.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Direct function-level assertions
      pattern: test_.*compat
---

<objective>
Create the compatibility baseline for autonomous DB schema auditing.

Purpose: Establish a single schema contract source before implementing upgrade and preflight behavior.
Output: Shared compatibility module, audit command integration, and baseline tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@src/bitbat/autonomous/models.py
@scripts/init_autonomous_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical schema compatibility contract</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Create a compatibility module that declares required columns for runtime-critical tables (including `prediction_outcomes.predicted_price`) and exposes schema inspection helpers built on SQLAlchemy connections/SQLite metadata introspection.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_models.py -q</automated>
  </verify>
  <done>Compatibility contract is centralized and reusable from monitor/init/CLI paths.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit compatibility audit command path</name>
  <files>scripts/init_autonomous_db.py</files>
  <action>Integrate a non-destructive audit mode in the DB init script that reports schema compatibility status and missing required columns in actionable output format.</action>
  <verify>
    <automated>poetry run python scripts/init_autonomous_db.py --database-url sqlite:///data/autonomous.db || true</automated>
  </verify>
  <done>Audit mode reports compatibility problems without mutating data or requiring table recreation.</done>
</task>

<task type="auto">
  <name>Task 3: Add baseline compatibility regression tests</name>
  <files>tests/autonomous/test_schema_compat.py</files>
  <action>Add tests for current schema detection and legacy missing-column detection using temporary SQLite databases; validate audit output shape and required-column contract behavior.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py -q</automated>
  </verify>
  <done>Automated tests fail when compatibility contract or audit behavior regresses.</done>
</task>

</tasks>

<verification>
- Compatibility audit exists and is non-destructive.
- Required runtime columns are codified in one reusable module.
- Regression tests cover legacy schema mismatch detection.
</verification>

<success_criteria>
- `schema_compat.py` provides required-column registry and inspection helpers.
- `init_autonomous_db.py` exposes compatibility audit output.
- Compatibility regression tests are present and passing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md`
</output>
