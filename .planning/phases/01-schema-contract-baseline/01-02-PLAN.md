---
phase: 01-schema-contract-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/db.py
  - scripts/init_autonomous_db.py
  - tests/autonomous/test_schema_compat.py
autonomous: true
requirements:
  - SCHE-01
must_haves:
  truths:
    - Legacy DBs can be upgraded additively to include required prediction runtime columns.
    - Running schema upgrade repeatedly does not break or duplicate schema changes.
    - Existing prediction rows are preserved after compatibility upgrades.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Idempotent additive migration/upgrade routine
    - path: src/bitbat/autonomous/db.py
      provides: Runtime entrypoint hooks for schema upgrade execution
    - path: tests/autonomous/test_schema_compat.py
      provides: Upgrade idempotency and data-preservation coverage
  key_links:
    - from: src/bitbat/autonomous/db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Upgrade invocation before runtime DB operations
      pattern: ensure_.*schema|upgrade_.*schema
    - from: scripts/init_autonomous_db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Explicit upgrade mode
      pattern: --force|upgrade
---

<objective>
Implement an idempotent, additive schema upgrade baseline for autonomous prediction tables.

Purpose: Safely reconcile long-lived SQLite schemas with current runtime expectations.
Output: Upgrade routine integrated into DB lifecycle paths with preservation guarantees.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md
@src/bitbat/autonomous/models.py
@src/bitbat/autonomous/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive idempotent schema upgrade routine</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Extend compatibility module with upgrade functionality that adds missing required columns (including `predicted_price`) only when absent. Ensure operations are additive and safe for repeated execution.</action>
  <verify>poetry run pytest tests/autonomous/test_schema_compat.py -q -k "upgrade or idempotent"</verify>
  <done>Upgrade routine can migrate legacy schemas and returns deterministic status on repeated runs.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate upgrade into DB initialization pathways</name>
  <files>src/bitbat/autonomous/db.py, scripts/init_autonomous_db.py</files>
  <action>Call the upgrade routine from controlled initialization paths so runtime DB handles are compatibility-safe before prediction IO. Keep behavior explicit and observable in logs/CLI output.</action>
  <verify>poetry run pytest tests/autonomous/test_db.py -q</verify>
  <done>DB initialization paths can repair legacy schema mismatch without data-destructive resets.</done>
</task>

<task type="auto">
  <name>Task 3: Validate idempotency and data preservation</name>
  <files>tests/autonomous/test_schema_compat.py</files>
  <action>Add tests that run upgrade multiple times and verify: no duplicate schema mutations, no exceptions on rerun, and existing prediction records remain intact.</action>
  <verify>poetry run pytest tests/autonomous/test_schema_compat.py -q</verify>
  <done>Automated tests enforce idempotent behavior and guard against data loss regressions.</done>
</task>

</tasks>

<verification>
- Legacy schema can be upgraded to include required runtime columns.
- Upgrade routine is idempotent across repeated invocations.
- Existing records survive schema upgrades unchanged.
</verification>

<success_criteria>
- Compatibility upgrade routine is implemented and wired into initialization paths.
- Re-running upgrade does not fail or mutate unexpectedly.
- Data preservation guarantees are test-backed.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-02-SUMMARY.md`
</output>
