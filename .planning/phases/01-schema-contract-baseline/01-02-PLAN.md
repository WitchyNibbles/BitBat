---
phase: 01-schema-contract-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/db.py
  - scripts/init_autonomous_db.py
  - tests/autonomous/test_schema_compat.py
autonomous: true
requirements:
  - SCHE-01
must_haves:
  truths:
    - Legacy autonomous DBs can be upgraded additively to include missing runtime columns.
    - Compatibility upgrade can be re-run safely without duplicate mutations or failures.
    - Existing prediction rows are preserved across compatibility upgrades.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Idempotent additive upgrade helper(s)
    - path: src/bitbat/autonomous/db.py
      provides: DB initialization path invoking compatibility upgrade
    - path: tests/autonomous/test_schema_compat.py
      provides: Idempotency and data-preservation assertions
  key_links:
    - from: src/bitbat/autonomous/db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Upgrade invocation before runtime repository operations
      pattern: upgrade|ensure_.*schema
    - from: scripts/init_autonomous_db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Explicit upgrade execution path
      pattern: upgrade|force
---

<objective>
Implement idempotent additive schema upgrade behavior for legacy SQLite compatibility.

Purpose: Move from detection-only baseline to safe automatic compatibility repair.
Output: Re-runnable upgrade routines wired to initialization paths with data-preservation guarantees.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md
@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add additive idempotent upgrade helpers</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Implement upgrade functions that add only missing nullable runtime columns (including `predicted_price`) with explicit guards. Ensure routine returns structured status (already-compatible vs upgraded) for operator observability.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py -q -k "upgrade or idempotent"</automated>
  </verify>
  <done>Upgrade helpers can reconcile legacy schema and rerun safely without side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Wire upgrade into DB initialization surfaces</name>
  <files>src/bitbat/autonomous/db.py, scripts/init_autonomous_db.py</files>
  <action>Invoke compatibility upgrade in DB initialization flows used by monitor/runtime entrypoints and explicit init script paths. Keep behavior explicit in logs/output and preserve non-destructive operation.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -q</automated>
  </verify>
  <done>Initialization paths can auto-repair legacy schema mismatches before runtime operations.</done>
</task>

<task type="auto">
  <name>Task 3: Add idempotency + data-preservation tests</name>
  <files>tests/autonomous/test_schema_compat.py</files>
  <action>Add tests proving repeated upgrades do not error or duplicate schema changes and that existing prediction rows remain accessible with unchanged core values after upgrade.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py -q</automated>
  </verify>
  <done>Automated tests protect against destructive or non-idempotent upgrade regressions.</done>
</task>

</tasks>

<verification>
- Legacy schemas can be upgraded to include required runtime columns.
- Upgrade path is idempotent and data-preserving.
- Initialization paths invoke compatibility repair before runtime IO.
</verification>

<success_criteria>
- Additive upgrade helpers exist and are used by initialization paths.
- Repeated execution remains safe and deterministic.
- Data-preservation and idempotency are regression-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-02-SUMMARY.md`
</output>
