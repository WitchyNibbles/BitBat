---
phase: 01-schema-contract-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/agent.py
  - src/bitbat/cli.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
autonomous: true
requirements:
  - SCHE-02
must_haves:
  truths:
    - Monitoring startup validates schema compatibility before attempting prediction IO.
    - Missing schema preconditions produce actionable error output instead of opaque DB trace failures.
    - CLI monitor paths expose compatibility failures clearly to operators.
  artifacts:
    - path: src/bitbat/autonomous/agent.py
      provides: Startup preflight invocation and failure handling for monitor cycles
    - path: src/bitbat/cli.py
      provides: User-facing monitor command error surfacing for schema incompatibility
    - path: tests/autonomous/test_agent_integration.py
      provides: Monitor preflight regression checks
  key_links:
    - from: src/bitbat/autonomous/agent.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Preflight validation call before run loop
      pattern: validate|ensure.*schema
    - from: src/bitbat/cli.py
      to: src/bitbat/autonomous/agent.py
      via: monitor command execution path
      pattern: monitor_(start|run_once|status)
---

<objective>
Add runtime schema preflight and actionable diagnostics for monitor startup and monitor-facing CLI workflows.

Purpose: Convert schema mismatch incidents from runtime crashes into explicit, operator-actionable readiness failures.
Output: Preflight-enforced monitor startup and clear compatibility error surfacing.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md
@.planning/phases/01-schema-contract-baseline/01-02-SUMMARY.md
@src/bitbat/autonomous/agent.py
@src/bitbat/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add monitor startup schema preflight</name>
  <files>src/bitbat/autonomous/agent.py, src/bitbat/autonomous/schema_compat.py</files>
  <action>Invoke schema compatibility validation at monitor startup before prediction/validation loops execute. Ensure failures include missing-column details and remediation hints.</action>
  <verify>poetry run pytest tests/autonomous/test_agent_integration.py -q</verify>
  <done>Monitor runtime refuses unsafe schema states with explicit compatibility failure diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Surface compatibility failures in monitor CLI workflows</name>
  <files>src/bitbat/cli.py</files>
  <action>Wrap monitor command entrypoints so schema compatibility exceptions are surfaced as clear, actionable CLI errors rather than generic operational traces.</action>
  <verify>poetry run pytest tests/test_cli.py -q -k "monitor"</verify>
  <done>Operators running monitor CLI commands receive clear next-step instructions when schema is incompatible.</done>
</task>

<task type="auto">
  <name>Task 3: Add preflight-focused regression tests</name>
  <files>tests/autonomous/test_agent_integration.py, tests/test_cli.py</files>
  <action>Add tests ensuring monitor and CLI paths fail fast on missing required columns and pass when compatibility upgrade/preflight has been applied.</action>
  <verify>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q</verify>
  <done>D1 readiness conditions are protected by automated tests in both monitor and CLI surfaces.</done>
</task>

</tasks>

<verification>
- Monitor startup blocks incompatible schemas before runtime prediction IO.
- CLI pathways expose schema incompatibility with actionable guidance.
- Regression tests cover both failing and compatible startup scenarios.
</verification>

<success_criteria>
- Preflight validation runs before monitor operational loop.
- Missing schema columns produce actionable diagnostics.
- Automated tests verify compatibility pass/fail behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-03-SUMMARY.md`
</output>
