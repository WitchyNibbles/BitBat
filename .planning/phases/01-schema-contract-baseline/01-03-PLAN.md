---
phase: 01-schema-contract-baseline
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/agent.py
  - src/bitbat/cli.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
autonomous: true
requirements:
  - SCHE-02
must_haves:
  truths:
    - Monitor startup validates schema compatibility before prediction runtime work begins.
    - Incompatible schema states fail fast with actionable remediation details.
    - CLI monitor commands surface compatibility failures clearly instead of opaque DB traces.
  artifacts:
    - path: src/bitbat/autonomous/agent.py
      provides: Monitor preflight compatibility gate
    - path: src/bitbat/cli.py
      provides: User-facing compatibility error handling in monitor commands
    - path: tests/autonomous/test_agent_integration.py
      provides: Preflight enforcement regression coverage
    - path: tests/test_cli.py
      provides: CLI compatibility failure messaging coverage
  key_links:
    - from: src/bitbat/autonomous/agent.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Preflight validation call before monitor cycle
      pattern: validate|ensure.*schema
    - from: src/bitbat/cli.py
      to: src/bitbat/autonomous/agent.py
      via: Monitor command execution path
      pattern: monitor_(run_once|start|status)
---

<objective>
Add startup preflight enforcement and actionable compatibility diagnostics for monitor entrypoints.

Purpose: Prevent runtime monitor crashes caused by schema mismatch and give operators clear recovery guidance.
Output: Preflight-enforced monitor startup and explicit CLI/runtime compatibility errors.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-RESEARCH.md
@.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md
@.planning/phases/01-schema-contract-baseline/01-02-SUMMARY.md
@src/bitbat/autonomous/agent.py
@src/bitbat/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce schema preflight in monitor bootstrap</name>
  <files>src/bitbat/autonomous/agent.py, src/bitbat/autonomous/schema_compat.py</files>
  <action>Invoke compatibility validation at monitor startup before prediction/validation work executes. Ensure failure payload includes missing column list and recommended remediation path (upgrade/audit command).</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q</automated>
  </verify>
  <done>Monitor startup blocks incompatible schemas and surfaces actionable diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Surface compatibility failures in monitor CLI commands</name>
  <files>src/bitbat/cli.py</files>
  <action>Update monitor-related CLI commands to convert compatibility exceptions into explicit user-facing errors with concise remediation guidance, avoiding raw OperationalError traces as primary output.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor"</automated>
  </verify>
  <done>CLI monitor entrypoints expose clear compatibility errors and next steps.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end preflight regression tests</name>
  <files>tests/autonomous/test_agent_integration.py, tests/test_cli.py</files>
  <action>Add/extend tests for both failing and passing compatibility scenarios: missing column should fail before runtime work; upgraded-compatible schema should allow monitor command flow to continue.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py -q</automated>
  </verify>
  <done>D1-related preflight behavior is guarded by automated monitor + CLI tests.</done>
</task>

</tasks>

<verification>
- Preflight validation executes before monitor operational logic.
- Compatibility failures are explicit and actionable.
- Passing compatible schema allows monitor path to proceed.
</verification>

<success_criteria>
- Monitor startup uses compatibility preflight gate.
- CLI monitor commands surface remediation-friendly compatibility errors.
- Regression tests cover both incompatible and compatible startup behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-contract-baseline/01-03-SUMMARY.md`
</output>
