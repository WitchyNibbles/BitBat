---
phase: 16-promotion-guardrails-and-optimization-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/model/optimize.py
  - src/bitbat/cli.py
  - src/bitbat/config/default.yaml
  - tests/model/test_optimize.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVAL-04
must_haves:
  truths:
    - Hyperparameter search uses nested walk-forward structure (inner tuning, outer evaluation) rather than single-loop tuning.
    - Optimization outputs include reproducible search provenance (search space, seed, folds/windows, trial history).
    - Operators can run nested optimization from CLI/config with deterministic artifacts.
  artifacts:
    - path: src/bitbat/model/optimize.py
      provides: Nested optimization engine and provenance payload builder
    - path: src/bitbat/cli.py
      provides: Model optimization command wiring and artifact persistence
    - path: tests/model/test_optimize.py
      provides: Regression coverage for nested split behavior and provenance determinism
  key_links:
    - from: src/bitbat/cli.py
      to: src/bitbat/model/optimize.py
      via: CLI model optimize command delegates nested tuning/evaluation and writes optimization summary artifacts
      pattern: optimize|nested|trials|provenance|walk_forward
    - from: src/bitbat/model/optimize.py
      to: tests/model/test_optimize.py
      via: Nested-loop execution and provenance fields are test-locked
      pattern: nested|outer|inner|summary|seed
---

<objective>
Establish nested optimization as the default safety baseline before candidate promotion.

Purpose: Fulfill the first half of EVAL-04 with a non-leaky optimization loop plus reproducible provenance.
Output: Nested optimization engine + CLI/config entrypoint + deterministic optimization artifacts.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-cost-aware-walk-forward-evaluation/15-03-SUMMARY.md
@src/bitbat/model/optimize.py
@src/bitbat/cli.py
@src/bitbat/config/default.yaml
@tests/model/test_optimize.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement nested walk-forward optimization engine (inner tune, outer evaluate)</name>
  <files>src/bitbat/model/optimize.py, tests/model/test_optimize.py</files>
  <action>Refactor optimization flow so inner folds tune hyperparameters and outer folds evaluate tuned candidates on held-out windows. Preserve deterministic behavior for fixed seed/folds and add explicit nested-run metadata (outer fold score table, selected params per outer window, aggregate out-of-sample score).</action>
  <verify>
    <automated>poetry run pytest tests/model/test_optimize.py -q -k "nested or optimize or walk_forward or summary"</automated>
  </verify>
  <done>Nested optimization returns deterministic outer-loop performance outputs with reproducible metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Persist optimization provenance artifacts for auditability</name>
  <files>src/bitbat/model/optimize.py, tests/model/test_optimize.py</files>
  <action>Extend optimization result serialization to include search provenance (search space bounds, seed, trial count, fold/window definitions, best-trial lineage, wall-clock metadata). Ensure payloads are JSON-safe and stable across repeat runs with identical inputs.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_optimize.py -q -k "provenance or deterministic or summary"</automated>
  </verify>
  <done>Optimization artifacts encode enough provenance to replay and audit the tuning decision path.</done>
</task>

<task type="auto">
  <name>Task 3: Expose nested optimization controls through CLI/config</name>
  <files>src/bitbat/cli.py, src/bitbat/config/default.yaml, tests/test_cli.py</files>
  <action>Add CLI/config controls for optimization trials and nested split settings, run the nested optimizer from model workflows, and persist a dedicated `metrics/optimization_summary.json` artifact. Keep existing model commands backward compatible when optimization is not requested.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and optimize and nested"</automated>
  </verify>
  <done>Operators can execute deterministic nested optimization runs and inspect persisted provenance artifacts.</done>
</task>

</tasks>

<verification>
- Optimization loop is nested and out-of-sample by construction.
- Search provenance is explicit, machine-readable, and reproducible.
- CLI/config can trigger nested optimization without breaking existing model flows.
</verification>

<success_criteria>
- EVAL-04 nested-validation foundations are implemented.
- Optimization artifacts can be audited and replayed from persisted metadata.
- Deterministic regression tests protect nested optimization behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/16-promotion-guardrails-and-optimization-safety/16-01-SUMMARY.md`
</output>
