---
phase: 16-promotion-guardrails-and-optimization-safety
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/model/evaluate.py
  - src/bitbat/model/optimize.py
  - src/bitbat/cli.py
  - tests/model/test_evaluate.py
  - tests/model/test_optimize.py
  - tests/test_cli.py
autonomous: true
requirements:
  - EVAL-04
must_haves:
  truths:
    - Multiple-testing safeguards are computed from optimization outcomes (not assumed) and persisted with candidate decisions.
    - Candidate promotion readiness is blocked when safeguard metrics indicate unstable or overfit results.
    - CLI artifacts clearly show safeguard metrics and fail reasons for rejected candidates.
  artifacts:
    - path: src/bitbat/model/evaluate.py
      provides: Safeguard metric helpers (multiple-testing and risk-adjusted confidence signals)
    - path: src/bitbat/model/optimize.py
      provides: Optimization summary now includes safeguard calculations and status flags
    - path: src/bitbat/cli.py
      provides: Candidate/safeguard reporting pipeline in model evaluation artifacts
  key_links:
    - from: src/bitbat/model/optimize.py
      to: src/bitbat/model/evaluate.py
      via: Optimization outputs feed safeguard calculators before candidate approval
      pattern: safeguards|multiple_testing|deflated|overfit|confidence
    - from: src/bitbat/cli.py
      to: tests/test_cli.py
      via: CLI artifacts expose safeguard metrics and rejection reasons
      pattern: candidate_reports|champion_decision|safeguards
---

<objective>
Add explicit multiple-testing skepticism to candidate selection outputs.

Purpose: Complete EVAL-04 by quantifying overfitting risk and rejecting unstable tuning outcomes.
Output: Safeguard metrics integrated into optimization/candidate artifacts and enforced in selection logic.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-promotion-guardrails-and-optimization-safety/16-01-PLAN.md
@src/bitbat/model/evaluate.py
@src/bitbat/model/optimize.py
@src/bitbat/cli.py
@tests/model/test_evaluate.py
@tests/model/test_optimize.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement multiple-testing safeguard metrics for optimization outcomes</name>
  <files>src/bitbat/model/evaluate.py, tests/model/test_evaluate.py</files>
  <action>Add deterministic safeguard helpers that summarize overfitting risk (for example, conservative Sharpe deflation and overfit-probability style diagnostics) from outer-fold outcomes. Return explicit pass/fail status with reasons and thresholds in a machine-readable payload.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_evaluate.py -q -k "safeguard or overfit or deflated or candidate"</automated>
  </verify>
  <done>Safeguard metrics are deterministic, thresholded, and available as reusable evaluation helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate safeguards into optimization summary and champion selection inputs</name>
  <files>src/bitbat/model/optimize.py, src/bitbat/model/evaluate.py, tests/model/test_optimize.py</files>
  <action>Wire safeguard metrics into optimization summary payloads and candidate selection inputs so unstable runs are flagged prior to champion promotion. Ensure summary includes both raw performance and safeguard-adjusted status fields for each candidate.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_optimize.py tests/model/test_evaluate.py -q -k "nested or safeguard or candidate or champion"</automated>
  </verify>
  <done>Optimization artifacts include safeguard status that can block unstable candidate promotion.</done>
</task>

<task type="auto">
  <name>Task 3: Surface safeguard metrics and block reasons in CLI evaluation artifacts</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Update model CLI evaluation output to persist safeguard payloads in `metrics/cv_summary.json` (and related evaluation artifacts), including explicit rejection reasons when safeguards fail. Preserve backward compatibility for existing consumers by extending, not replacing, existing keys.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and (candidate or champion or safeguard or evaluation)"</automated>
  </verify>
  <done>CLI artifacts transparently report multiple-testing safeguards and candidate acceptance/rejection reasons.</done>
</task>

</tasks>

<verification>
- Safeguard metrics are computed deterministically from nested optimization outputs.
- Candidate selection receives safeguard pass/fail signals before promotion decisions.
- CLI artifacts persist safeguard payloads and clear rejection reasons.
</verification>

<success_criteria>
- EVAL-04 multiple-testing safeguards are fully integrated into model evaluation artifacts.
- Unstable/overfit candidate runs are automatically flagged and blocked by rule.
- Safeguard behavior is regression-locked in optimize/evaluate/CLI tests.
</success_criteria>

<output>
After completion, create `.planning/phases/16-promotion-guardrails-and-optimization-safety/16-02-SUMMARY.md`
</output>
