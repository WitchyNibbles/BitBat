---
phase: 16-promotion-guardrails-and-optimization-safety
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - src/bitbat/model/evaluate.py
  - src/bitbat/autonomous/retrainer.py
  - src/bitbat/cli.py
  - src/bitbat/config/default.yaml
  - tests/model/test_evaluate.py
  - tests/autonomous/test_retrainer.py
  - tests/test_cli.py
autonomous: true
requirements:
  - OPER-02
must_haves:
  truths:
    - Promotion gate requires incumbent-beating performance across consecutive out-of-sample windows, not single-window wins.
    - Candidates violating drawdown guardrails are rejected even when headline score improvements exist.
    - Promotion decisions are deterministic, persisted, and reproducible across CLI and autonomous retraining flows.
  artifacts:
    - path: src/bitbat/model/evaluate.py
      provides: Promotion-gate evaluator for consecutive-window and drawdown guardrail checks
    - path: src/bitbat/autonomous/retrainer.py
      provides: Autonomous deployment gate honoring promotion guardrail outputs
    - path: src/bitbat/cli.py
      provides: Operator-facing promotion-gate report and persisted decision metadata
  key_links:
    - from: src/bitbat/model/evaluate.py
      to: src/bitbat/autonomous/retrainer.py
      via: Promotion gate output drives deploy/no-deploy decisions
      pattern: promotion_gate|consecutive|drawdown|incumbent
    - from: src/bitbat/cli.py
      to: tests/autonomous/test_retrainer.py
      via: Shared promotion decision schema remains consistent between CLI and autonomous runtime
      pattern: champion_decision|promotion_gate|deployed
---

<objective>
Enforce promotion safety by requiring stable incumbent-beating performance under drawdown constraints.

Purpose: Fulfill OPER-02 so autonomous promotion cannot ship overfit or drawdown-unsafe candidates.
Output: Deterministic promotion gate logic wired into CLI evaluation and retrainer deployment flows.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-promotion-guardrails-and-optimization-safety/16-02-PLAN.md
@src/bitbat/model/evaluate.py
@src/bitbat/autonomous/retrainer.py
@src/bitbat/cli.py
@src/bitbat/config/default.yaml
@tests/model/test_evaluate.py
@tests/autonomous/test_retrainer.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement promotion-gate evaluator with consecutive-window incumbent checks</name>
  <files>src/bitbat/model/evaluate.py, tests/model/test_evaluate.py</files>
  <action>Add explicit promotion-gate helper(s) that compare candidate vs incumbent over consecutive out-of-sample windows and enforce configurable drawdown ceilings. Return a deterministic decision payload with per-window verdicts, guardrail thresholds, and final pass/fail rationale.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_evaluate.py -q -k "promotion or gate or incumbent or drawdown"</automated>
  </verify>
  <done>Promotion gate deterministically rejects candidates that fail consecutive-window or drawdown constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce promotion gate in autonomous retrainer deployment flow</name>
  <files>src/bitbat/autonomous/retrainer.py, tests/autonomous/test_retrainer.py</files>
  <action>Update retrainer deployment decisions to require promotion-gate approval in addition to existing improvement thresholds. Persist promotion-gate metadata into retraining events/model metadata so rejected promotions are auditable with explicit fail reasons.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_retrainer.py -q -k "deploy or promotion or gate or drawdown"</automated>
  </verify>
  <done>Autonomous retrainer never deploys candidates that fail consecutive-window or drawdown guardrails.</done>
</task>

<task type="auto">
  <name>Task 3: Surface promotion-gate outcomes in CLI artifacts and defaults</name>
  <files>src/bitbat/cli.py, src/bitbat/config/default.yaml, tests/test_cli.py</files>
  <action>Add promotion-gate defaults to config and ensure CLI evaluation artifacts include promotion decision details (consecutive-window checks, drawdown threshold, decision reason). Maintain existing artifact keys while extending schema for downstream consumers.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and (promotion or gate or champion or drawdown)"</automated>
  </verify>
  <done>CLI and autonomous flows share one persisted promotion-gate decision schema suitable for operations audits.</done>
</task>

</tasks>

<verification>
- Promotion decisions require consecutive incumbent-beating out-of-sample windows.
- Drawdown guardrails can veto otherwise high-scoring candidates.
- Promotion gate outputs are deterministic and persisted across CLI/autonomous paths.
</verification>

<success_criteria>
- OPER-02 is satisfied with explicit consecutive-window and drawdown guardrails.
- Autonomous deployment path enforces the same promotion decision contract used by CLI evaluation.
- Promotion gate behavior is regression-locked and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/16-promotion-guardrails-and-optimization-safety/16-03-SUMMARY.md`
</output>
