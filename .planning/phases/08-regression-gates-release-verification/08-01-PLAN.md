---
phase: 08-regression-gates-release-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/autonomous/test_phase8_d1_monitor_schema_complete.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
  - tests/api/test_health.py
  - tests/api/test_metrics.py
autonomous: true
requirements:
  - QUAL-01
must_haves:
  truths:
    - D1 release coverage explicitly verifies schema compatibility behavior and monitor runtime stability under both compatible and incompatible DB states.
    - Critical monitor DB failures remain operator-visible with actionable remediation across agent, CLI, and API surfaces.
    - A canonical D1 regression command exists and fails deterministically on schema/monitor contract drift.
  artifacts:
    - path: tests/autonomous/test_phase8_d1_monitor_schema_complete.py
      provides: Phase-level D1 gate exercising schema preflight, monitor execution, and runtime DB error surfacing
    - path: tests/autonomous/test_agent_integration.py
      provides: Existing monitor integration invariants extended/aligned for release gate coverage
    - path: tests/test_cli.py
      provides: CLI monitor diagnostics and schema/runtime error messaging assertions
    - path: tests/api/test_health.py
      provides: Schema-readiness service behavior checks under incompatible/compatible states
    - path: tests/api/test_metrics.py
      provides: Metrics contract checks for schema gauges and prediction gauge gating
  key_links:
    - from: tests/autonomous/test_phase8_d1_monitor_schema_complete.py
      to: tests/autonomous/test_agent_integration.py
      via: Phase-level gate composes and tightens monitor/schema invariants already established in agent integration tests
      pattern: schema_preflight|monitor|runtime_db_failure
    - from: tests/test_cli.py
      to: tests/autonomous/test_phase8_d1_monitor_schema_complete.py
      via: CLI monitor diagnostics align with agent-level failure surfaces and remediation semantics
      pattern: monitor|schema|runtime_db_error
    - from: tests/api/test_health.py
      to: tests/api/test_metrics.py
      via: Health and metrics schema-readiness contracts stay consistent under incompatible schema conditions
      pattern: schema_readiness|schema_compatible|missing_columns
---

<objective>
Add and consolidate D1 regression gates for schema compatibility and monitor runtime stability.

Purpose: Satisfy QUAL-01 with explicit release-grade coverage and reproducible verification commands.
Output: Phase-level D1 gate test plus aligned CLI/API/agent assertions and canonical D1 command.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-regression-gates-release-verification/08-RESEARCH.md
@tests/autonomous/test_agent_integration.py
@tests/test_cli.py
@tests/api/test_health.py
@tests/api/test_metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 D1 monitor/schema regression gate module</name>
  <files>tests/autonomous/test_phase8_d1_monitor_schema_complete.py</files>
  <action>Create a dedicated phase-level D1 test module that exercises schema-preflight blocking, upgraded-schema monitor success, and runtime DB failure surfacing semantics in one focused gate. Reuse realistic fixtures and enforce explicit remediation-oriented assertions.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_phase8_d1_monitor_schema_complete.py -q</automated>
  </verify>
  <done>Phase-level D1 gate exists and fails deterministically when schema or monitor runtime contracts regress.</done>
</task>

<task type="auto">
  <name>Task 2: Align CLI/API monitor schema diagnostics with release-gate expectations</name>
  <files>tests/test_cli.py, tests/api/test_health.py, tests/api/test_metrics.py</files>
  <action>Expand or refine assertions so CLI monitor messaging, health schema-readiness status, and metrics schema gauges remain consistent with D1 contract language and failure semantics validated by the new phase gate.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py tests/api/test_health.py tests/api/test_metrics.py -q -k "schema or monitor or runtime_db_error"</automated>
  </verify>
  <done>CLI/API schema-monitor diagnostics are release-aligned and protected by focused regression checks.</done>
</task>

<task type="auto">
  <name>Task 3: Lock canonical D1 regression command across agent/CLI/API suites</name>
  <files>tests/autonomous/test_phase8_d1_monitor_schema_complete.py, tests/autonomous/test_agent_integration.py, tests/test_cli.py, tests/api/test_health.py, tests/api/test_metrics.py</files>
  <action>Finalize D1 coverage so one canonical command runs the phase gate plus supporting suites and serves as the authoritative D1 release check.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_phase8_d1_monitor_schema_complete.py tests/autonomous/test_agent_integration.py tests/test_cli.py tests/api/test_health.py tests/api/test_metrics.py -q -k "schema or monitor"</automated>
  </verify>
  <done>A single command provides reproducible D1 release evidence for schema compatibility and monitor stability.</done>
</task>

</tasks>

<verification>
- Schema incompatibility and compatibility states are both validated under monitor execution surfaces.
- Runtime DB failures remain visible with actionable monitor-step diagnostics.
- D1 release verification can be re-run using one canonical command.
</verification>

<success_criteria>
- QUAL-01 is covered by explicit phase-level and supporting regression gates.
- D1 regressions fail fast across agent, CLI, and API health/metrics surfaces.
- Canonical D1 command is stable and reusable for release validation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-regression-gates-release-verification/08-01-SUMMARY.md`
</output>
