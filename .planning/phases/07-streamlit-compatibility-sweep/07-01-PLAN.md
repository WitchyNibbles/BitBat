---
phase: 07-streamlit-compatibility-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - streamlit/pages/0_Quick_Start.py
  - streamlit/pages/4_🔧_System.py
  - tests/gui/test_streamlit_width_compat.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - GUI-01
  - GUI-02
must_haves:
  truths:
    - Runtime Streamlit pages no longer use deprecated `use_container_width=True` semantics.
    - Content- and stretch-width behavior uses explicit modern width arguments (`width='content'` or `width='stretch'`) instead of boolean flags.
    - Width API compatibility is protected by deterministic automated checks in the GUI test suite.
  artifacts:
    - path: streamlit/pages/0_Quick_Start.py
      provides: Migrated width arguments for primary training, monitoring, timeline, and retrain controls
    - path: streamlit/pages/4_🔧_System.py
      provides: Migrated width argument for autonomous settings save control
    - path: tests/gui/test_streamlit_width_compat.py
      provides: Runtime source-compatibility assertions for deprecated width API usage
    - path: tests/gui/test_complete_gui.py
      provides: Integration regression assurance after width API migration
  key_links:
    - from: tests/gui/test_streamlit_width_compat.py
      to: streamlit/pages/0_Quick_Start.py
      via: Source-based checks enforce no deprecated width API usage in Quick Start controls and timeline rendering
      pattern: use_container_width|width
    - from: tests/gui/test_streamlit_width_compat.py
      to: streamlit/pages/4_🔧_System.py
      via: Source-based checks enforce modern width argument usage in system settings controls
      pattern: use_container_width|width
    - from: tests/gui/test_complete_gui.py
      to: streamlit/pages/0_Quick_Start.py
      via: Existing GUI integration behavior remains stable after width argument migration
      pattern: timeline|monitoring|status
---

<objective>
Apply Streamlit width API migration across active runtime pages and lock GUI-01/GUI-02 with automated compatibility tests.

Purpose: Remove deprecated width argument patterns without altering existing user workflows.
Output: Migrated runtime pages plus test coverage preventing deprecated width API regressions.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-streamlit-compatibility-sweep/07-RESEARCH.md
@streamlit/pages/0_Quick_Start.py
@streamlit/pages/4_🔧_System.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deprecated width arguments in runtime Streamlit pages</name>
  <files>streamlit/pages/0_Quick_Start.py, streamlit/pages/4_🔧_System.py</files>
  <action>Replace every `use_container_width=True` runtime call with explicit modern width semantics (`width='stretch'`), preserving existing button and chart behavior. If any `use_container_width=False` call is discovered during migration, replace it with `width='content'` and preserve intended compact rendering.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_streamlit_width_compat.py -q -k "deprecated_usage_absent"</automated>
  </verify>
  <done>Active runtime pages no longer contain `use_container_width` arguments and preserve full-width behavior where previously intended.</done>
</task>

<task type="auto">
  <name>Task 2: Add Streamlit width compatibility regression tests for runtime source files</name>
  <files>tests/gui/test_streamlit_width_compat.py</files>
  <action>Create deterministic tests that scan `streamlit/app.py` and `streamlit/pages/*.py` and assert: (1) no deprecated `use_container_width` usage, (2) no boolean width arguments, and (3) explicit width values are limited to supported compatibility literals (`stretch`/`content`) when provided as constants.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_streamlit_width_compat.py -q</automated>
  </verify>
  <done>Compatibility tests fail on any reintroduction of deprecated or invalid width API usage in runtime Streamlit files.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile GUI integration coverage after width migration</name>
  <files>tests/gui/test_complete_gui.py</files>
  <action>Add or adjust targeted integration assertions as needed so primary GUI flows validated by existing integration fixtures remain green after migration. Keep assertions focused on runtime stability rather than visual styling details.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py tests/gui/test_streamlit_width_compat.py -q</automated>
  </verify>
  <done>Core GUI integration tests and width compatibility tests pass together after migration changes.</done>
</task>

</tasks>

<verification>
- Deprecated Streamlit width API usage is removed from runtime page code.
- Width behavior is expressed via modern explicit width arguments.
- Automated compatibility tests prevent regression of GUI-01/GUI-02.
</verification>

<success_criteria>
- GUI-01 is satisfied by code migration from deprecated `use_container_width=True` to `width='stretch'`.
- GUI-02 is protected through tests enforcing no boolean width API usage and acceptance of modern content/stretch semantics.
- Width migration does not break existing GUI integration behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/07-streamlit-compatibility-sweep/07-01-SUMMARY.md`
</output>
