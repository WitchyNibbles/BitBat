---
phase: 04-monitor-flow-consistency-api-alignment
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/api/schemas.py
  - src/bitbat/api/routes/predictions.py
  - src/bitbat/gui/timeline.py
  - src/bitbat/gui/widgets.py
  - streamlit/app.py
  - tests/api/test_predictions.py
  - tests/api/test_phase4_complete.py
  - tests/gui/test_timeline.py
  - tests/gui/test_widgets.py
autonomous: true
requirements:
  - MON-02
  - API-01
must_haves:
  truths:
    - API prediction responses expose fields with the same semantics as monitor-persisted prediction records.
    - Timeline/widget consumers handle unavailable confidence/probability values without fabricating or mislabeling prediction confidence.
    - For the same prediction row, API and GUI surfaces agree on direction, confidence semantics, and realized outcome indicators.
  artifacts:
    - path: src/bitbat/api/schemas.py
      provides: Prediction response schema that carries aligned semantic fields for timeline/API consumers
    - path: src/bitbat/api/routes/predictions.py
      provides: Canonical DB-to-API mapping for prediction semantic fields
    - path: src/bitbat/gui/timeline.py
      provides: Timeline read-model/render behavior aligned with normalized confidence semantics
    - path: src/bitbat/gui/widgets.py
      provides: Latest-prediction widget data contract aligned with monitor/API semantics
    - path: streamlit/app.py
      provides: Home page rendering resilient to nullable confidence semantics
    - path: tests/api/test_predictions.py
      provides: API regression coverage for aligned prediction fields
    - path: tests/gui/test_timeline.py
      provides: Timeline regression coverage for confidence semantics and direction/outcome display
    - path: tests/gui/test_widgets.py
      provides: Widget regression coverage for latest-prediction semantic contract
  key_links:
    - from: src/bitbat/api/routes/predictions.py
      to: src/bitbat/api/schemas.py
      via: Prediction endpoint mapping reflects canonical semantic contract for response fields
      pattern: _prediction_to_response|PredictionResponse
    - from: src/bitbat/gui/timeline.py
      to: src/bitbat/api/schemas.py
      via: Timeline confidence/direction presentation follows the same field semantics as API contracts
      pattern: get_timeline_data|build_timeline_figure|confidence|predicted_direction
    - from: streamlit/app.py
      to: src/bitbat/gui/widgets.py
      via: Home page latest-prediction card consumes normalized widget contract without key mismatches
      pattern: get_latest_prediction|render_prediction_card|confidence
---

<objective>
Align API and GUI read surfaces with normalized monitor prediction semantics.

Purpose: Ensure timeline/API consumers observe one coherent direction/confidence/outcome contract.
Output: API schema/route and GUI timeline/widgets that agree with monitor-persisted semantics.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-monitor-flow-consistency-api-alignment/04-RESEARCH.md
@.planning/phases/04-monitor-flow-consistency-api-alignment/04-01-PLAN.md
@src/bitbat/api/schemas.py
@src/bitbat/api/routes/predictions.py
@src/bitbat/gui/timeline.py
@src/bitbat/gui/widgets.py
@streamlit/app.py
@tests/api/test_predictions.py
@tests/api/test_phase4_complete.py
@tests/gui/test_timeline.py
@tests/gui/test_widgets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand API prediction read model to expose aligned semantic fields</name>
  <files>src/bitbat/api/schemas.py, src/bitbat/api/routes/predictions.py</files>
  <action>Update prediction response schema and row-mapping helpers to expose normalized probability/confidence semantics (with additive nullable fields to preserve compatibility). Ensure latest/history/performance endpoints operate against the same direction/correctness semantics established in 04-01.</action>
  <verify>
    <automated>poetry run pytest tests/api/test_predictions.py tests/api/test_phase4_complete.py -q -k "predictions or history or latest or performance"</automated>
  </verify>
  <done>API prediction endpoints return semantically aligned fields that match monitor-persisted records and remain backward compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Align GUI timeline and widget read helpers with nullable confidence semantics</name>
  <files>src/bitbat/gui/timeline.py, src/bitbat/gui/widgets.py, streamlit/app.py</files>
  <action>Refactor timeline/widget helpers to treat confidence/probability as optional semantics, avoid implicit `0%` fallbacks when unavailable, and keep rendering resilient when fields are null. Remove key mismatches between widget payload and Streamlit home-page consumers.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py tests/gui/test_widgets.py -q</automated>
  </verify>
  <done>GUI timeline/widgets and home-page consumers render prediction semantics correctly with or without confidence probabilities.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-surface semantic consistency regression tests</name>
  <files>tests/api/test_predictions.py, tests/api/test_phase4_complete.py, tests/gui/test_timeline.py, tests/gui/test_widgets.py</files>
  <action>Add regression tests that compare API payloads and GUI-read-model outputs for the same seeded prediction rows (including nullable confidence cases) and assert consistent direction/outcome semantics across surfaces.</action>
  <verify>
    <automated>poetry run pytest tests/api/test_predictions.py tests/api/test_phase4_complete.py tests/gui/test_timeline.py tests/gui/test_widgets.py -q -k "prediction or timeline or widget or confidence"</automated>
  </verify>
  <done>Automated tests fail if API/GUI semantic alignment regresses for timeline-consumed prediction fields.</done>
</task>

</tasks>

<verification>
- API prediction payload semantics match monitor-persisted direction/confidence/outcome definitions.
- GUI timeline and widgets interpret nullable confidence/probability consistently and safely.
- Cross-surface tests assert consistent prediction semantics for identical records.
</verification>

<success_criteria>
- API-01 is satisfied with coherent API + GUI timeline field semantics.
- MON-02 consistency extends from monitor persistence into read surfaces and presentation helpers.
- Regression coverage prevents cross-surface semantic drift from reappearing.
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitor-flow-consistency-api-alignment/04-02-SUMMARY.md`
</output>
