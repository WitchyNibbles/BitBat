---
phase: 04-monitor-flow-consistency-api-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/db.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/autonomous/validator.py
  - tests/autonomous/test_db.py
  - tests/autonomous/test_validator.py
  - tests/autonomous/test_agent_integration.py
autonomous: true
requirements:
  - MON-02
must_haves:
  truths:
    - Monitor prediction persistence uses one explicit semantic contract for direction/probability/return fields across freq/horizon pairs.
    - Validation correctness computed during realization is persisted consistently with validator logic.
    - Monitor read paths return semantically consistent records for pending and realized predictions across active freq/horizon settings.
  artifacts:
    - path: src/bitbat/autonomous/db.py
      provides: Canonical prediction semantic normalization and realization correctness persistence logic
    - path: src/bitbat/autonomous/predictor.py
      provides: Predictor write path aligned to canonical persistence contract
    - path: src/bitbat/autonomous/validator.py
      provides: Validator realization flow aligned to canonical correctness semantics
    - path: tests/autonomous/test_db.py
      provides: Regression coverage for prediction write/read semantics and defaults
    - path: tests/autonomous/test_validator.py
      provides: Regression coverage for correctness persistence consistency
    - path: tests/autonomous/test_agent_integration.py
      provides: Integration coverage across monitor prediction and validation cycles
  key_links:
    - from: src/bitbat/autonomous/predictor.py
      to: src/bitbat/autonomous/db.py
      via: Predictor prediction payloads are normalized at DB store boundary before persistence
      pattern: predict_latest|store_prediction
    - from: src/bitbat/autonomous/validator.py
      to: src/bitbat/autonomous/db.py
      via: Validator realization flow persists correctness using shared semantics
      pattern: validate_prediction|validate_batch|realize_prediction
    - from: src/bitbat/autonomous/db.py
      to: src/bitbat/autonomous/metrics.py
      via: Persisted `correct` and probability fields remain coherent for downstream metrics
      pattern: realize_prediction|get_recent_predictions|correct|p_up|p_down
---

<objective>
Normalize monitor prediction write/read semantics for phase-4 consistency goals.

Purpose: Remove semantic drift between predictor, validator, and persisted prediction records.
Output: Canonical persistence + realization contract used consistently across active freq/horizon paths.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-monitor-flow-consistency-api-alignment/04-RESEARCH.md
@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/predictor.py
@src/bitbat/autonomous/validator.py
@src/bitbat/autonomous/metrics.py
@tests/autonomous/test_db.py
@tests/autonomous/test_validator.py
@tests/autonomous/test_agent_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce canonical prediction field normalization in autonomous DB boundary</name>
  <files>src/bitbat/autonomous/db.py, src/bitbat/autonomous/predictor.py</files>
  <action>Add a shared normalization path for prediction writes so `predicted_direction`, `predicted_return`, `predicted_price`, and probability/confidence-related fields carry explicit semantics (including unavailable values) instead of implicit fabricated defaults. Update predictor write payload plumbing to use this contract across all freq/horizon runs.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -q -k "store_prediction or prediction or freq or horizon"</automated>
  </verify>
  <done>Prediction rows written by monitor have consistent, explicit semantics for direction/return/probability fields in DB records.</done>
</task>

<task type="auto">
  <name>Task 2: Align realization correctness persistence with validator semantics</name>
  <files>src/bitbat/autonomous/db.py, src/bitbat/autonomous/validator.py</files>
  <action>Refactor realization updates so persisted `correct` matches validator-derived correctness logic (including sign-based evaluation when `predicted_return` exists). Ensure batch validation summaries and stored records cannot diverge for the same prediction row.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_validator.py -q -k "validate_batch or validate_prediction or correct or realize"</automated>
  </verify>
  <done>Validator-reported correctness and persisted DB correctness are deterministic and consistent for realized predictions.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for cross-dimension monitor consistency</name>
  <files>tests/autonomous/test_db.py, tests/autonomous/test_validator.py, tests/autonomous/test_agent_integration.py</files>
  <action>Add focused regression tests covering multiple freq/horizon records, pending-to-realized transitions, and semantic consistency of probability/correctness fields so monitor write/read behavior remains stable under future changes.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py tests/autonomous/test_validator.py tests/autonomous/test_agent_integration.py -q -k "monitor or prediction or freq or horizon or correct"</automated>
  </verify>
  <done>Automated tests fail if monitor prediction semantic consistency regresses across write/read and realization paths.</done>
</task>

</tasks>

<verification>
- Prediction write semantics are explicit and consistent across monitor freq/horizon settings.
- Realized correctness values are persisted using the same rule validator uses for reporting.
- Regression tests protect monitor write/read semantic parity under future changes.
</verification>

<success_criteria>
- MON-02 is satisfied at autonomous monitor write/read boundaries.
- Predictor, validator, and DB persistence share one coherent prediction semantic contract.
- Cross-dimension monitor consistency is regression-tested for both pending and realized predictions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-monitor-flow-consistency-api-alignment/04-01-SUMMARY.md`
</output>
