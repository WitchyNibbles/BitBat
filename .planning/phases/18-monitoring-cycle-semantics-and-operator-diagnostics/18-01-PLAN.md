---
phase: 18-monitoring-cycle-semantics-and-operator-diagnostics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/agent.py
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/cli.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MON-04
must_haves:
  truths:
    - Monitor cycle output distinguishes prediction generation outcomes instead of collapsing to ambiguous `prediction=None`.
    - Cycle payload explicitly states realization state (no predictions, pending validations, or realized metrics available).
    - `bitbat monitor run-once` surfaces the same state semantics visible in cycle payload/log output.
  artifacts:
    - path: src/bitbat/autonomous/predictor.py
      provides: Explicit prediction-generation status/reason contract for no-prediction scenarios
    - path: src/bitbat/autonomous/agent.py
      provides: Structured cycle summary payload with prediction and realization state fields
    - path: src/bitbat/cli.py
      provides: Operator-facing run-once output for cycle state semantics
    - path: tests/autonomous/test_agent_integration.py
      provides: Regression coverage for cycle status payload semantics
  key_links:
    - from: src/bitbat/autonomous/predictor.py
      to: src/bitbat/autonomous/agent.py
      via: Predictor status/reason feeds cycle summary contract and no-data semantics
      pattern: prediction_status|reason|missing_model|insufficient_data
    - from: src/bitbat/autonomous/agent.py
      to: src/bitbat/cli.py
      via: CLI run-once echoes agent cycle-state payload instead of inferring from zero metrics
      pattern: cycle_state|prediction_state|realization_state
---

<objective>
Make monitor cycle semantics explicit for generated, pending, and realized states.

Purpose: Fulfill MON-04 by replacing ambiguous cycle outputs with a structured state contract.
Output: Predictor/agent/CLI cycle payload semantics that clearly communicate why predictions or realizations are absent.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/bitbat/autonomous/predictor.py
@src/bitbat/autonomous/agent.py
@src/bitbat/cli.py
@tests/autonomous/test_agent_integration.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define explicit no-prediction status contract in live predictor</name>
  <files>src/bitbat/autonomous/predictor.py, tests/autonomous/test_agent_integration.py</files>
  <action>Refactor predictor return semantics so no-prediction paths return structured diagnostics (status + reason + optional details) for cases like missing model, missing prices, insufficient bars, duplicate bar, or feature/model mismatch. Keep DB writes unchanged when a prediction is generated.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q -k "predict or no_prediction or status or reason"</automated>
  </verify>
  <done>Predictor communicates explicit no-prediction reasons without relying on traceback inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Build cycle-state payload contract in monitoring agent</name>
  <files>src/bitbat/autonomous/agent.py, tests/autonomous/test_agent_integration.py</files>
  <action>Update `run_once` to emit explicit cycle-state fields covering prediction generation and realization availability (`none`, `pending`, `realized`). Ensure metrics/snapshot behavior is coherent when realized count is zero and preserve current drift/retraining behavior.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q -k "cycle_state or realization or metrics"</automated>
  </verify>
  <done>Agent cycle result is self-explanatory when predictions are absent, pending, or realized.</done>
</task>

<task type="auto">
  <name>Task 3: Surface cycle semantics through `bitbat monitor run-once` output</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Extend monitor run-once CLI output to print the new cycle-state fields and key reason text so operators can distinguish no-prediction, pending-realization, and realized-metrics states from one command invocation.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor and run_once and (state or semantics or pending or realized)"</automated>
  </verify>
  <done>CLI run-once output communicates explicit cycle semantics without ambiguous all-zero interpretation.</done>
</task>

</tasks>

<verification>
- Cycle payload includes explicit prediction-generation and realization-state fields.
- No-prediction outcomes carry reason codes/messages.
- Run-once output shows unambiguous cycle semantics for operators.
</verification>

<success_criteria>
- MON-04 semantics are encoded in predictor/agent/CLI outputs.
- Zero-realized windows no longer appear as unexplained all-zero monitor states.
- Regression tests lock cycle-state contract behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/18-monitoring-cycle-semantics-and-operator-diagnostics/18-01-SUMMARY.md`
</output>
