---
phase: 18-monitoring-cycle-semantics-and-operator-diagnostics
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/autonomous/predictor.py
  - src/bitbat/autonomous/agent.py
  - scripts/run_monitoring_agent.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
  - tests/test_run_monitoring_agent.py
autonomous: true
requirements:
  - MON-06
  - MON-04
must_haves:
  truths:
    - Missing-model root cause appears in one operator-facing cycle summary/log line, without requiring traceback inspection.
    - Heartbeat and run-once surfaces include cycle diagnostic reason text for no-prediction conditions.
    - Cycle diagnostic semantics remain consistent when realized metrics are unavailable.
  artifacts:
    - path: src/bitbat/autonomous/predictor.py
      provides: Stable diagnostic reason codes/messages for no-prediction conditions
    - path: src/bitbat/autonomous/agent.py
      provides: Cycle summary fields and concise operator-log lines with root-cause diagnostics
    - path: scripts/run_monitoring_agent.py
      provides: Heartbeat error/diagnostic propagation for cycle-level operator visibility
    - path: tests/test_run_monitoring_agent.py
      provides: Regression coverage for diagnostic propagation in heartbeat payloads
  key_links:
    - from: src/bitbat/autonomous/predictor.py
      to: src/bitbat/autonomous/agent.py
      via: Predictor diagnostic reasons drive cycle summary/log messages
      pattern: missing_model|diagnostic|reason|prediction_status
    - from: src/bitbat/autonomous/agent.py
      to: scripts/run_monitoring_agent.py
      via: Agent cycle diagnostics are surfaced through daemon heartbeat/log output
      pattern: cycle_summary|status|diagnostic_reason
    - from: src/bitbat/cli.py
      to: tests/test_cli.py
      via: Run-once output includes concise root-cause diagnostics for no-prediction cycles
      pattern: monitor run-once|reason|missing_model|no_prediction
---

<objective>
Expose missing-model and no-prediction root causes through operator-facing diagnostics.

Purpose: Fulfill MON-06 (and reinforce MON-04) by making cycle-level root causes visible without stack traces.
Output: Consistent diagnostic reason propagation from predictor to agent, CLI run-once, and monitor heartbeat/log surfaces.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/bitbat/autonomous/predictor.py
@src/bitbat/autonomous/agent.py
@src/bitbat/cli.py
@scripts/run_monitoring_agent.py
@tests/autonomous/test_agent_integration.py
@tests/test_cli.py
@tests/test_run_monitoring_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize predictor no-prediction diagnostic reason schema</name>
  <files>src/bitbat/autonomous/predictor.py, tests/autonomous/test_agent_integration.py</files>
  <action>Normalize no-prediction diagnostics into stable reason codes/messages (including missing-model) that can be consumed by agent/CLI/heartbeat surfaces. Preserve current prediction persistence behavior when generation succeeds.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py -q -k "missing_model or diagnostic or no_prediction"</automated>
  </verify>
  <done>No-prediction paths yield stable diagnostic reasons suitable for single-line operator reporting.</done>
</task>

<task type="auto">
  <name>Task 2: Surface concise cycle diagnostic root-cause lines in agent/CLI output</name>
  <files>src/bitbat/autonomous/agent.py, src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Add concise cycle-level diagnostic summary fields and log/output lines that include root-cause reason text (for example missing model artifact) without requiring traceback parsing. Ensure diagnostics are present in run-once output and remain coherent with cycle-state semantics.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor and run_once and (reason or root cause or missing_model or no_prediction)"</automated>
  </verify>
  <done>Operators can identify no-prediction root cause from one run-once cycle summary line.</done>
</task>

<task type="auto">
  <name>Task 3: Propagate cycle diagnostics into heartbeat updates and lock with tests</name>
  <files>scripts/run_monitoring_agent.py, tests/test_run_monitoring_agent.py</files>
  <action>Extend heartbeat update flow to carry latest cycle diagnostic reason/status fields (when present) so daemon operators can diagnose no-prediction root causes from heartbeat/log artifacts without stack traces.</action>
  <verify>
    <automated>poetry run pytest tests/test_run_monitoring_agent.py -q -k "diagnostic or heartbeat or error"</automated>
  </verify>
  <done>Heartbeat payload includes cycle diagnostic reason context for operator troubleshooting.</done>
</task>

</tasks>

<verification>
- Missing-model/no-prediction root causes surface in one cycle summary line.
- CLI and heartbeat surfaces expose the same diagnostic reason semantics.
- Regression tests lock operator-facing diagnostic outputs.
</verification>

<success_criteria>
- MON-06 is satisfied via concise root-cause diagnostics in monitor cycle surfaces.
- MON-04 semantics remain consistent with explicit no-prediction diagnostics.
- Operator troubleshooting no longer depends on traceback inspection for missing-model/no-prediction paths.
</success_criteria>

<output>
After completion, create `.planning/phases/18-monitoring-cycle-semantics-and-operator-diagnostics/18-03-SUMMARY.md`
</output>
