---
phase: 18-monitoring-cycle-semantics-and-operator-diagnostics
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/autonomous/db.py
  - src/bitbat/cli.py
  - tests/autonomous/test_db.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MON-05
must_haves:
  truths:
    - `bitbat monitor status` reports total, unrealized, and realized prediction counts for the active runtime pair.
    - Count semantics are sourced from runtime DB state, not inferred from snapshots alone.
    - Status output remains coherent when snapshots are absent but prediction history exists.
  artifacts:
    - path: src/bitbat/autonomous/db.py
      provides: Pair-scoped prediction count helper(s) for total/unrealized/realized states
    - path: src/bitbat/cli.py
      provides: Monitor status output with explicit prediction counts
    - path: tests/autonomous/test_db.py
      provides: DB-level regression coverage for prediction count semantics
    - path: tests/test_cli.py
      provides: CLI status regression coverage for count output
  key_links:
    - from: src/bitbat/autonomous/db.py
      to: src/bitbat/cli.py
      via: Monitor status command uses one canonical DB helper for pair-scoped counts
      pattern: total_predictions|unrealized|realized|freq|horizon
    - from: src/bitbat/cli.py
      to: tests/test_cli.py
      via: Status output count fields are test-locked for active pair semantics
      pattern: monitor status|total|unrealized|realized
---

<objective>
Make monitor status counts operationally explicit for active runtime pairs.

Purpose: Fulfill MON-05 by exposing total/unrealized/realized prediction counts from monitor status.
Output: DB-backed count helpers and status output updates that remove ambiguity from pending vs realized monitor state.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/models.py
@src/bitbat/cli.py
@tests/autonomous/test_db.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pair-scoped prediction count helper(s) to autonomous DB layer</name>
  <files>src/bitbat/autonomous/db.py, tests/autonomous/test_db.py</files>
  <action>Introduce a deterministic DB helper that returns total/unrealized/realized counts for a given `freq/horizon` pair using `prediction_outcomes` rows. Keep helper API simple for monitor status consumption and add focused DB unit coverage.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -q -k "count or unrealized or realized or monitor_status"</automated>
  </verify>
  <done>DB layer provides a canonical count payload for active pair monitoring status.</done>
</task>

<task type="auto">
  <name>Task 2: Update monitor status CLI to print total/unrealized/realized counts</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Refactor `monitor status` to display explicit total, unrealized, and realized counts from DB helper output while preserving existing snapshot/retraining diagnostics. Ensure output remains meaningful when latest snapshot is missing.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "monitor_status and (total or unrealized or realized or snapshot)"</automated>
  </verify>
  <done>Operators can read pair-specific prediction lifecycle counts directly from one status command.</done>
</task>

<task type="auto">
  <name>Task 3: Add MON-05 regression tests for active-pair count semantics</name>
  <files>tests/test_cli.py, tests/autonomous/test_db.py</files>
  <action>Add/extend regressions that seed mixed realized/unrealized rows and assert monitor status surfaces accurate counts for the selected runtime pair, including scenarios with zero snapshots but non-zero prediction totals.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py tests/test_cli.py -q -k "monitor status and (count or unrealized or realized)"</automated>
  </verify>
  <done>Automated tests fail if status count semantics regress to ambiguous pending-only output.</done>
</task>

</tasks>

<verification>
- Status command prints total, unrealized, and realized counts for active pair.
- Count semantics come from DB rows for chosen freq/horizon.
- Regressions validate mixed and zero-snapshot scenarios.
</verification>

<success_criteria>
- MON-05 is satisfied through explicit pair-scoped lifecycle counts in monitor status.
- Status output remains useful even when snapshots are absent.
- DB/CLI tests lock lifecycle count behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/18-monitoring-cycle-semantics-and-operator-diagnostics/18-02-SUMMARY.md`
</output>
