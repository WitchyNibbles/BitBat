---
phase: 19-regression-gates-and-runbook-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/monitor-operations-runbook.md
  - docs/README.md
  - docs/usage-guide.md
  - docs/monitoring_strategy.md
  - docs/testing-quality.md
  - deployment/bitbat-monitor.service
  - tests/test_monitor_runbook_contract.py
autonomous: true
requirements:
  - QUAL-07
  - QUAL-08
  - QUAL-09
must_haves:
  truths:
    - Operator-facing runbook documents supported monitor config wiring via `--config` and `BITBAT_CONFIG` for manual and service modes.
    - Runbook guidance explains startup guardrail failures, cycle-state semantics, and schema-remediation diagnostics in one operational flow.
    - Documentation contract tests fail if supported config wiring or monitor diagnostic guidance drifts.
  artifacts:
    - path: docs/monitor-operations-runbook.md
      provides: Canonical monitor operations runbook for startup, diagnostics, and release verification workflow
    - path: deployment/bitbat-monitor.service
      provides: Service template aligned with documented config wiring expectations
    - path: tests/test_monitor_runbook_contract.py
      provides: Automated contract checks for runbook/config-wiring guidance
  key_links:
    - from: deployment/bitbat-monitor.service
      to: docs/monitor-operations-runbook.md
      via: Service startup command and BITBAT_CONFIG wiring are documented and kept in sync
      pattern: ExecStart|BITBAT_CONFIG|--config
    - from: docs/monitor-operations-runbook.md
      to: tests/test_monitor_runbook_contract.py
      via: Runbook startup/diagnostic requirements are encoded in automated documentation contract assertions
      pattern: BITBAT_CONFIG|monitor run-once|monitor status|cycle diagnostic|schema remediation
---

<objective>
Harden monitor operations documentation and config-wiring contracts for release usage.

Purpose: Ensure v1.3 monitor guardrails are operable by documenting supported config wiring and diagnostic interpretation, with regression checks that prevent drift.
Output: Runbook/docs/service-template alignment plus automated documentation contract tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/README.md
@docs/usage-guide.md
@docs/monitoring_strategy.md
@docs/testing-quality.md
@deployment/bitbat-monitor.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish monitor operations runbook with explicit config-wiring and diagnostic workflow</name>
  <files>docs/monitor-operations-runbook.md, docs/usage-guide.md, docs/README.md</files>
  <action>Create a dedicated monitor runbook that documents supported config wiring (`--config` and `BITBAT_CONFIG`) for manual and service execution, startup-guardrail triage, cycle-state/status interpretation, and release verification commands. Link it from docs hub and usage guide so operators can find it quickly.</action>
  <verify>
    <automated>rg -n "BITBAT_CONFIG|--config|monitor run-once|monitor status|cycle diagnostic|make test-release" docs/monitor-operations-runbook.md docs/usage-guide.md docs/README.md</automated>
  </verify>
  <done>Operators have one discoverable runbook that covers supported config wiring and monitor diagnostic interpretation end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Align deployment monitor service template with documented config contract</name>
  <files>deployment/bitbat-monitor.service, docs/monitor-operations-runbook.md, docs/monitoring_strategy.md</files>
  <action>Update deployment service guidance/template so BITBAT_CONFIG-backed startup is explicit and consistent with the runbook. Ensure monitoring strategy docs reference the same supported monitor startup contract to avoid split-brain operational guidance.</action>
  <verify>
    <automated>rg -n "BITBAT_CONFIG|ExecStart|run_monitoring_agent.py|--config" deployment/bitbat-monitor.service docs/monitor-operations-runbook.md docs/monitoring_strategy.md</automated>
  </verify>
  <done>Service template and monitoring docs use one consistent config wiring contract for monitor startup.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated documentation contract checks for monitor runbook wiring</name>
  <files>tests/test_monitor_runbook_contract.py, docs/monitor-operations-runbook.md, deployment/bitbat-monitor.service, docs/testing-quality.md</files>
  <action>Add focused tests that assert required runbook/service wiring strings and diagnostic guidance remain present (including `--config`, `BITBAT_CONFIG`, startup guardrail remediation, cycle/status semantics, and schema remediation references). Update testing-quality docs to include this contract suite in monitor guardrails.</action>
  <verify>
    <automated>poetry run pytest tests/test_monitor_runbook_contract.py -q</automated>
  </verify>
  <done>Automated docs contract tests fail when monitor runbook or service wiring guidance regresses.</done>
</task>

</tasks>

<verification>
- Runbook documents supported config wiring and monitor diagnostic interpretation.
- Deployment service template matches documented startup contract.
- Automated docs contract tests enforce runbook/service wiring stability.
</verification>

<success_criteria>
- Operators can reliably configure monitor startup using `--config` or `BITBAT_CONFIG` from documented workflows.
- Diagnostic guidance for startup mismatch, cycle semantics, and schema remediation is centralized and test-locked.
- Phase 19 operational documentation closes release-readiness gaps for v1.3 monitor alignment behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/19-regression-gates-and-runbook-hardening/19-02-SUMMARY.md`
</output>
