---
phase: 19-regression-gates-and-runbook-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
  - tests/autonomous/test_phase8_d1_monitor_schema_complete.py
  - tests/gui/test_phase8_release_verification_complete.py
  - tests/autonomous/test_agent_integration.py
  - tests/test_cli.py
  - tests/autonomous/test_schema_compat.py
  - tests/test_run_monitoring_agent.py
  - Makefile
autonomous: true
requirements:
  - QUAL-07
  - QUAL-08
  - QUAL-09
must_haves:
  truths:
    - Canonical monitor release gates fail if startup guardrails for runtime/model mismatch are bypassed.
    - Canonical monitor release gates fail if cycle/status semantics regress to ambiguous all-zero behavior without explicit state reasoning.
    - Canonical monitor release gates fail if schema compatibility coverage for performance_snapshots runtime columns regresses.
  artifacts:
    - path: tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
      provides: Dedicated phase-level D1 gate covering startup mismatch, cycle/status semantics, and schema compatibility regressions
    - path: tests/autonomous/test_phase8_d1_monitor_schema_complete.py
      provides: Canonical D1 suite contract updated to include phase 19 gate coverage
    - path: tests/gui/test_phase8_release_verification_complete.py
      provides: Release contract assertions that require phase 19 monitor gate participation
    - path: Makefile
      provides: Canonical `test-release` target wired to phase 19 monitor regression gate
  key_links:
    - from: tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
      to: tests/autonomous/test_agent_integration.py
      via: Phase gate depends on monitor cycle-state and startup guardrail semantics verified by integration tests
      pattern: startup|run_once|prediction_state|realization_state|diagnostic
    - from: tests/autonomous/test_phase19_d1_monitor_alignment_complete.py
      to: tests/test_cli.py
      via: Phase gate requires operator-facing monitor run-once/status semantics to remain explicit
      pattern: monitor run-once|monitor status|cycle diagnostic|unrealized|realized
    - from: Makefile
      to: tests/gui/test_phase8_release_verification_complete.py
      via: Release command and release contract tests enforce the same canonical phase 19 gate membership
      pattern: test-release|phase19|D1_CANONICAL_SUITE
---

<objective>
Lock v1.3 monitor alignment semantics behind canonical regression gates.

Purpose: Fulfill QUAL-07/08/09 by wiring startup mismatch, cycle/status semantics, and schema-contract protections into release-grade automated suites.
Output: Phase-level D1 gate tests plus canonical suite/release wiring that fails deterministically when v1.3 monitor guarantees regress.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@tests/autonomous/test_phase8_d1_monitor_schema_complete.py
@tests/gui/test_phase8_release_verification_complete.py
@tests/autonomous/test_agent_integration.py
@tests/test_cli.py
@tests/autonomous/test_schema_compat.py
@tests/test_run_monitoring_agent.py
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 19 D1 monitor-alignment gate suite</name>
  <files>tests/autonomous/test_phase19_d1_monitor_alignment_complete.py</files>
  <action>Create a dedicated phase-level D1 regression gate that encodes QUAL-07/08/09 invariants and validates required upstream monitor suites/files are present. Gate scenarios should explicitly cover startup runtime/model mismatch protection, explicit cycle/status state semantics, and performance_snapshots schema-compatibility guard coverage.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_phase19_d1_monitor_alignment_complete.py -q</automated>
  </verify>
  <done>Phase 19 has a standalone monitor-alignment gate suite that fails if any QUAL-07/08/09 contract is absent from canonical D1 verification.</done>
</task>

<task type="auto">
  <name>Task 2: Harden monitor startup/cycle/schema regressions consumed by the new phase gate</name>
  <files>tests/autonomous/test_agent_integration.py, tests/test_cli.py, tests/autonomous/test_schema_compat.py, tests/test_run_monitoring_agent.py</files>
  <action>Add or tighten focused regressions so the new phase gate has deterministic behavioral anchors: startup mismatch blocking, explicit run-once/status semantics, schema remediation for monitor-required snapshot columns, and heartbeat diagnostic propagation. Keep test intent aligned to operator-observable outcomes rather than implementation internals.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_agent_integration.py tests/test_cli.py tests/autonomous/test_schema_compat.py tests/test_run_monitoring_agent.py -q -k "monitor or startup or schema or diagnostic or cycle_state or status"</automated>
  </verify>
  <done>Underlying monitor suites contain explicit, stable assertions that enforce the QUAL-07/08/09 contracts consumed by the phase 19 gate.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Phase 19 gate into canonical D1 and release verification contracts</name>
  <files>tests/autonomous/test_phase8_d1_monitor_schema_complete.py, tests/gui/test_phase8_release_verification_complete.py, Makefile</files>
  <action>Update canonical D1 suite membership and release-contract assertions so Phase 19 monitor-alignment gate participation is mandatory, then wire `make test-release` to execute the new gate in the D1 command block.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_phase8_d1_monitor_schema_complete.py tests/gui/test_phase8_release_verification_complete.py -q -k "phase19 or canonical or release or d1"</automated>
  </verify>
  <done>Canonical release verification paths enforce Phase 19 monitor-alignment gating by contract, and `make test-release` executes the new gate.</done>
</task>

</tasks>

<verification>
- Phase-level D1 gate enforces startup mismatch, cycle/status semantics, and schema-contract invariants.
- Canonical D1/release contract tests include the Phase 19 gate as a required suite member.
- `make test-release` includes Phase 19 monitor gate execution in the D1 segment.
</verification>

<success_criteria>
- QUAL-07 is test-locked in release-grade suites.
- QUAL-08 is test-locked against ambiguous monitor cycle/status semantics.
- QUAL-09 is test-locked against performance_snapshots schema-contract regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/19-regression-gates-and-runbook-hardening/19-01-SUMMARY.md`
</output>
