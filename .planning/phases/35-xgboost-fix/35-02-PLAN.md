---
phase: 35-xgboost-fix
plan: 02
type: tdd
wave: 2
depends_on:
  - 35-01
files_modified:
  - src/bitbat/cli/_helpers.py
  - src/bitbat/cli/commands/model.py
  - tests/test_cli.py
  - tests/model/test_phase5_complete.py
autonomous: true
requirements:
  - DEBT-04
---

<objective>
Wire the CLI CV/optimization flow to the classification-aware XGBoost core, emit PR-AUC-aware summaries, and keep existing CLI/reporting coverage green.

Purpose: Phase 35 is only complete once the operator-facing model-selection path stops feeding `r_forward` into the XGBoost classification family and the summary output reflects probability-based evaluation.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing CLI/model integration tests for label-driven XGBoost CV and optimization</name>
  <files>tests/test_cli.py, tests/model/test_phase5_complete.py</files>
  <action>
    Add RED tests that require:
    1. `model cv` loads labels for the XGBoost path and records PR-AUC-aware output.
    2. `model optimize` loads labels for the XGBoost path and no longer assumes `reg:squarederror`.
    3. The summary/artifact surface still exposes enough compatibility fields for existing consumers while adding classification evidence.

    Run:
    `poetry run pytest tests/test_cli.py tests/model/test_phase5_complete.py -x`
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CLI XGBoost classification wiring and summary updates</name>
  <files>src/bitbat/cli/_helpers.py, src/bitbat/cli/commands/model.py</files>
  <action>
    Update the CLI CV/optimization path so the XGBoost family uses labels + probability outputs, while RandomForest keeps its numeric-target path. Emit PR-AUC in summaries and preserve existing artifact expectations where practical.

    Run:
    - `poetry run pytest tests/test_cli.py tests/model/test_phase5_complete.py tests/model/test_train.py tests/model/test_infer.py -x`
    - `poetry run ruff check src/bitbat/cli/_helpers.py src/bitbat/cli/commands/model.py tests/test_cli.py tests/model/test_phase5_complete.py`
  </action>
</task>

</tasks>

<verification>
poetry run pytest tests/model/test_train.py tests/model/test_infer.py tests/model/test_walk_forward.py tests/model/test_optimize.py tests/model/test_phase5_complete.py tests/test_cli.py -x
poetry run ruff check src/bitbat/model src/bitbat/cli/_helpers.py src/bitbat/cli/commands/model.py tests/model tests/test_cli.py
</verification>

<output>
After completion, create `.planning/phases/35-xgboost-fix/35-02-SUMMARY.md`
</output>
