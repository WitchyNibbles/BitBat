---
phase: 35-xgboost-fix
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/bitbat/model/walk_forward.py
  - src/bitbat/model/optimize.py
  - src/bitbat/model/evaluate.py
  - tests/model/test_walk_forward.py
  - tests/model/test_optimize.py
autonomous: true
requirements:
  - DEBT-04
---

<objective>
Make the XGBoost walk-forward and optimization core classification-aware when the target is the direction label, while preserving regression fallback for existing numeric-target fixtures.

Purpose: the saved training artifact already uses `multi:softprob`, but the runtime CV/optimizer stack still trains XGBoost with `reg:squarederror`, so the model-selection path is inconsistent with the actual model family.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing model-core tests for classification-mode walk-forward and optimization</name>
  <files>tests/model/test_walk_forward.py, tests/model/test_optimize.py</files>
  <action>
    Add RED tests that require:
    1. `WalkForwardValidator` uses `multi:softprob` when given direction labels.
    2. Classification-mode walk-forward surfaces valid 3-class probability outputs and PR-AUC in its summaries.
    3. `HyperparamOptimizer` uses a classification objective when optimizing over labels and tracks a PR-AUC-based score or equivalent classification metric.

    Run:
    `poetry run pytest tests/model/test_walk_forward.py tests/model/test_optimize.py -x`
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement adaptive classification/regression behavior in walk-forward and optimize</name>
  <files>src/bitbat/model/walk_forward.py, src/bitbat/model/optimize.py, src/bitbat/model/evaluate.py</files>
  <action>
    Implement the smallest coherent change that:
    1. Uses `multi:softprob` for label targets.
    2. Preserves regression-mode behavior for numeric targets.
    3. Adds PR-AUC/probability metrics for the classification path without breaking existing regression summaries.

    Run:
    - `poetry run pytest tests/model/test_walk_forward.py tests/model/test_optimize.py -x`
    - `poetry run ruff check src/bitbat/model/walk_forward.py src/bitbat/model/optimize.py src/bitbat/model/evaluate.py`
  </action>
</task>

</tasks>

<verification>
poetry run pytest tests/model/test_walk_forward.py tests/model/test_optimize.py tests/model/test_train.py tests/model/test_infer.py -x
poetry run ruff check src/bitbat/model/walk_forward.py src/bitbat/model/optimize.py src/bitbat/model/evaluate.py
</verification>

<output>
After completion, create `.planning/phases/35-xgboost-fix/35-01-SUMMARY.md`
</output>
