---
phase: 36-live-recovery-evidence-closure
plan: 02
type: verification
wave: 2
depends_on:
  - 36-01
files_modified:
  - .planning/phases/36-live-recovery-evidence-closure/36-02-SUMMARY.md
  - .planning/phases/36-live-recovery-evidence-closure/36-VERIFICATION.md
  - .planning/phases/30-fix-and-reset/30-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - FIXR-03
---

<objective>
Execute the real fresh-state recovery flow in a sandbox, capture the passing diagnosis evidence, and formally replace Phase 30's deferred FIXR-03 checkpoint with saved passed verification artifacts.

Purpose: the audit gap is not theoretical anymore once there is a recorded reset + retrain + realized-accuracy run against fresh data with saved evidence.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Run the sandbox recovery flow end-to-end and capture evidence</name>
  <files>.planning/phases/36-live-recovery-evidence-closure/36-02-SUMMARY.md, .planning/phases/36-live-recovery-evidence-closure/36-VERIFICATION.md, .planning/phases/30-fix-and-reset/30-VERIFICATION.md</files>
  <action>
    Execute:
    1. create a sandbox config for the `1h_1h` recovery pair;
    2. run `poetry run bitbat --config <sandbox> system reset --yes`;
    3. run `poetry run python scripts/build_recovery_evidence.py stage --config <sandbox> --source-dataset data/features/1h_1h/dataset.parquet --evaluation-rows 300`;
    4. run `poetry run bitbat --config <sandbox> model train --freq 1h --horizon 1h`;
    5. run `poetry run python scripts/build_recovery_evidence.py realize --config <sandbox>`;
    6. run `BITBAT_CONFIG=<sandbox> poetry run pytest tests/diagnosis/test_pipeline_stage_trace.py -v`.

    Record the realized accuracy, class counts, and artifact paths in both the Phase 36 verification report and the superseded Phase 30 verification report.
  </action>
</task>

<task type="auto">
  <name>Task 2: Update roadmap/state metadata to mark Phase 36 complete</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    Mark Phase 36 planned/executed, update progress counters, and note that FIXR-03 is now closed by saved Phase 36 evidence rather than deferred human action.
  </action>
</task>

</tasks>

<verification>
BITBAT_CONFIG=<sandbox-config> poetry run pytest tests/diagnosis/test_pipeline_stage_trace.py -v
poetry run pytest tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/test_recovery_runbook_contract.py -x
</verification>

<output>
After completion, create `.planning/phases/36-live-recovery-evidence-closure/36-02-SUMMARY.md` and `.planning/phases/36-live-recovery-evidence-closure/36-VERIFICATION.md`
</output>
