---
phase: 36-live-recovery-evidence-closure
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/recovery_evidence.py
  - scripts/build_recovery_evidence.py
  - src/bitbat/model/persist.py
  - tests/autonomous/test_recovery_evidence.py
  - tests/model/test_persist.py
  - tests/diagnosis/test_pipeline_stage_trace.py
  - tests/test_recovery_runbook_contract.py
  - docs/testing-quality.md
  - docs/usage-guide.md
autonomous: true
requirements:
  - FIXR-03
---

<objective>
Build a deterministic recovery-evidence harness that can operate against a sandboxed config, make the diagnosis assertions runtime-config-aware, and fix the config/model-path mismatch that currently prevents a real reset + retrain recovery flow.

Purpose: Phase 30 already proved the code fixes, but the saved evidence remained deferred because no fresh runtime proof existed and the sandbox retrain path is not fully config-consistent today.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tests for recovery evidence generation, configured model persistence, and recovery docs contracts</name>
  <files>tests/autonomous/test_recovery_evidence.py, tests/model/test_persist.py, tests/test_recovery_runbook_contract.py</files>
  <action>
    Add failing tests that require:
    1. a recovery-evidence builder can turn a held-out dataset into fresh realized DB outcomes with accuracy above 33%;
    2. model persistence defaults honor configured `models_dir`;
    3. repo docs describe the reproducible recovery flow (`system reset` -> stage -> model train -> realize -> diagnosis test).

    Run:
    `poetry run pytest tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/test_recovery_runbook_contract.py -x`
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement recovery evidence module, script, config-aware diagnosis resolution, and docs updates</name>
  <files>src/bitbat/autonomous/recovery_evidence.py, scripts/build_recovery_evidence.py, src/bitbat/model/persist.py, tests/diagnosis/test_pipeline_stage_trace.py, docs/testing-quality.md, docs/usage-guide.md</files>
  <action>
    Implement the smallest coherent change that:
    1. stages a train/eval split for the sandbox recovery flow;
    2. generates fresh prediction_outcomes from a held-out split and realizes them via the validator;
    3. writes a recovery evidence summary;
    4. makes diagnosis tests resolve model/DB targets from runtime config;
    5. makes model persistence follow configured `models_dir` by default.

    Run:
    - `poetry run pytest tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/diagnosis/test_pipeline_stage_trace.py tests/test_recovery_runbook_contract.py -x`
    - `poetry run ruff check src/bitbat/autonomous/recovery_evidence.py scripts/build_recovery_evidence.py src/bitbat/model/persist.py tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/diagnosis/test_pipeline_stage_trace.py tests/test_recovery_runbook_contract.py`
  </action>
</task>

</tasks>

<verification>
poetry run pytest tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/diagnosis/test_pipeline_stage_trace.py tests/test_recovery_runbook_contract.py -x
poetry run ruff check src/bitbat/autonomous/recovery_evidence.py scripts/build_recovery_evidence.py src/bitbat/model/persist.py tests/autonomous/test_recovery_evidence.py tests/model/test_persist.py tests/diagnosis/test_pipeline_stage_trace.py tests/test_recovery_runbook_contract.py
</verification>

<output>
After completion, create `.planning/phases/36-live-recovery-evidence-closure/36-01-SUMMARY.md`
</output>
