---
phase: 09-timeline-readability-overlay-clarity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/gui/timeline.py
  - tests/gui/test_timeline.py
autonomous: true
requirements:
  - TIM-03
must_haves:
  truths:
    - Default timeline view is readable at a glance even with dense event history.
    - Direction and status remain clear from visual encoding, while detailed confidence stays in hover context.
    - Timeline rendering avoids per-event trace explosion that obscures interpretation.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Readability-first timeline rendering contract and semantic marker grouping
    - path: tests/gui/test_timeline.py
      provides: Unit regression coverage for readability and marker semantics
  key_links:
    - from: src/bitbat/gui/timeline.py
      to: tests/gui/test_timeline.py
      via: Marker grouping, hover semantics, and trace count/readability invariants are test-locked
      pattern: marker|confidence|status|readability
---

<objective>
Refactor primary timeline figure rendering for readability without regressing status/direction semantics.

Purpose: Close TIM-03 readability gap by reducing visual noise in default timeline interpretation.
Output: Readability-first marker rendering contract plus deterministic unit coverage.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-timeline-readability-overlay-clarity/09-CONTEXT.md
@.planning/phases/09-timeline-readability-overlay-clarity/09-RESEARCH.md
@src/bitbat/gui/timeline.py
@tests/gui/test_timeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace per-row marker trace creation with semantic marker grouping</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Refactor timeline marker rendering to group points by direction/status semantics instead of creating one Plotly trace per event row. Preserve existing hover data (confidence, predicted/actual return, status label) while materially reducing trace count in dense windows.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "timeline and (status_marker_semantics or confidence_exact_percent or readability)"</automated>
  </verify>
  <done>Default timeline marker layer remains semantically correct and demonstrably less cluttered under dense datasets.</done>
</task>

<task type="auto">
  <name>Task 2: Apply readability-first visual hierarchy in the primary timeline figure</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Adjust default timeline visual hierarchy so the chart communicates event outcomes quickly: clear legend semantics, balanced marker opacity/size, and layout settings that reduce visual competition between price context and prediction events.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "timeline and (figure or marker or status)"</automated>
  </verify>
  <done>Primary timeline has an intentional readability-first composition and keeps direction/status semantics explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Add TIM-03 readability regressions for dense-history rendering</name>
  <files>tests/gui/test_timeline.py</files>
  <action>Add deterministic tests asserting readability proxy invariants for dense history fixtures (for example bounded marker trace fan-out, preserved confidence hover semantics, and clear status labeling) so future rendering changes cannot reintroduce unreadable composition.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "timeline and (readability or dense or marker)"</automated>
  </verify>
  <done>Unit tests fail if timeline readability regresses while still passing semantic status/direction contracts.</done>
</task>

</tasks>

<verification>
- Marker rendering semantics remain correct for pending/realized state and direction.
- Dense datasets remain interpretable in default view.
- Confidence remains detailed in hover (including explicit `n/a` handling) without cluttering base chart.
</verification>

<success_criteria>
- TIM-03 default readability gap is materially reduced in timeline base rendering.
- Trace-level complexity is reduced through grouped semantics.
- Regression coverage guards readability invariants in timeline unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/09-timeline-readability-overlay-clarity/09-01-SUMMARY.md`
</output>
