---
phase: 13-data-and-label-contract-upgrade
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/labeling/returns.py
  - src/bitbat/labeling/targets.py
  - src/bitbat/labeling/__init__.py
  - src/bitbat/dataset/build.py
  - src/bitbat/contracts.py
  - tests/labeling/test_returns.py
  - tests/labeling/test_targets.py
  - tests/dataset/test_build_xy.py
autonomous: true
requirements:
  - DATA-02
must_haves:
  truths:
    - Forward-return and direction labels are derived from one shared horizon definition.
    - Label outputs remain deterministic for irregular timestamps and missing-horizon rows.
    - Baseline dataset output exposes return targets with direction labels that cannot drift by implementation path.
  artifacts:
    - path: src/bitbat/labeling/returns.py
      provides: Canonical horizon-aware forward-return labeling utilities
    - path: src/bitbat/labeling/targets.py
      provides: Direction-label derivation from canonical return targets
    - path: src/bitbat/dataset/build.py
      provides: Integrated return plus direction target generation in dataset build flow
  key_links:
    - from: src/bitbat/labeling/returns.py
      to: src/bitbat/labeling/targets.py
      via: Direction labels are derived from canonical forward-return outputs
      pattern: forward_return|horizon|direction|target
    - from: src/bitbat/dataset/build.py
      to: tests/labeling/test_targets.py
      via: Dataset-integrated labels and standalone label helpers validate the same rules
      pattern: tau|direction|label|consistency
---

<objective>
Lock return-first target and direction-label consistency under one horizon contract.

Purpose: Fulfill DATA-02 so return and direction labels are generated from the same canonical logic.
Output: Shared label APIs, dataset integration, and consistency regression tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-data-and-label-contract-upgrade/13-CONTEXT.md
@.planning/phases/13-data-and-label-contract-upgrade/13-01-PLAN.md
@src/bitbat/labeling/returns.py
@src/bitbat/labeling/targets.py
@src/bitbat/dataset/build.py
@src/bitbat/contracts.py
@tests/labeling/test_returns.py
@tests/labeling/test_targets.py
@tests/dataset/test_build_xy.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a canonical horizon and return-target labeling interface</name>
  <files>src/bitbat/labeling/returns.py, src/bitbat/labeling/__init__.py, tests/labeling/test_returns.py</files>
  <action>Refactor return labeling helpers so horizon parsing and forward-return alignment are centralized and reusable by all downstream label paths. Add tests for horizon parsing, gap behavior, and deterministic return calculations across irregular timestamps.</action>
  <verify>
    <automated>poetry run pytest tests/labeling/test_returns.py -q</automated>
  </verify>
  <done>Forward-return labels are generated through one canonical horizon-aware API with deterministic behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Derive direction labels directly from canonical forward returns</name>
  <files>src/bitbat/labeling/targets.py, tests/labeling/test_targets.py</files>
  <action>Update target-label helpers so direction labels are computed from canonical forward returns (and shared thresholds) rather than any parallel/duplicate horizon path. Ensure edge and flat-threshold handling remain explicit and test-covered.</action>
  <verify>
    <automated>poetry run pytest tests/labeling/test_targets.py -q</automated>
  </verify>
  <done>Direction labels and forward returns are guaranteed to share one horizon definition and threshold policy.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate the shared label contract into dataset build and validation</name>
  <files>src/bitbat/dataset/build.py, src/bitbat/contracts.py, tests/dataset/test_build_xy.py</files>
  <action>Wire canonical return and direction-label generation into build_xy outputs and contract validation so default datasets expose consistent targets without schema drift. Add regression tests asserting return/direction alignment at identical timestamps and horizons.</action>
  <verify>
    <automated>poetry run pytest tests/dataset/test_build_xy.py tests/labeling/test_returns.py tests/labeling/test_targets.py -q -k "label or direction or horizon"</automated>
  </verify>
  <done>Dataset output and contracts enforce synchronized return/direction target semantics.</done>
</task>

</tasks>

<verification>
- Return and direction labels are derived from one canonical horizon path.
- Dataset outputs and label helpers stay semantically aligned.
- Labeling edge cases are regression-covered under automated tests.
</verification>

<success_criteria>
- DATA-02 is satisfied via shared return/direction label derivation.
- No parallel label paths can drift on horizon semantics.
- Dataset contracts preserve deterministic label output behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-and-label-contract-upgrade/13-02-SUMMARY.md`
</output>
