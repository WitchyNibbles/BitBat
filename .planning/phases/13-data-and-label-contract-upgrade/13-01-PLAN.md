---
phase: 13-data-and-label-contract-upgrade
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/timealign/asof.py
  - src/bitbat/dataset/build.py
  - src/bitbat/contracts.py
  - tests/timealign/test_asof_join.py
  - tests/dataset/test_build_xy.py
autonomous: true
requirements:
  - DATA-01
must_haves:
  truths:
    - Every training row is built from as-of aligned sources with no future data leakage.
    - Dataset construction rejects or drops rows that violate timestamp ordering guarantees.
    - Leakage guarantees are regression-tested with deterministic fixtures.
  artifacts:
    - path: src/bitbat/timealign/asof.py
      provides: Reusable as-of alignment and anti-leakage guards for feature joins
    - path: src/bitbat/dataset/build.py
      provides: Dataset builder integration that applies as-of alignment before label generation
    - path: tests/timealign/test_asof_join.py
      provides: Deterministic anti-leakage tests for as-of alignment semantics
  key_links:
    - from: src/bitbat/timealign/asof.py
      to: src/bitbat/dataset/build.py
      via: Dataset joins route through shared as-of guard utilities
      pattern: asof|alignment|future|timestamp
    - from: src/bitbat/dataset/build.py
      to: tests/dataset/test_build_xy.py
      via: Dataset tests assert no future timestamps can influence feature rows
      pattern: leakage|asof|contract
---

<objective>
Implement leakage-safe as-of alignment as a hard data contract for Phase 13.

Purpose: Fulfill DATA-01 by guaranteeing that feature rows cannot contain future information.
Output: Shared as-of alignment helpers, dataset-build integration, and leakage guard tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-data-and-label-contract-upgrade/13-CONTEXT.md
@src/bitbat/dataset/build.py
@src/bitbat/contracts.py
@src/bitbat/timealign/calendar.py
@src/bitbat/timealign/purging.py
@tests/dataset/test_build_xy.py
@tests/timealign/test_purging.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared as-of alignment helpers with explicit future-data guards</name>
  <files>src/bitbat/timealign/asof.py, tests/timealign/test_asof_join.py</files>
  <action>Create a dedicated time-alignment helper module that validates monotonic timestamp alignment and prevents future-source values from entering a target bar row. Add focused unit tests for boundary-inclusive as-of behavior, strict future-row rejection, and irregular-gap handling.</action>
  <verify>
    <automated>poetry run pytest tests/timealign/test_asof_join.py -q</automated>
  </verify>
  <done>As-of helper behavior is deterministic and future-data leakage checks are test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Route dataset feature joins through the as-of guard path</name>
  <files>src/bitbat/dataset/build.py</files>
  <action>Refactor dataset assembly so sentiment/macro/on-chain joins pass through the shared as-of helper, preserving row order and eliminating future joins. Keep current baseline output schema stable while enforcing hard timestamp ordering before label generation.</action>
  <verify>
    <automated>poetry run pytest tests/dataset/test_build_xy.py -q -k "asof or leakage or build_xy"</automated>
  </verify>
  <done>Dataset builder only emits rows that satisfy as-of alignment guarantees across joined feature sources.</done>
</task>

<task type="auto">
  <name>Task 3: Harden feature-contract checks around timestamp integrity</name>
  <files>src/bitbat/contracts.py, tests/dataset/test_build_xy.py</files>
  <action>Extend feature-contract validation to assert strict timestamp normalization and ordering expectations needed by leakage-safe training data. Add contract-focused dataset tests that fail when ordering or alignment assumptions are violated.</action>
  <verify>
    <automated>poetry run pytest tests/dataset/test_build_xy.py tests/contracts/test_contracts.py -q -k "timestamp or contract or leakage"</automated>
  </verify>
  <done>Contract validation enforces timestamp integrity as a first-class anti-leakage invariant.</done>
</task>

</tasks>

<verification>
- As-of alignment guards block future-data leakage across feature joins.
- Dataset rows maintain strict timestamp contract before labels are computed.
- Anti-leakage behavior is enforced by deterministic automated tests.
</verification>

<success_criteria>
- DATA-01 is satisfied with reusable, test-backed as-of alignment guards.
- Feature assembly cannot silently leak future information.
- Dataset contract rejects timestamp-integrity violations.
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-and-label-contract-upgrade/13-01-SUMMARY.md`
</output>
