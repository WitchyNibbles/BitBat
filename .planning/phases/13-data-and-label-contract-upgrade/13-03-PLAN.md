---
phase: 13-data-and-label-contract-upgrade
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/bitbat/labeling/triple_barrier.py
  - src/bitbat/labeling/__init__.py
  - src/bitbat/dataset/build.py
  - src/bitbat/contracts.py
  - src/bitbat/cli.py
  - tests/labeling/test_triple_barrier.py
  - tests/dataset/test_build_xy.py
  - tests/test_cli.py
autonomous: true
requirements:
  - LABL-01
must_haves:
  truths:
    - Triple-barrier labels can be generated in an optional mode without breaking default dataset output.
    - Baseline return/direction datasets remain stable when triple-barrier mode is not enabled.
    - Optional barrier-label outputs are deterministic and test-verifiable for stop, take-profit, and timeout paths.
  artifacts:
    - path: src/bitbat/labeling/triple_barrier.py
      provides: Triple-barrier labeling implementation with deterministic event outcomes
    - path: src/bitbat/dataset/build.py
      provides: Optional dataset mode wiring for triple-barrier labels
    - path: src/bitbat/cli.py
      provides: Feature-build interface to enable barrier-label mode without breaking defaults
  key_links:
    - from: src/bitbat/labeling/triple_barrier.py
      to: src/bitbat/dataset/build.py
      via: Dataset build chooses barrier-label mode through explicit option wiring
      pattern: triple|barrier|mode|label
    - from: src/bitbat/cli.py
      to: src/bitbat/dataset/build.py
      via: CLI option toggles barrier-label mode in feature build flow
      pattern: features build|label mode|triple barrier
---

<objective>
Add optional triple-barrier labels as a trading-aligned dataset mode.

Purpose: Fulfill LABL-01 while preserving default return/direction dataset behavior.
Output: Triple-barrier label module, optional dataset/CLI wiring, and mode-compatibility tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-data-and-label-contract-upgrade/13-CONTEXT.md
@.planning/phases/13-data-and-label-contract-upgrade/13-02-PLAN.md
@src/bitbat/labeling/returns.py
@src/bitbat/labeling/targets.py
@src/bitbat/dataset/build.py
@src/bitbat/contracts.py
@src/bitbat/cli.py
@tests/dataset/test_build_xy.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic triple-barrier labeling utilities</name>
  <files>src/bitbat/labeling/triple_barrier.py, src/bitbat/labeling/__init__.py, tests/labeling/test_triple_barrier.py</files>
  <action>Create a triple-barrier labeling module supporting take-profit, stop-loss, and timeout outcomes over configured horizons. Expose the API through the labeling package and add deterministic tests for each barrier path and edge-case ordering behavior.</action>
  <verify>
    <automated>poetry run pytest tests/labeling/test_triple_barrier.py -q</automated>
  </verify>
  <done>Triple-barrier event labels are deterministic, reusable, and independently test-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Add optional triple-barrier mode to dataset build path</name>
  <files>src/bitbat/dataset/build.py, src/bitbat/contracts.py, tests/dataset/test_build_xy.py</files>
  <action>Extend build_xy with an explicit optional label mode for triple-barrier outputs while preserving default return/direction behavior. Update feature-contract validation so optional barrier label columns are accepted only when barrier mode is enabled and baseline mode remains backward-compatible.</action>
  <verify>
    <automated>poetry run pytest tests/dataset/test_build_xy.py -q -k "triple or barrier or label_mode or compatibility"</automated>
  </verify>
  <done>Dataset builder supports optional barrier labels without breaking baseline output contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI control for optional barrier labeling mode</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Add features-build CLI/config wiring that enables triple-barrier mode through explicit options, with baseline defaults unchanged. Add CLI tests proving mode selection and default compatibility behavior.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "features and (label or triple or barrier)"</automated>
  </verify>
  <done>Operators can enable triple-barrier labels explicitly via CLI while default behavior remains stable.</done>
</task>

</tasks>

<verification>
- Triple-barrier labels are available as an optional mode and deterministic under tests.
- Baseline dataset behavior remains stable when barrier mode is off.
- CLI path can toggle label mode without introducing default-path regressions.
</verification>

<success_criteria>
- LABL-01 is satisfied with optional triple-barrier dataset mode.
- Default return/direction dataset path remains backward-compatible.
- Triple-barrier behavior is regression-locked via labeling/dataset/CLI tests.
</success_criteria>

<output>
After completion, create `.planning/phases/13-data-and-label-contract-upgrade/13-03-SUMMARY.md`
</output>
