---
phase: 34-db-unification
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/db.py
  - src/bitbat/autonomous/models.py
  - tests/autonomous/test_db.py
autonomous: true
requirements:
  - DEBT-03

must_haves:
  truths:
    - "AutonomousDB is the single runtime DB access layer chosen for Phase 34"
    - "AutonomousDB exposes compatibility-aware read methods needed by /system API routes and Streamlit helpers"
    - "Transient SQLite lock handling (brief retry + explicit circuit-open failure) lives in the DB layer, not in callers"
    - "AutonomousDB exposes atomic write helpers for retraining completion/failure and model promotion bookkeeping"
  artifacts:
    - path: "src/bitbat/autonomous/db.py"
      provides: "Unified read facade, retry/circuit-breaker policy, and atomic retraining write helpers"
      exports:
        - "AutonomousDB"
        - "MonitorDatabaseError"
    - path: "tests/autonomous/test_db.py"
      provides: "Behavioral coverage for compatibility-aware reads, lock retry policy, and atomic retraining helpers"
  key_links:
    - from: "tests/autonomous/test_db.py"
      to: "src/bitbat/autonomous/db.py"
      via: "Read/query helpers and transactional helper coverage"
      pattern: "AutonomousDB"
---

<objective>
Establish the single DB access layer for Phase 34 by extending `AutonomousDB` with the compatibility-aware read methods and atomic write helpers that all remaining runtime consumers need.

Purpose: before migrating API and Streamlit modules, the DB layer must already know how to read legacy-compatible rows, handle transient lock contention consistently, and package retraining/model-activation writes into small atomic transactions.

Output: updated `src/bitbat/autonomous/db.py` (and model support only if truly required) plus focused DB-layer tests proving the new facade and transaction helpers work.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-db-unification/34-CONTEXT.md
@.planning/phases/34-db-unification/34-RESEARCH.md

@src/bitbat/autonomous/db.py
@src/bitbat/autonomous/models.py
@tests/autonomous/test_db.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing DB-layer tests for unified reads and transient lock handling</name>
  <files>tests/autonomous/test_db.py</files>
  <behavior>
    - add tests for compatibility-aware system-log/retraining-event/performance-snapshot reads through `AutonomousDB`
    - add tests for latest-prediction/timeline-style reads needed by GUI helpers without importing sqlite3 in runtime code
    - add tests proving transient lock failures retry briefly and then surface an explicit circuit-open style failure
  </behavior>
  <action>
    Extend `tests/autonomous/test_db.py` with focused failing tests that lock the Phase 34 foundation contract.

    Required coverage:
    1. A DB-layer read method returns system logs ordered by the correct timestamp column when only `created_at` or `timestamp` is present.
    2. Read methods for retraining events and performance snapshots tolerate legacy-compatible column shapes already covered by the schema-compat path.
    3. A DB-layer read method returns latest prediction/timeline rows for a requested `freq`/`horizon` pair using the same fallback semantics currently implemented in GUI helpers.
    4. A transient lock path is retried briefly and, after the retry budget is exhausted, raises a structured DB error whose operator-facing detail can be rendered as “short message + hint line” with an explicit circuit-open state.

    Run:
    `poetry run pytest tests/autonomous/test_db.py -x`

    The new tests must fail first for the missing methods/behavior.
  </action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -x</automated>
  </verify>
  <done>New DB-layer tests exist and fail for missing read facade / retry-circuit-breaker behavior</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement unified read facade and transient lock policy in AutonomousDB</name>
  <files>src/bitbat/autonomous/db.py, src/bitbat/autonomous/models.py</files>
  <behavior>
    - `AutonomousDB` exposes compatibility-aware read/query methods for system routes and GUI helpers
    - transient lock handling is centralized in the DB layer with brief retry and explicit circuit-open failure signaling
    - runtime callers no longer need to know about legacy timestamp/column fallback details
  </behavior>
  <action>
    Implement the smallest coherent DB-layer API needed by the migration plans.

    Constraints:
    1. Keep SQLAlchemy as the only runtime DB approach. Using SQLAlchemy inspection or `text()` inside `AutonomousDB` is acceptable; raw `sqlite3` runtime access is not.
    2. Keep compatibility-aware read logic in `AutonomousDB`, not in future callers.
    3. Implement brief retry for transient lock failures plus a circuit-open style structured failure. The message should support the Phase 34 context: short user-facing detail, one hint line, and explicit circuit-open wording when relevant.
    4. Do not change the on-disk schema for existing `autonomous.db` files.

    Run:
    - `poetry run pytest tests/autonomous/test_db.py -x`
    - `poetry run ruff check src/bitbat/autonomous/db.py src/bitbat/autonomous/models.py`
  </action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -x && poetry run ruff check src/bitbat/autonomous/db.py src/bitbat/autonomous/models.py</automated>
  </verify>
  <done>DB-layer read facade and transient lock policy are implemented and the DB test suite is green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add atomic retraining/model-promotion helpers to the DB layer</name>
  <files>src/bitbat/autonomous/db.py, tests/autonomous/test_db.py</files>
  <behavior>
    - DB-layer helpers bundle retraining success/failure bookkeeping into small atomic transactions
    - model activation and retraining-event completion can be committed together
    - failure-path event updates remain atomic and do not partially persist success state
  </behavior>
  <action>
    Add DB-layer helper methods that future runtime callers can use to satisfy the Phase 34 all-or-nothing decision without wrapping long-running subprocess work in one giant transaction.

    Required shape:
    1. One helper for successful retraining/promotion bookkeeping (including model activation/event completion in one DB transaction).
    2. One helper for failure bookkeeping.
    3. Tests proving the helper updates either all required rows or none of them.

    Run:
    `poetry run pytest tests/autonomous/test_db.py -x`
  </action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_db.py -x</automated>
  </verify>
  <done>Atomic DB-layer helpers exist for retraining success/failure bookkeeping and tests cover them</done>
</task>

</tasks>

<verification>
poetry run pytest tests/autonomous/test_db.py tests/autonomous/test_schema_compat.py -x
poetry run ruff check src/bitbat/autonomous/db.py src/bitbat/autonomous/models.py
</verification>

<success_criteria>
- `AutonomousDB` is the chosen single runtime DB layer for Phase 34
- compatibility-aware read methods exist for API system and Streamlit consumers
- transient lock handling is centralized in the DB layer with brief retry and explicit circuit-open failure
- atomic DB helpers exist for retraining success/failure bookkeeping
- DB-layer targeted tests pass without schema migration changes
</success_criteria>

<output>
After completion, create `.planning/phases/34-db-unification/34-01-SUMMARY.md`
</output>
