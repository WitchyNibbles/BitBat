---
phase: 34-db-unification
plan: 02
type: auto
wave: 2
depends_on:
  - 34-01
files_modified:
  - src/bitbat/api/routes/system.py
  - tests/api/test_system.py
autonomous: true
requirements:
  - DEBT-03

must_haves:
  truths:
    - "The /system API routes no longer import or call sqlite3 directly"
    - "System logs, retraining events, and performance snapshots are served through AutonomousDB read methods"
    - "Read-only /system DB failures fail the request with a short message plus hint line"
    - "Settings and training routes remain behaviorally unchanged"
  artifacts:
    - path: "src/bitbat/api/routes/system.py"
      provides: "Fail-fast API routes backed by the unified DB layer"
    - path: "tests/api/test_system.py"
      provides: "Direct API regression coverage for /system logs/events/snapshots and DB failure behavior"
  key_links:
    - from: "tests/api/test_system.py"
      to: "src/bitbat/api/routes/system.py"
      via: "FastAPI route coverage for unified DB reads and request failure behavior"
      pattern: "/system/"
---

<objective>
Migrate the `/system` API read routes from raw sqlite access onto the unified `AutonomousDB` layer, and add direct regression coverage for those endpoints.

Purpose: Phase 34 requires the API to use the same DB access path as the rest of the runtime, and the user explicitly chose fail-fast request behavior with short standardized diagnostics.

Output: `src/bitbat/api/routes/system.py` no longer imports `sqlite3`, a new `tests/api/test_system.py` covers the migrated routes, and request-level DB failures are explicit and standardized.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-db-unification/34-CONTEXT.md
@.planning/phases/34-db-unification/34-RESEARCH.md
@.planning/phases/34-db-unification/34-01-SUMMARY.md

@src/bitbat/api/routes/system.py
@src/bitbat/api/schemas.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing API tests for unified /system DB reads</name>
  <files>tests/api/test_system.py</files>
  <behavior>
    - cover `/system/logs`, `/system/retraining-events`, and `/system/performance-snapshots`
    - assert DB-backed routes fail the request when the DB is unavailable or the unified layer raises a DB error
    - assert failure payload text stays short and includes a hint line rather than raw DB internals
  </behavior>
  <action>
    Create `tests/api/test_system.py` with direct FastAPI client coverage for the three DB-backed `/system` read routes.

    Required scenarios:
    1. happy-path payloads for logs/events/snapshots backed by `AutonomousDB`
    2. DB unavailable / unified-layer failure causes request failure (not silent empty payload)
    3. returned error text is short and includes a hint line; low-level exception names are not leaked
    4. existing `/system/settings` and `/system/training/start` semantics are not part of the DB migration and should remain untouched

    Run:
    `poetry run pytest tests/api/test_system.py -x`

    The new tests must fail first before the route migration.
  </action>
  <verify>
    <automated>poetry run pytest tests/api/test_system.py -x</automated>
  </verify>
  <done>`tests/api/test_system.py` exists and fails against the pre-migration route implementation</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate /system DB routes to AutonomousDB and standardize failure handling</name>
  <files>src/bitbat/api/routes/system.py</files>
  <behavior>
    - `/system/logs`, `/system/retraining-events`, and `/system/performance-snapshots` use unified DB-layer methods
    - route failures fail the request instead of returning degraded payloads
    - user-facing error text stays short, includes a hint line, and does not expose low-level DB internals by default
  </behavior>
  <action>
    Refactor `src/bitbat/api/routes/system.py` so only the DB-backed read routes move to the unified layer. Preserve settings/training route behavior.

    Constraints:
    1. Remove runtime `sqlite3` imports and helper functions from this file.
    2. Use the Phase 34 foundation from Plan 01 rather than re-implementing column/legacy fallback logic in the route.
    3. Keep request failures explicit and fail-fast.
    4. Do not import from `bitbat.gui`.

    Run:
    - `poetry run pytest tests/api/test_system.py -x`
    - `poetry run pytest tests/api/test_settings.py tests/api/test_no_gui_import.py -x`
    - `poetry run ruff check src/bitbat/api/routes/system.py tests/api/test_system.py`
  </action>
  <verify>
    <automated>poetry run pytest tests/api/test_system.py tests/api/test_settings.py tests/api/test_no_gui_import.py -x && poetry run ruff check src/bitbat/api/routes/system.py tests/api/test_system.py</automated>
  </verify>
  <done>The DB-backed /system routes use AutonomousDB, fail requests on DB errors, and API regression tests are green</done>
</task>

</tasks>

<verification>
poetry run pytest tests/api/test_system.py tests/api/test_settings.py tests/api/test_metrics.py tests/api/test_predictions.py -x
poetry run ruff check src/bitbat/api/routes/system.py tests/api/test_system.py
</verification>

<success_criteria>
- `/system/logs`, `/system/retraining-events`, and `/system/performance-snapshots` route through AutonomousDB
- `src/bitbat/api/routes/system.py` has no raw `sqlite3` runtime access
- request-level DB failures are explicit and standardized
- settings/training routes remain unchanged
- dedicated API tests exist and pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-db-unification/34-02-SUMMARY.md`
</output>
