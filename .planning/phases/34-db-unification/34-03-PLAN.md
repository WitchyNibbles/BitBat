---
phase: 34-db-unification
plan: 03
type: auto
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - src/bitbat/gui/widgets.py
  - src/bitbat/gui/timeline.py
  - src/bitbat/autonomous/retrainer.py
  - src/bitbat/autonomous/continuous_trainer.py
  - tests/gui/test_widgets.py
  - tests/gui/test_timeline.py
  - tests/autonomous/test_retrainer.py
  - tests/autonomous/test_agent_integration.py
  - tests/contracts/test_db_unification.py
autonomous: true
requirements:
  - DEBT-03

must_haves:
  truths:
    - "Streamlit widgets/timeline no longer import sqlite3 and still preserve current None/[]/empty-frame contracts"
    - "AutoRetrainer and ContinuousTrainer use atomic DB-layer helpers for retraining bookkeeping and model activation"
    - "No raw sqlite3 runtime call sites remain anywhere under src/bitbat/"
    - "Targeted GUI/autonomous/CLI regressions pass after the migration"
  artifacts:
    - path: "src/bitbat/gui/widgets.py"
      provides: "GUI data helpers backed by the unified DB layer"
    - path: "src/bitbat/gui/timeline.py"
      provides: "Timeline queries and filter options backed by the unified DB layer"
    - path: "src/bitbat/autonomous/retrainer.py"
      provides: "Atomic retraining completion/promotion flow"
    - path: "src/bitbat/autonomous/continuous_trainer.py"
      provides: "Atomic continuous retraining completion/promotion flow"
    - path: "tests/contracts/test_db_unification.py"
      provides: "Structural guard for zero raw sqlite3 runtime call sites in src/bitbat"
  key_links:
    - from: "tests/gui/test_widgets.py"
      to: "src/bitbat/gui/widgets.py"
      via: "Return-shape preservation under unified DB access"
      pattern: "get_system_status|get_latest_prediction|get_recent_events"
    - from: "tests/gui/test_timeline.py"
      to: "src/bitbat/gui/timeline.py"
      via: "Timeline query behavior preserved without raw sqlite access"
      pattern: "get_timeline_data|list_timeline_filter_options"
    - from: "tests/contracts/test_db_unification.py"
      to: "src/bitbat/"
      via: "Structural no-sqlite3 runtime guard"
      pattern: "sqlite3"
---

<objective>
Finish Phase 34 by migrating the remaining Streamlit DB helpers to the unified layer, refactoring retraining flows to use atomic DB transactions, and adding the structural gate that proves raw runtime `sqlite3` access is gone from `src/bitbat/`.

Purpose: this plan closes both halves of the phase goal — zero remaining split-access runtime call sites and one consistent connection/transaction lifecycle across the remaining DB-heavy autonomous flows.

Output: GUI DB helpers backed by `AutonomousDB`, retraining bookkeeping made atomic, a structural no-sqlite3 runtime test added, and targeted regressions green.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-db-unification/34-CONTEXT.md
@.planning/phases/34-db-unification/34-RESEARCH.md
@.planning/phases/34-db-unification/34-01-SUMMARY.md
@.planning/phases/34-db-unification/34-02-SUMMARY.md

@src/bitbat/gui/widgets.py
@src/bitbat/gui/timeline.py
@src/bitbat/autonomous/retrainer.py
@src/bitbat/autonomous/continuous_trainer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock GUI helper and timeline contracts before migration</name>
  <files>tests/gui/test_widgets.py, tests/gui/test_timeline.py, tests/contracts/test_db_unification.py</files>
  <behavior>
    - existing widget/timeline return-shape contracts stay unchanged under the unified DB layer
    - add a structural test that fails if runtime `src/bitbat/` still imports or calls sqlite3 directly
  </behavior>
  <action>
    Extend the GUI tests and add a new structural contract test before touching runtime code.

    Required assertions:
    1. `get_system_status()` still returns `not_started`/`idle`/`active` payloads rather than raising for missing/empty DBs.
    2. `get_latest_prediction()` still returns `None` when appropriate and preserves legacy-column compatibility.
    3. `get_recent_events()` still returns `[]` when appropriate.
    4. `get_timeline_data()` and `list_timeline_filter_options()` preserve current filtering/legacy timestamp behavior.
    5. A structural contract test fails if `src/bitbat/` still contains `import sqlite3` or `sqlite3.connect`.

    Run:
    `poetry run pytest tests/gui/test_widgets.py tests/gui/test_timeline.py tests/contracts/test_db_unification.py -x`

    The new structural test must fail before the runtime migration is complete.
  </action>
  <verify>
    <automated>poetry run pytest tests/gui/test_widgets.py tests/gui/test_timeline.py tests/contracts/test_db_unification.py -x</automated>
  </verify>
  <done>GUI/structural tests exist and fail against the remaining raw sqlite runtime call sites</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate Streamlit widgets and timeline to the unified DB layer</name>
  <files>src/bitbat/gui/widgets.py, src/bitbat/gui/timeline.py</files>
  <behavior>
    - widgets/timeline use AutonomousDB read methods instead of sqlite3
    - existing GUI helper return-shape contracts are preserved
    - legacy-compatible column/timestamp behavior still works
  </behavior>
  <action>
    Refactor the Streamlit helper modules to call the Plan 34 foundation methods from `AutonomousDB`.

    Constraints:
    1. Remove runtime `sqlite3` imports from both files.
    2. Preserve helper return shapes and current empty-state behavior enforced by the GUI tests.
    3. Keep legacy compatibility logic in the DB layer; do not re-implement raw SQL compatibility probing in the GUI files.

    Run:
    `poetry run pytest tests/gui/test_widgets.py tests/gui/test_timeline.py tests/gui/test_complete_gui.py -x`
  </action>
  <verify>
    <automated>poetry run pytest tests/gui/test_widgets.py tests/gui/test_timeline.py tests/gui/test_complete_gui.py -x</automated>
  </verify>
  <done>Widgets/timeline no longer use sqlite3 and GUI regressions remain green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor retraining flows to use atomic DB helpers and close the phase gate</name>
  <files>src/bitbat/autonomous/retrainer.py, src/bitbat/autonomous/continuous_trainer.py, tests/autonomous/test_retrainer.py, tests/autonomous/test_agent_integration.py, tests/contracts/test_db_unification.py</files>
  <behavior>
    - retraining/model activation/event completion use atomic DB-layer helpers
    - failure paths do not partially persist success state
    - structural test now passes because no raw sqlite3 runtime call sites remain in src/bitbat/
  </behavior>
  <action>
    Refactor the write-side runtime flows to use the atomic DB helpers created in Plan 01.

    Required outcomes:
    1. `AutoRetrainer` no longer spreads one logical promotion/event-completion unit across multiple DB sessions.
    2. `ContinuousTrainer` no longer spreads one logical promotion/event-completion unit across multiple DB sessions.
    3. The structural no-sqlite3 test passes across `src/bitbat/`.
    4. Targeted autonomous/CLI regressions pass.

    Run:
    - `poetry run pytest tests/autonomous/test_retrainer.py tests/autonomous/test_agent_integration.py -x`
    - `poetry run pytest tests/contracts/test_db_unification.py tests/test_cli.py -x`
  </action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_retrainer.py tests/autonomous/test_agent_integration.py tests/contracts/test_db_unification.py -x</automated>
  </verify>
  <done>Write-side flows use atomic DB helpers, structural gate passes, and targeted regressions are green</done>
</task>

</tasks>

<verification>
poetry run pytest tests/gui/test_widgets.py tests/gui/test_timeline.py tests/gui/test_complete_gui.py -x
poetry run pytest tests/autonomous/test_retrainer.py tests/autonomous/test_agent_integration.py tests/test_cli.py -x
poetry run pytest tests/contracts/test_db_unification.py -x
</verification>

<success_criteria>
- `src/bitbat/gui/widgets.py` and `src/bitbat/gui/timeline.py` no longer use raw sqlite runtime access
- `AutoRetrainer` and `ContinuousTrainer` use atomic DB-layer transaction helpers
- no raw `sqlite3` runtime call sites remain in `src/bitbat/`
- GUI and autonomous regression tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-db-unification/34-03-SUMMARY.md`
</output>
