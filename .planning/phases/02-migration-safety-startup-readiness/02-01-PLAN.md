---
phase: 02-migration-safety-startup-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/autonomous/schema_compat.py
  - src/bitbat/autonomous/db.py
  - scripts/init_autonomous_db.py
  - tests/autonomous/test_schema_compat.py
  - tests/autonomous/test_init_script.py
autonomous: true
requirements:
  - SCHE-03
must_haves:
  truths:
    - Re-running compatibility upgrade and runtime DB initialization is deterministic and non-destructive.
    - Legacy prediction rows remain intact after repeated upgrade paths.
    - Upgrade surfaces report clear status for "upgraded" vs "already compatible" states.
  artifacts:
    - path: src/bitbat/autonomous/schema_compat.py
      provides: Idempotent upgrade outcome semantics and compatibility status utilities
    - path: scripts/init_autonomous_db.py
      provides: Repeat-safe script upgrade flow with deterministic operator output
    - path: tests/autonomous/test_schema_compat.py
      provides: Repeated upgrade and data-preservation regression assertions
    - path: tests/autonomous/test_init_script.py
      provides: Script-level repeated execution safety coverage
  key_links:
    - from: src/bitbat/autonomous/db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Runtime initialization invoking repeat-safe compatibility enforcement
      pattern: ensure_schema_compatibility|auto_upgrade
    - from: scripts/init_autonomous_db.py
      to: src/bitbat/autonomous/schema_compat.py
      via: CLI upgrade path using shared additive migration logic
      pattern: upgrade_schema_compatibility|audit_schema_compatibility
    - from: tests/autonomous/test_schema_compat.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Direct assertions for idempotency and row preservation
      pattern: test_.*upgrade|idempotent|preserv
---

<objective>
Harden migration safety and idempotency behavior for repeated executions.

Purpose: Ensure schema migration paths remain safe across repeated runtime and operator invocations.
Output: Deterministic migration status semantics and stronger repeat-run preservation guarantees.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-schema-contract-baseline/01-01-SUMMARY.md
@.planning/phases/01-schema-contract-baseline/01-02-SUMMARY.md
@.planning/phases/02-migration-safety-startup-readiness/02-RESEARCH.md
@src/bitbat/autonomous/schema_compat.py
@src/bitbat/autonomous/db.py
@scripts/init_autonomous_db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize compatibility upgrade outcome semantics</name>
  <files>src/bitbat/autonomous/schema_compat.py</files>
  <action>Refine upgrade/ensure helpers so repeated runs have deterministic "already-compatible" behavior and stable structured status metadata (e.g., explicit operation counts and upgrade state), without mutating existing compatible schema/data.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py -q -k "upgrade or idempotent"</automated>
  </verify>
  <done>Schema upgrade helpers clearly distinguish no-op vs applied changes and remain repeat-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Harden runtime + script migration entrypoints</name>
  <files>src/bitbat/autonomous/db.py, scripts/init_autonomous_db.py</files>
  <action>Align runtime and CLI migration entrypoints to use the normalized outcome semantics with consistent operator-readable status; preserve additive-only behavior and avoid destructive fallbacks when compatibility can be restored safely.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_init_script.py -q</automated>
  </verify>
  <done>Runtime and script upgrade entrypoints behave consistently across first-run and repeat-run scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Extend repeat-run preservation regression coverage</name>
  <files>tests/autonomous/test_schema_compat.py, tests/autonomous/test_init_script.py</files>
  <action>Add regression tests that exercise repeated upgrade and initialization cycles while asserting legacy row integrity (key values unchanged, row counts stable, no duplicate schema operations).</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_schema_compat.py tests/autonomous/test_init_script.py -q</automated>
  </verify>
  <done>Automated coverage fails if migration paths become non-idempotent or data-destructive.</done>
</task>

</tasks>

<verification>
- Upgrade behavior is idempotent across repeated execution paths.
- Existing prediction history remains intact after migration operations.
- Migration surfaces report deterministic status states for operators and runtime logs.
</verification>

<success_criteria>
- Additive migration paths remain repeat-safe and non-destructive.
- Runtime and script initialization surfaces expose consistent status semantics.
- Regression tests explicitly protect idempotency and row-preservation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-migration-safety-startup-readiness/02-01-SUMMARY.md`
</output>
