---
phase: 02-migration-safety-startup-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/api/schemas.py
  - src/bitbat/api/routes/health.py
  - src/bitbat/api/routes/analytics.py
  - src/bitbat/api/routes/metrics.py
  - tests/api/test_health.py
  - tests/api/test_metrics.py
autonomous: true
requirements:
  - API-02
must_haves:
  truths:
    - API health/status endpoints report schema compatibility readiness, not only DB file presence.
    - Incompatible schema states are surfaced with actionable diagnostics instead of opaque generic failures.
    - Readiness checks are non-mutating and safe to call repeatedly.
  artifacts:
    - path: src/bitbat/api/routes/health.py
      provides: Schema-aware readiness reporting for health endpoints
    - path: src/bitbat/api/routes/analytics.py
      provides: Status endpoint alignment with schema compatibility signals
    - path: src/bitbat/api/schemas.py
      provides: Response contracts for readiness details
    - path: tests/api/test_health.py
      provides: Regression tests for schema-aware health semantics
    - path: tests/api/test_metrics.py
      provides: Readiness metric signal coverage
  key_links:
    - from: src/bitbat/api/routes/health.py
      to: src/bitbat/autonomous/schema_compat.py
      via: Non-mutating compatibility audit integrated into detailed readiness output
      pattern: audit_schema_compatibility|schema
    - from: src/bitbat/api/routes/analytics.py
      to: src/bitbat/api/schemas.py
      via: Status payload fields exposing compatibility/readiness details
      pattern: SystemStatusResponse|schema|readiness
    - from: tests/api/test_health.py
      to: src/bitbat/api/routes/health.py
      via: Endpoint-level assertions for degraded vs ready compatibility states
      pattern: test_.*health|detailed
---

<objective>
Wire schema readiness into API health/status surfaces with accurate, actionable diagnostics.

Purpose: Ensure readiness endpoints reflect true operational compatibility rather than only file/system presence.
Output: Schema-aware health/status responses and regression tests for incompatible and compatible states.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-migration-safety-startup-readiness/02-RESEARCH.md
@.planning/phases/02-migration-safety-startup-readiness/02-01-SUMMARY.md
@src/bitbat/autonomous/schema_compat.py
@src/bitbat/api/schemas.py
@src/bitbat/api/routes/health.py
@src/bitbat/api/routes/analytics.py
@src/bitbat/api/routes/metrics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schema-aware readiness response contracts</name>
  <files>src/bitbat/api/schemas.py</files>
  <action>Extend API schema models so health/status payloads can express schema compatibility state and missing-column diagnostics without breaking existing endpoint contracts.</action>
  <verify>
    <automated>poetry run pytest tests/api/test_health.py -q -k "services or detailed"</automated>
  </verify>
  <done>Response models can represent schema readiness details required for API-02.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate non-mutating schema readiness in health and status endpoints</name>
  <files>src/bitbat/api/routes/health.py, src/bitbat/api/routes/analytics.py, src/bitbat/api/routes/metrics.py</files>
  <action>Use non-mutating compatibility audits in readiness endpoints to distinguish unavailable DB, incompatible schema, and fully ready states; include actionable details while keeping endpoint behavior deterministic across repeated calls.</action>
  <verify>
    <automated>poetry run pytest tests/api/test_health.py tests/api/test_metrics.py -q</automated>
  </verify>
  <done>Health/status/metrics surfaces expose accurate schema readiness without side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Add incompatible-schema readiness regression tests</name>
  <files>tests/api/test_health.py, tests/api/test_metrics.py</files>
  <action>Add tests that simulate legacy incompatible schema and upgraded-compatible schema to verify readiness output semantics, including explicit missing-column diagnostics and repeat-call stability.</action>
  <verify>
    <automated>poetry run pytest tests/api/test_health.py tests/api/test_metrics.py -q</automated>
  </verify>
  <done>Automated tests fail if readiness surfaces regress to inaccurate or opaque compatibility reporting.</done>
</task>

</tasks>

<verification>
- Readiness endpoints report schema compatibility state explicitly.
- Incompatible schemas produce actionable diagnostics.
- Repeated readiness calls remain non-mutating and deterministic.
</verification>

<success_criteria>
- API readiness/status payloads include schema compatibility semantics.
- `/health/detailed` and `/analytics/status` reflect true readiness when schema is incompatible.
- API readiness regression tests cover both incompatible and compatible paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-migration-safety-startup-readiness/02-02-SUMMARY.md`
</output>
