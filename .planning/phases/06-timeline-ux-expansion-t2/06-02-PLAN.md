---
phase: 06-timeline-ux-expansion-t2
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/gui/timeline.py
  - streamlit/pages/0_Quick_Start.py
  - tests/gui/test_timeline.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - TIM-04
must_haves:
  truths:
    - Timeline exposes always-visible freq/horizon/date-window controls with defaults matching context decisions.
    - Filter selections persist across Streamlit reruns during a session.
    - No-result filter combinations preserve user selections and render explicit empty-state messaging instead of implicit auto-reset.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Filter/window helper logic and deterministic filtered timeline dataset shaping
    - path: streamlit/pages/0_Quick_Start.py
      provides: Visible filter controls, session state persistence, and no-result handling
    - path: tests/gui/test_timeline.py
      provides: Unit regressions for date-window and filter application semantics
    - path: tests/gui/test_complete_gui.py
      provides: Integration checks for filter-state and empty-state behavior
  key_links:
    - from: streamlit/pages/0_Quick_Start.py
      to: src/bitbat/gui/timeline.py
      via: UI filter controls pass through to canonical filter helpers
      pattern: session_state|get_timeline_data|date window
    - from: src/bitbat/gui/timeline.py
      to: tests/gui/test_timeline.py
      via: Filter and window logic is locked by deterministic fixture assertions
      pattern: filter|window|freq|horizon
    - from: tests/gui/test_complete_gui.py
      to: streamlit/pages/0_Quick_Start.py
      via: Integration behavior verifies no-result messaging and persistent filter state
      pattern: timeline|filter|empty
---

<objective>
Implement and validate practical timeline filtering controls with persistence and explicit empty-state semantics.

Purpose: Satisfy TIM-04 by making filtering useful and robust under operational data variability.
Output: Visible and persistent filters + deterministic no-result behavior + regression tests.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-timeline-ux-expansion-t2/06-CONTEXT.md
@.planning/phases/06-timeline-ux-expansion-t2/06-RESEARCH.md
@.planning/phases/06-timeline-ux-expansion-t2/06-01-PLAN.md
@src/bitbat/gui/timeline.py
@streamlit/pages/0_Quick_Start.py
@tests/gui/test_timeline.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical filter/window helpers for timeline data shaping</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Implement helper logic for date-window filtering and reusable timeline shaping that composes with existing freq/horizon selection, with a default last-7-days window and deterministic behavior for small datasets.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "timeline and (filter or window or freq or horizon)"</automated>
  </verify>
  <done>Timeline module exposes stable filter/window semantics suitable for direct UI consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Wire always-visible filters and session persistence in Quick Start timeline</name>
  <files>streamlit/pages/0_Quick_Start.py, src/bitbat/gui/timeline.py</files>
  <action>Add visible `freq`, `horizon`, and date-window controls to the timeline surface with session-state persistence. Preserve selected filters across reruns and show explicit no-result messaging when a selection yields zero events.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py -q -k "timeline and (filter or empty or status)"</automated>
  </verify>
  <done>Quick Start timeline behavior remains interactive, stateful, and explicit under no-result filter combinations.</done>
</task>

<task type="auto">
  <name>Task 3: Add TIM-04 regression tests for filter controls and no-result behavior</name>
  <files>tests/gui/test_timeline.py, tests/gui/test_complete_gui.py</files>
  <action>Extend test matrices for date-window defaults, filter persistence assumptions, and empty-state semantics when no rows match selected controls. Ensure no test expects auto-reset behavior.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py tests/gui/test_complete_gui.py -q -k "timeline and (filter or window or empty)"</automated>
  </verify>
  <done>Regression suite detects drift in TIM-04 filter semantics and no-result messaging behavior.</done>
</task>

</tasks>

<verification>
- Always-visible filters exist for freq/horizon/date window.
- Filter selections persist in session state during page reruns.
- No-result filter combinations surface explicit messaging without mutating selections.
</verification>

<success_criteria>
- TIM-04 practical filtering is user-visible and behaviorally stable.
- Date-window and freq/horizon filters remain semantically aligned with timeline summaries.
- Regression coverage protects filter UX behavior from regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-ux-expansion-t2/06-02-SUMMARY.md`
</output>
