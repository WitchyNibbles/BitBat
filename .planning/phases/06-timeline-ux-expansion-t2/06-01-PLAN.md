---
phase: 06-timeline-ux-expansion-t2
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/gui/timeline.py
  - streamlit/pages/0_Quick_Start.py
  - tests/gui/test_timeline.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - TIM-03
must_haves:
  truths:
    - Timeline events expose clear direction context (color + shape) while confidence remains hover-only and exact when available.
    - Missing confidence is represented explicitly as `n/a` without synthetic values or marker-level confidence cues.
    - Timeline UX includes compact filtered-context summary semantics while keeping the chart surface minimally dense.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Confidence/direction presentation contract and summary helpers
    - path: streamlit/pages/0_Quick_Start.py
      provides: Timeline presentation wiring to chart and summary strip surfaces
    - path: tests/gui/test_timeline.py
      provides: Marker/hover-level confidence and direction regressions
    - path: tests/gui/test_complete_gui.py
      provides: Integration-level status and summary behavior assertions
  key_links:
    - from: src/bitbat/gui/timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Quick Start timeline rendering and summary strip use shared presentation semantics
      pattern: build_timeline_figure|summarize_timeline_status
    - from: src/bitbat/gui/timeline.py
      to: tests/gui/test_timeline.py
      via: Direction/confidence hover semantics and marker style invariants are locked by tests
      pattern: Confidence|Status|predicted_direction|marker
    - from: tests/gui/test_complete_gui.py
      to: streamlit/pages/0_Quick_Start.py
      via: Integration tests preserve timeline summary assumptions consumed in Quick Start
      pattern: summarize_timeline_status|timeline
---

<objective>
Implement confidence/direction timeline presentation improvements aligned to user decisions while preserving the low-noise chart surface.

Purpose: Satisfy TIM-03 by making confidence and direction context explicit, consistent, and testable.
Output: Updated timeline presentation contract + compact summary support + regression coverage.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-timeline-ux-expansion-t2/06-CONTEXT.md
@.planning/phases/06-timeline-ux-expansion-t2/06-RESEARCH.md
@src/bitbat/gui/timeline.py
@streamlit/pages/0_Quick_Start.py
@tests/gui/test_timeline.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Codify TIM-03 confidence/direction presentation semantics in timeline helpers</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Refine timeline presentation helpers so direction context remains color+shape driven and confidence remains hover-only with exact percentage formatting when present and explicit `n/a` when absent. Ensure marker rendering does not introduce confidence-based visual cues on the base chart.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "timeline and (confidence or direction or status)"</automated>
  </verify>
  <done>Timeline helper behavior enforces exact hover confidence semantics and direction encoding rules without increasing base chart density.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact timeline summary-strip data contract for filtered contexts</name>
  <files>src/bitbat/gui/timeline.py, streamlit/pages/0_Quick_Start.py</files>
  <action>Implement and wire a compact summary-strip data path for current timeline context (for example counts/accuracy/average confidence) while keeping the primary plot minimal. Keep summary semantics derived from normalized timeline rows to avoid drift.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py -q -k "timeline and (status or summary or confidence)"</automated>
  </verify>
  <done>Quick Start timeline presents compact analysis summary metrics derived from canonical timeline helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Lock TIM-03 presentation behavior with focused regressions</name>
  <files>tests/gui/test_timeline.py, tests/gui/test_complete_gui.py</files>
  <action>Add/update tests that assert hover confidence exactness, explicit `n/a` behavior, direction presentation stability, and compact summary-strip output semantics under representative fixtures.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py tests/gui/test_complete_gui.py -q -k "timeline and (confidence or direction or summary)"</automated>
  </verify>
  <done>Regression suite fails deterministically if TIM-03 presentation semantics regress.</done>
</task>

</tasks>

<verification>
- Direction and confidence presentation matches locked context decisions.
- Missing confidence remains explicit (`n/a`) and non-synthetic.
- Compact summary-strip semantics remain aligned with normalized timeline data.
</verification>

<success_criteria>
- TIM-03 confidence/direction context is explicit, consistent, and user-visible.
- Base chart remains minimal while hover and summary views carry dense analytics.
- Regression coverage protects presentation semantics from future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/06-timeline-ux-expansion-t2/06-01-SUMMARY.md`
</output>
