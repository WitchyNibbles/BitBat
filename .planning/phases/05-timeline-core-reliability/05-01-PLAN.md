---
phase: 05-timeline-core-reliability
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/gui/timeline.py
  - tests/gui/test_timeline.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - TIM-01
must_haves:
  truths:
    - Timeline data loading returns a stable normalized contract for both recent and historical predictions across active freq/horizon pairs.
    - Mixed prediction rows (legacy probability-only and return/price-first rows) are normalized without crashing or semantic ambiguity.
    - Timeline consumers can derive rendering and summary behavior from explicit normalized status fields instead of ad hoc null checks.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Canonical timeline read-model normalization and schema-tolerant query logic
    - path: tests/gui/test_timeline.py
      provides: Unit/regression coverage for normalized timeline data semantics
    - path: tests/gui/test_complete_gui.py
      provides: GUI integration coverage for normalized prediction payload usage
  key_links:
    - from: src/bitbat/gui/timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Quick Start timeline fragment consumes normalized timeline dataframe fields
      pattern: get_timeline_data|_live_timeline
    - from: src/bitbat/gui/timeline.py
      to: tests/gui/test_timeline.py
      via: Timeline normalization contract is locked via fixture-based regressions
      pattern: get_timeline_data|prediction_status|confidence
    - from: src/bitbat/gui/timeline.py
      to: src/bitbat/autonomous/models.py
      via: Query/normalization aligns with canonical prediction_outcomes schema fields
      pattern: predicted_return|predicted_price|correct|actual_return
---

<objective>
Build/upgrade the timeline read-model normalization layer.

Purpose: Make timeline data retrieval reliable across operational row shapes and active freq/horizon settings.
Output: A canonical timeline dataframe contract that downstream rendering can consume deterministically.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-timeline-core-reliability/05-RESEARCH.md
@src/bitbat/gui/timeline.py
@src/bitbat/autonomous/models.py
@streamlit/pages/0_Quick_Start.py
@tests/gui/test_timeline.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical timeline row normalization helpers</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Add explicit normalization helpers that convert raw DB rows into a stable timeline contract (`prediction_status`, `is_realized`, normalized `correct`, nullable `confidence`, normalized return/price fields). Keep semantics backward-compatible for legacy probability rows while prioritizing return/price-first rows from Phase 4.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "get_timeline_data or filters or empty"</automated>
  </verify>
  <done>`get_timeline_data` emits deterministic normalized columns for mixed pending/realized prediction records without relying on implicit defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Harden timeline query path for mixed schema payload reliability</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Refactor timeline SQL/select and dataframe shaping so missing optional columns/values do not break output. Ensure freq/horizon filtering, timestamp parsing, and ordering remain stable for both small and larger historical windows.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q</automated>
  </verify>
  <done>Timeline query + normalization paths remain reliable across operational datasets containing nullable and mixed semantic fields.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression fixtures for normalized timeline data behavior</name>
  <files>tests/gui/test_timeline.py, tests/gui/test_complete_gui.py</files>
  <action>Add focused fixture coverage for mixed freq/horizon rows, mixed pending/realized rows, and mixed legacy/new semantic columns. Assert normalized status and confidence semantics are stable and consumable by GUI integration helpers.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py tests/gui/test_complete_gui.py -q -k "timeline or prediction or status"</automated>
  </verify>
  <done>Regression tests fail if timeline normalization regresses for recent/historical operational prediction data.</done>
</task>

</tasks>

<verification>
- Timeline read-model output remains stable for mixed pending/realized and mixed semantic prediction rows.
- Freq/horizon filtering and timestamp ordering are deterministic under representative fixtures.
- GUI integration tests confirm normalized fields remain consumable by timeline surfaces.
</verification>

<success_criteria>
- TIM-01 timeline data reliability is established at the read-model boundary.
- Timeline consumers can rely on explicit normalized status semantics rather than implicit null checks.
- Regression coverage prevents schema-shape drift from breaking timeline data loading.
</success_criteria>

<output>
After completion, create `.planning/phases/05-timeline-core-reliability/05-01-SUMMARY.md`
</output>
