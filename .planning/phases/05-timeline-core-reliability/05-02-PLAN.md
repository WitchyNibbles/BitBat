---
phase: 05-timeline-core-reliability
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/gui/timeline.py
  - streamlit/pages/0_Quick_Start.py
  - tests/gui/test_timeline.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - TIM-02
must_haves:
  truths:
    - Timeline markers and hover details clearly distinguish pending predictions from realized outcomes.
    - Timeline rendering remains resilient when market price coverage is sparse or partially missing for prediction timestamps.
    - Quick Start timeline summary metrics report completed/correct/pending counts consistently from normalized timeline status fields.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Status-driven marker rendering and sparse-price fallback behavior
    - path: streamlit/pages/0_Quick_Start.py
      provides: Timeline widget consumer aligned with normalized pending/realized semantics
    - path: tests/gui/test_timeline.py
      provides: Rendering-level regressions for pending/realized visual semantics
    - path: tests/gui/test_complete_gui.py
      provides: Integration assertions for timeline summary metric correctness
  key_links:
    - from: src/bitbat/gui/timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Figure + summary logic consume shared normalized status semantics
      pattern: build_timeline_figure|prediction_status|correct
    - from: src/bitbat/gui/timeline.py
      to: src/bitbat/gui/timeline.py
      via: Marker placement falls back from nearest price lookup to prediction-level fallback values
      pattern: get_price_series|build_timeline_figure|predicted_price
    - from: tests/gui/test_timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Pending/realized distinction and metric assumptions stay aligned with page consumption
      pattern: pending|realized|accuracy
---

<objective>
Close remaining TIM-02 gaps after 05-01 normalization by aligning UI consumption and locking render semantics with targeted regressions.

Purpose: Ensure timeline status semantics are consistently expressed in both figure behavior and Quick Start summary outputs.
Output: Status-consistent Quick Start timeline surface plus regression coverage for pending/realized and sparse-price behavior.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-timeline-core-reliability/05-RESEARCH.md
@.planning/phases/05-timeline-core-reliability/05-01-PLAN.md
@src/bitbat/gui/timeline.py
@streamlit/pages/0_Quick_Start.py
@tests/gui/test_timeline.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock status-driven marker semantics with explicit regression assertions</name>
  <files>src/bitbat/gui/timeline.py, tests/gui/test_timeline.py</files>
  <action>Audit `build_timeline_figure` status behavior from 05-01 and add/adjust tests to assert marker styling + hover semantics for `pending`, `realized_correct`, and `realized_wrong`. Keep direction styling deterministic and ensure regressions fail if semantics drift.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "build_timeline_figure and (pending or realized or status)"</automated>
  </verify>
  <done>Render-level tests prove timeline markers/hover text explicitly differentiate pending and realized outcomes via normalized status fields.</done>
</task>

<task type="auto">
  <name>Task 2: Align Quick Start timeline metrics and legend with normalized status summaries</name>
  <files>streamlit/pages/0_Quick_Start.py, src/bitbat/gui/timeline.py</files>
  <action>Refactor Quick Start timeline summary derivation to use `summarize_timeline_status`/`prediction_status` semantics instead of direct `correct` null checks. Update legend copy so pending/realized meaning matches status-driven rendering and summary counts.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py -q -k "timeline and status"</automated>
  </verify>
  <done>Quick Start metrics report completed/correct/pending counts from normalized status semantics with consistent legend language.</done>
</task>

<task type="auto">
  <name>Task 3: Add sparse-price fallback regression coverage tied to UI consumption assumptions</name>
  <files>tests/gui/test_timeline.py, tests/gui/test_complete_gui.py</files>
  <action>Add regression cases where price series is missing/partial but `predicted_price` exists so timeline markers still render. Ensure GUI integration assertions remain aligned with these fallback semantics and status-driven summaries.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py tests/gui/test_complete_gui.py -q -k "timeline or status or fallback"</automated>
  </verify>
  <done>Timeline and GUI tests fail deterministically if sparse-price fallback or status-consumption semantics regress.</done>
</task>

</tasks>

<verification>
- Marker styling/hover semantics for pending vs realized outcomes are explicitly validated.
- Sparse-price fallback behavior remains visible and deterministic in regression coverage.
- Quick Start timeline summaries/legend consume normalized status semantics consistently.
</verification>

<success_criteria>
- TIM-02 pending/realized distinction is explicit and stable in timeline rendering.
- Timeline outcome signals remain reliable under real operational data delays and sparsity.
- Regression tests protect visual/metric semantics from future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/05-timeline-core-reliability/05-02-SUMMARY.md`
</output>
