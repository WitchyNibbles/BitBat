---
phase: 05-timeline-core-reliability
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/gui/timeline.py
  - streamlit/pages/0_Quick_Start.py
  - tests/gui/test_timeline.py
  - tests/gui/test_complete_gui.py
autonomous: true
requirements:
  - TIM-02
must_haves:
  truths:
    - Timeline markers and hover details clearly distinguish pending predictions from realized outcomes.
    - Timeline rendering remains resilient when market price coverage is sparse or partially missing for prediction timestamps.
    - Quick Start timeline summary metrics report completed/correct/pending counts consistently from normalized timeline status fields.
  artifacts:
    - path: src/bitbat/gui/timeline.py
      provides: Status-driven marker rendering and sparse-price fallback behavior
    - path: streamlit/pages/0_Quick_Start.py
      provides: Timeline widget consumer aligned with normalized pending/realized semantics
    - path: tests/gui/test_timeline.py
      provides: Rendering-level regressions for pending/realized visual semantics
    - path: tests/gui/test_complete_gui.py
      provides: Integration assertions for timeline summary metric correctness
  key_links:
    - from: src/bitbat/gui/timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Figure + summary logic consume shared normalized status semantics
      pattern: build_timeline_figure|prediction_status|correct
    - from: src/bitbat/gui/timeline.py
      to: src/bitbat/gui/timeline.py
      via: Marker placement falls back from nearest price lookup to prediction-level fallback values
      pattern: get_price_series|build_timeline_figure|predicted_price
    - from: tests/gui/test_timeline.py
      to: streamlit/pages/0_Quick_Start.py
      via: Pending/realized distinction and metric assumptions stay aligned with page consumption
      pattern: pending|realized|accuracy
---

<objective>
Repair timeline rendering for mixed pending/realized datasets.

Purpose: Make realized outcome alignment and pending state visibility explicit and robust in timeline UI behavior.
Output: Status-aware timeline figure rendering and Quick Start timeline metrics that remain reliable under sparse or delayed data.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-timeline-core-reliability/05-RESEARCH.md
@.planning/phases/05-timeline-core-reliability/05-01-PLAN.md
@src/bitbat/gui/timeline.py
@streamlit/pages/0_Quick_Start.py
@tests/gui/test_timeline.py
@tests/gui/test_complete_gui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor timeline marker rendering to use explicit prediction status semantics</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Update `build_timeline_figure` to style markers and hover content from normalized status fields (`pending`, `realized_correct`, `realized_wrong`) instead of implicit confidence/correct null checks. Preserve direction styling while making realized outcome state explicit in hover and marker treatment.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "build_timeline_figure or pending or realized"</automated>
  </verify>
  <done>Figure output visually and semantically distinguishes pending vs realized outcomes using deterministic status rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add resilient marker placement for sparse or missing price coverage</name>
  <files>src/bitbat/gui/timeline.py</files>
  <action>Improve marker y-position logic so predictions are still rendered when nearest price lookup fails (for example, use `predicted_price` fallback when available, and avoid silently dropping valid prediction events). Keep behavior deterministic and tested for partial price windows.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_timeline.py -q -k "empty_prices or sparse or fallback"</automated>
  </verify>
  <done>Predictions remain visible in timeline output even when the available price series is incomplete for some timestamps.</done>
</task>

<task type="auto">
  <name>Task 3: Align Quick Start timeline metrics and legends with normalized status fields</name>
  <files>streamlit/pages/0_Quick_Start.py, tests/gui/test_complete_gui.py</files>
  <action>Refactor timeline summary metric derivation and legend text in Quick Start to consume normalized timeline status fields. Ensure completed/correct/accuracy values and pending semantics remain coherent when mixed pending/realized records are present.</action>
  <verify>
    <automated>poetry run pytest tests/gui/test_complete_gui.py -q -k "timeline or prediction or status"</automated>
  </verify>
  <done>Quick Start timeline surface reports outcome metrics consistently for mixed pending/realized records.</done>
</task>

</tasks>

<verification>
- Pending and realized predictions are clearly differentiated in marker styling and hover semantics.
- Timeline figure remains populated under partial price-series coverage scenarios.
- Quick Start timeline metrics align with normalized status semantics.
</verification>

<success_criteria>
- TIM-02 pending/realized distinction is explicit and stable in timeline rendering.
- Timeline outcome signals remain reliable under real operational data delays and sparsity.
- Regression tests protect visual/metric semantics from future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/05-timeline-core-reliability/05-02-SUMMARY.md`
</output>
