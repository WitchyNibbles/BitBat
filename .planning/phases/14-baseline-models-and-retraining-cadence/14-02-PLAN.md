---
phase: 14-baseline-models-and-retraining-cadence
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/bitbat/dataset/splits.py
  - src/bitbat/model/walk_forward.py
  - src/bitbat/autonomous/continuous_trainer.py
  - src/bitbat/autonomous/retrainer.py
  - src/bitbat/cli.py
  - src/bitbat/config/default.yaml
  - tests/model/test_walk_forward.py
  - tests/autonomous/test_retrainer.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MODL-02
must_haves:
  truths:
    - Retraining and backtest windows are configurable and applied consistently across CV and retraining paths.
    - Rolling walk-forward execution uses explicit train/backtest window definitions rather than implicit one-shot spans.
    - Window configuration is reproducible from CLI/config without manual timestamp surgery.
  artifacts:
    - path: src/bitbat/dataset/splits.py
      provides: Window-generation utilities for rolling train/backtest folds
    - path: src/bitbat/model/walk_forward.py
      provides: Walk-forward execution aligned to configurable window cadence
    - path: src/bitbat/autonomous/retrainer.py
      provides: Retraining orchestration that consumes configurable window semantics
  key_links:
    - from: src/bitbat/cli.py
      to: src/bitbat/dataset/splits.py
      via: CLI window options compile into deterministic fold schedules
      pattern: train_window|backtest_window|walk_forward|window
    - from: src/bitbat/autonomous/retrainer.py
      to: tests/autonomous/test_retrainer.py
      via: Retraining loop behavior is locked to configurable window cadence
      pattern: retrain|window|rolling|cadence
---

<objective>
Introduce configurable rolling retraining/backtest windows across model and autonomous flows.

Purpose: Fulfill MODL-02 by replacing ad-hoc training spans with explicit window cadence.
Output: Window-aware split generation, retraining integration, and configuration-driven CLI wiring.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-CONTEXT.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-01-PLAN.md
@src/bitbat/dataset/splits.py
@src/bitbat/model/walk_forward.py
@src/bitbat/autonomous/continuous_trainer.py
@src/bitbat/autonomous/retrainer.py
@src/bitbat/cli.py
@src/bitbat/config/default.yaml
@tests/model/test_walk_forward.py
@tests/autonomous/test_retrainer.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit rolling-window schedule utilities for train/backtest cycles</name>
  <files>src/bitbat/dataset/splits.py, tests/model/test_walk_forward.py</files>
  <action>Implement deterministic utilities that produce rolling train/backtest windows from configurable durations and anchor timestamps. Ensure fold generation is time-ordered, reproducible, and compatible with existing walk-forward consumers.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_walk_forward.py -q -k "window or rolling or fold"</automated>
  </verify>
  <done>Train/backtest windows are explicit, configurable, and regression-covered.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate windowed retraining cadence into autonomous retrainer and walk-forward paths</name>
  <files>src/bitbat/model/walk_forward.py, src/bitbat/autonomous/continuous_trainer.py, src/bitbat/autonomous/retrainer.py, tests/autonomous/test_retrainer.py</files>
  <action>Refactor retraining flow so model validation/training follows configurable rolling windows instead of fixed one-year spans or implicit splits. Ensure autonomous retraining events persist window metadata for auditability.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_retrainer.py tests/model/test_walk_forward.py -q -k "retrain or window or rolling"</automated>
  </verify>
  <done>Retraining and walk-forward execution share explicit rolling-window cadence.</done>
</task>

<task type="auto">
  <name>Task 3: Expose window controls through config/CLI with backward-compatible defaults</name>
  <files>src/bitbat/cli.py, src/bitbat/config/default.yaml, tests/test_cli.py</files>
  <action>Add CLI/config controls for train/backtest window durations and reuse them in `model cv` / retraining flows. Preserve current command defaults while making rolling window behavior explicit and operator-configurable.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and (cv or train) and (window or rolling or retrain)"</automated>
  </verify>
  <done>Operators can run reproducible rolling retraining cycles via config/CLI without manual window edits.</done>
</task>

</tasks>

<verification>
- Rolling train/backtest windows are configurable and deterministic.
- Retraining and CV consume shared window semantics.
- CLI/config controls expose cadence without breaking existing defaults.
</verification>

<success_criteria>
- MODL-02 is satisfied with configurable rolling retraining/backtest windows.
- No manual timestamp management is required for recurring retraining cycles.
- Window cadence is test-locked across model and autonomous paths.
</success_criteria>

<output>
After completion, create `.planning/phases/14-baseline-models-and-retraining-cadence/14-02-SUMMARY.md`
</output>
