---
phase: 14-baseline-models-and-retraining-cadence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bitbat/model/train.py
  - src/bitbat/model/persist.py
  - src/bitbat/cli.py
  - tests/model/test_train.py
  - tests/model/test_persist.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MODL-01
must_haves:
  truths:
    - XGBoost and RandomForest baselines train from the same feature/target contract and split semantics.
    - Baseline artifacts are versioned with model-family metadata so downstream comparison is reproducible.
    - Default model flows remain backward-compatible while enabling explicit baseline-family selection.
  artifacts:
    - path: src/bitbat/model/train.py
      provides: Unified tree-baseline training entrypoints for xgboost and random-forest families
    - path: src/bitbat/model/persist.py
      provides: Family-aware model artifact persistence and loading helpers
    - path: src/bitbat/cli.py
      provides: Operator interface to train/evaluate baseline families from one dataset contract
  key_links:
    - from: src/bitbat/model/train.py
      to: src/bitbat/model/persist.py
      via: Baseline trainers persist comparable artifacts per model family
      pattern: xgb|random_forest|baseline|artifact
    - from: src/bitbat/cli.py
      to: tests/model/test_train.py
      via: CLI model-family wiring is regression-locked against baseline training contracts
      pattern: model train|cv|family|baseline
---

<objective>
Establish dual tree-ensemble baselines with comparable artifacts under one dataset contract.

Purpose: Fulfill MODL-01 by making XGBoost and RandomForest first-class baseline families.
Output: Shared baseline training/persistence APIs and CLI coverage for model-family selection.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-CONTEXT.md
@src/bitbat/model/train.py
@src/bitbat/model/persist.py
@src/bitbat/model/evaluate.py
@src/bitbat/cli.py
@tests/model/test_train.py
@tests/model/test_persist.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RandomForest baseline training alongside XGBoost under a shared interface</name>
  <files>src/bitbat/model/train.py, tests/model/test_train.py</files>
  <action>Extend model training utilities with a RandomForest baseline path while preserving existing XGBoost behavior. Ensure both families consume identical `X`/`y` contracts and deterministic seed behavior, and add targeted tests for family-specific training outputs.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_train.py -q -k "xgb or random or baseline"</automated>
  </verify>
  <done>Both tree-ensemble baselines train reproducibly from one contract with test-backed behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement family-aware artifact persistence for comparable baseline outputs</name>
  <files>src/bitbat/model/persist.py, tests/model/test_persist.py</files>
  <action>Update persistence helpers to store/load both baseline families with explicit metadata and stable artifact paths. Keep existing XGBoost artifacts intact while enabling RandomForest artifact lifecycle without ad-hoc file handling.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_persist.py tests/model/test_train.py -q -k "persist or random or xgb"</automated>
  </verify>
  <done>Baseline artifacts are family-aware, reproducible, and backwards-compatible.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI model family selection for baseline train/cv entrypoints</name>
  <files>src/bitbat/cli.py, tests/test_cli.py</files>
  <action>Add CLI/config controls so operators can run XGBoost, RandomForest, or both baseline families from the same dataset contract. Persist comparable metric outputs and ensure defaults maintain existing behavior unless family overrides are provided.</action>
  <verify>
    <automated>poetry run pytest tests/test_cli.py -q -k "model and (train or cv) and (baseline or random or family)"</automated>
  </verify>
  <done>Operators can run baseline families explicitly with no default-path regressions.</done>
</task>

</tasks>

<verification>
- XGBoost and RandomForest baselines use the same dataset contract and deterministic split logic.
- Baseline artifact schema supports family comparison without path ambiguity.
- CLI flows can select baseline family while preserving backward compatibility.
</verification>

<success_criteria>
- MODL-01 is satisfied with dual tree-baseline training and comparable artifacts.
- Baseline outputs are deterministic and regression-tested.
- Existing model workflows remain compatible unless family overrides are requested.
</success_criteria>

<output>
After completion, create `.planning/phases/14-baseline-models-and-retraining-cadence/14-01-SUMMARY.md`
</output>
