---
phase: 14-baseline-models-and-retraining-cadence
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/bitbat/model/evaluate.py
  - src/bitbat/model/walk_forward.py
  - src/bitbat/autonomous/drift.py
  - src/bitbat/autonomous/continuous_trainer.py
  - src/bitbat/cli.py
  - tests/model/test_evaluate.py
  - tests/model/test_walk_forward.py
  - tests/autonomous/test_drift.py
  - tests/test_cli.py
autonomous: true
requirements:
  - MODL-03
must_haves:
  truths:
    - Regime/drift diagnostics are computed and persisted per retraining window.
    - Diagnostics are tied to the same fold/window metadata as baseline model metrics.
    - Window-level diagnostics are deterministic and regression-tested for downstream evaluation use.
  artifacts:
    - path: src/bitbat/model/evaluate.py
      provides: Regime/drift diagnostic calculations and artifact writers
    - path: src/bitbat/model/walk_forward.py
      provides: Fold-level integration of diagnostics with window metadata
    - path: src/bitbat/autonomous/drift.py
      provides: Runtime drift checks aligned with persisted window diagnostics
  key_links:
    - from: src/bitbat/model/walk_forward.py
      to: src/bitbat/model/evaluate.py
      via: Fold execution emits diagnostics through shared evaluation helpers
      pattern: fold|window|regime|diagnostic
    - from: src/bitbat/autonomous/drift.py
      to: tests/autonomous/test_drift.py
      via: Drift thresholds and window outputs are locked by automated tests
      pattern: drift|window_days|threshold|diagnostic
---

<objective>
Emit regime and drift diagnostics per retraining window as first-class evaluation artifacts.

Purpose: Fulfill MODL-03 so retraining quality can be interpreted by market regime, not only aggregate error.
Output: Window-level diagnostics in model/autonomous outputs with regression coverage.
</objective>

<execution_context>
@/home/eimi/.codex/get-shit-done/workflows/execute-plan.md
@/home/eimi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@deep-research-report.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-CONTEXT.md
@.planning/phases/14-baseline-models-and-retraining-cadence/14-02-PLAN.md
@src/bitbat/model/evaluate.py
@src/bitbat/model/walk_forward.py
@src/bitbat/autonomous/drift.py
@src/bitbat/autonomous/continuous_trainer.py
@src/bitbat/cli.py
@tests/model/test_evaluate.py
@tests/model/test_walk_forward.py
@tests/autonomous/test_drift.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic regime/drift diagnostic metrics for retraining windows</name>
  <files>src/bitbat/model/evaluate.py, tests/model/test_evaluate.py</files>
  <action>Implement reusable helpers that compute regime/drift diagnostics (for example volatility regime, directional stability, and performance deltas) from windowed prediction outcomes. Ensure calculations are deterministic and serializable for downstream reporting.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_evaluate.py -q -k "drift or regime or diagnostic"</automated>
  </verify>
  <done>Diagnostic primitives exist and are test-covered for deterministic window analysis.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate diagnostics into walk-forward and continuous retraining outputs</name>
  <files>src/bitbat/model/walk_forward.py, src/bitbat/autonomous/continuous_trainer.py, tests/model/test_walk_forward.py</files>
  <action>Attach regime/drift diagnostics to each walk-forward fold and retraining cycle, including window identifiers and baseline-family metadata. Persist diagnostics in machine-readable artifacts so later evaluation/promotion phases can reuse them directly.</action>
  <verify>
    <automated>poetry run pytest tests/model/test_walk_forward.py tests/model/test_evaluate.py -q -k "diagnostic or regime or fold"</automated>
  </verify>
  <done>Per-window diagnostics are emitted and persisted alongside fold/retraining outputs.</done>
</task>

<task type="auto">
  <name>Task 3: Align drift-monitoring and CLI reporting with window diagnostic artifacts</name>
  <files>src/bitbat/autonomous/drift.py, src/bitbat/cli.py, tests/autonomous/test_drift.py, tests/test_cli.py</files>
  <action>Update drift-monitoring and operator CLI reporting to surface the new window diagnostics without breaking existing monitor flows. Add regression tests confirming diagnostics are present, bounded, and traceable to retraining windows.</action>
  <verify>
    <automated>poetry run pytest tests/autonomous/test_drift.py tests/test_cli.py -q -k "drift or regime or diagnostic"</automated>
  </verify>
  <done>Regime/drift diagnostics are visible in runtime tooling and linked to windowed retraining history.</done>
</task>

</tasks>

<verification>
- Regime/drift diagnostics are emitted for each retraining window.
- Diagnostic artifacts align with fold/window metadata and baseline family context.
- Autonomous drift logic and CLI outputs consume diagnostics without compatibility regressions.
</verification>

<success_criteria>
- MODL-03 is satisfied with deterministic per-window regime/drift diagnostics.
- Diagnostics are persisted for downstream selection and promotion guardrails.
- Window diagnostic behavior is regression-covered across model and autonomous modules.
</success_criteria>

<output>
After completion, create `.planning/phases/14-baseline-models-and-retraining-cadence/14-03-SUMMARY.md`
</output>
