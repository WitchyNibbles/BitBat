# Legacy Static Shell

`web/` is no longer the authoritative operator console.

- Primary frontend surface: `dashboard/`
- Purpose of this folder: legacy demo/archive assets only
- Source of truth for paper-trading state: `src/bitbat_v2/api/app.py` consumed by the React dashboard
